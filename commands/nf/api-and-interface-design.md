---
name: nf:api-and-interface-design
description: Design stable APIs and interfaces — contract-first, consistent errors, boundary validation
argument-hint: "[API or interface to design]"
---

Load the `nf:api-and-interface-design` skill, then apply it to the user's input.

<steps>
1. Use the `skill` tool to load the **nf:api-and-interface-design** skill.
2. Follow the loaded skill instructions to process `$ARGUMENTS`.
</steps>
