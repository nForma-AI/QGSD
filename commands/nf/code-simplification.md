---
name: nf:code-simplification
description: Systematically simplify code — reduce complexity, remove dead code, flatten abstractions
argument-hint: "[file or module to simplify]"
---

Load the `nf:code-simplification` skill, then apply it to the user's input.

<steps>
1. Use the `skill` tool to load the **nf:code-simplification** skill.
2. Follow the loaded skill instructions to process `$ARGUMENTS`.
</steps>
