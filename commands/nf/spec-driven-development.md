---
name: nf:spec-driven-development
description: Write specifications before code — surface assumptions, define success criteria, establish boundaries
argument-hint: "[feature or project to specify]"
---

Load the `nf:spec-driven-development` skill, then apply it to the user's input.

<steps>
1. Use the `skill` tool to load the **nf:spec-driven-development** skill.
2. Follow the loaded skill instructions to process `$ARGUMENTS`.
</steps>
