---
name: nf:incremental-implementation
description: Build in thin vertical slices — implement, test, verify, commit, repeat
argument-hint: "[feature to build incrementally]"
---

Load the `nf:incremental-implementation` skill, then apply it to the user's input.

<steps>
1. Use the `skill` tool to load the **nf:incremental-implementation** skill.
2. Follow the loaded skill instructions to process `$ARGUMENTS`.
</steps>
