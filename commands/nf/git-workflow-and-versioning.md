---
name: nf:git-workflow-and-versioning
description: Git workflow discipline — atomic commits, branch management, pre-commit checks, versioning
argument-hint: "[git workflow question or setup task]"
---

Load the `nf:git-workflow-and-versioning` skill, then apply it to the user's input.

<steps>
1. Use the `skill` tool to load the **nf:git-workflow-and-versioning** skill.
2. Follow the loaded skill instructions to process `$ARGUMENTS`.
</steps>
