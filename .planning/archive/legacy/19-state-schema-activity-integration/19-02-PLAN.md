---
phase: 19-state-schema-activity-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/resume-project.md
  - ~/.claude/qgsd/workflows/resume-project.md
autonomous: true
requirements: [INTG-02]

must_haves:
  truths:
    - "Running /qgsd:resume-work after an interrupted discover step routes to '/qgsd:fix-tests — re-trigger discovery'"
    - "Running /qgsd:resume-work after an interrupted running_batch (activity=maintain_tests) step routes to '/qgsd:fix-tests — load state file, re-run batch N of M'"
    - "Running /qgsd:resume-work after an interrupted categorizing_batch (activity=maintain_tests) step routes to the categorization recovery command"
    - "Running /qgsd:resume-work after an interrupted actioning_batch (activity=maintain_tests) step routes to the action dispatch recovery command"
    - "Running /qgsd:resume-work after an interrupted verifying_batch (activity=maintain_tests) step routes to verification recovery"
    - "Running /qgsd:resume-work on a complete (activity=maintain_tests) session routes to summary/clear action"
    - "Source (get-shit-done/workflows/resume-project.md) and installed (~/.claude/qgsd/workflows/resume-project.md) each have all 6 new rows; both files are edited in place independently"
  artifacts:
    - path: "get-shit-done/workflows/resume-project.md"
      provides: "6 new routing rows for maintain_tests sub-activities"
      contains: "discovering_tests"
    - path: "~/.claude/qgsd/workflows/resume-project.md"
      provides: "Installed copy of routing table with 6 new rows (qgsd:resume-work reads THIS file)"
      contains: "discovering_tests"
  key_links:
    - from: "resume-project.md routing table"
      to: "discovering_tests sub_activity"
      via: "activity=maintain_tests qualifier row"
      pattern: "discovering_tests"
    - from: "resume-project.md routing table"
      to: "running_batch (activity=maintain_tests)"
      via: "qualified sub_activity row to disambiguate from other running_batch activities"
      pattern: "running_batch.*maintain_tests"
---

<objective>
Add 6 new routing rows to the resume-project.md routing table in two files — the source copy and the QGSD-installed copy — so that interrupted /qgsd:fix-tests sessions route correctly back to the exact interrupted step when the user runs /qgsd:resume-work.

Purpose: Without these rows, an interrupted maintain-tests session appears orphaned in resume-work — the user sees no recovery option and must manually determine where to restart. The routing rows close INTG-02 and make the fix-tests tool production-safe for long-running 20k+ suite maintenance sessions.

Output: Updated source and installed copies of resume-project.md, each independently edited in place to add 6 new routing rows covering all maintain_tests sub_activity values defined in the Phase 19 research.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-state-schema-activity-integration/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 maintain_tests routing rows to the source copy (get-shit-done/workflows/resume-project.md)</name>
  <files>get-shit-done/workflows/resume-project.md</files>
  <action>
Read get-shit-done/workflows/resume-project.md. Locate the routing table inside the "determine_next_action" step. The table currently ends with:

  | planning (activity=quick) | `/qgsd:quick` — quick task planning was in progress |

Immediately after that row (before the blank line and the paragraph beginning "Present the matched recovery option..."), insert the following 6 rows:

  | discovering_tests (activity=maintain_tests) | `/qgsd:fix-tests` — re-trigger discovery; no state file means fresh run |
  | running_batch (activity=maintain_tests) | `/qgsd:fix-tests` — load state file, re-run batch {batch} of {batch_total} |
  | categorizing_batch (activity=maintain_tests) | `/qgsd:fix-tests` — load state, batch results on disk, re-enter categorization for batch {batch} |
  | actioning_batch (activity=maintain_tests) | `/qgsd:fix-tests` — load state, dispatch quick tasks for batch {batch} failures |
  | verifying_batch (activity=maintain_tests) | `/qgsd:fix-tests` — load state, re-run verification for actioned tests in batch {batch} |
  | complete (activity=maintain_tests) | `/qgsd:fix-tests` — session already complete; print summary and clear activity state |

Disambiguation requirements (CRITICAL — from Pitfall 5 in the research):
- running_batch, categorizing_batch, actioning_batch, and verifying_batch MUST carry the "(activity=maintain_tests)" qualifier. Future activities may use the same sub_activity names; the qualifier prevents mis-routing. This is the same pattern established in Phase 15 for the ACT-04 fix.
- discovering_tests and complete also carry "(activity=maintain_tests)" for consistency and future safety, even though they are currently unique.

Use the Edit tool to insert these rows. Do NOT remove or reorder any existing rows. Do NOT add section headers or blank lines inside the table — keep it as a contiguous markdown table. Do NOT use cp to propagate changes; this file and the installed copy must be edited independently (they differ in path references: this source uses tilde paths, the installed copy uses absolute paths).
  </action>
  <verify>
1. New rows present in source:
   grep -c 'maintain_tests' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
   Expected: 6

2. Specific rows present:
   grep 'discovering_tests' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
   Expected: one line containing "discovering_tests" and "fix-tests"

   grep 'running_batch.*maintain_tests' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
   Expected: one line containing the running_batch maintain_tests row

3. Existing rows still intact:
   grep 'executing_plan\|planning (activity=quick)' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
   Expected: both original rows present

4. All 6 sub_activities covered:
   grep -E 'discovering_tests|running_batch.*maintain|categorizing_batch|actioning_batch|verifying_batch.*maintain|complete.*maintain' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md | wc -l
   Expected: 6
  </verify>
  <done>
- 6 new routing rows present in the routing table in get-shit-done/workflows/resume-project.md
- Each row covers one maintain_tests sub_activity: discovering_tests, running_batch, categorizing_batch, actioning_batch, verifying_batch, complete
- All ambiguous sub_activity values carry the (activity=maintain_tests) qualifier
- All pre-existing routing rows are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the same 6 routing rows to the QGSD installed copy (~/.claude/qgsd/workflows/resume-project.md)</name>
  <files>~/.claude/qgsd/workflows/resume-project.md</files>
  <action>
Read /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md. This is the file that /qgsd:resume-work actually reads at runtime (verified: ~/.claude/gsd-local-patches/commands/qgsd/resume-work.md overrides the skill to point to this path). The get-shit-done installed copy at ~/.claude/get-shit-done/workflows/resume-project.md is NOT read by qgsd:resume-work and must NOT be touched.

CRITICAL — do NOT use cp from the source file. The source (get-shit-done/workflows/resume-project.md) uses tilde paths (e.g., ~/.claude/qgsd/bin/gsd-tools.cjs). This installed copy uses absolute paths (e.g., /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs). A cp would overwrite absolute paths with tilde paths, breaking the installed runtime file. Use the Edit tool on this file directly.

Locate the routing table inside the "determine_next_action" step. The table currently ends with:

  | planning (activity=quick) | `/qgsd:quick` — quick task planning was in progress |

Immediately after that row (before the blank line and the paragraph beginning "Present the matched recovery option..."), insert the identical 6 rows:

  | discovering_tests (activity=maintain_tests) | `/qgsd:fix-tests` — re-trigger discovery; no state file means fresh run |
  | running_batch (activity=maintain_tests) | `/qgsd:fix-tests` — load state file, re-run batch {batch} of {batch_total} |
  | categorizing_batch (activity=maintain_tests) | `/qgsd:fix-tests` — load state, batch results on disk, re-enter categorization for batch {batch} |
  | actioning_batch (activity=maintain_tests) | `/qgsd:fix-tests` — load state, dispatch quick tasks for batch {batch} failures |
  | verifying_batch (activity=maintain_tests) | `/qgsd:fix-tests` — load state, re-run verification for actioned tests in batch {batch} |
  | complete (activity=maintain_tests) | `/qgsd:fix-tests` — session already complete; print summary and clear activity state |

Do NOT remove or reorder any existing rows. Do NOT add section headers or blank lines inside the table. Preserve the absolute paths already present in this file — they must not be replaced with tilde paths.
  </action>
  <verify>
1. New rows present in installed copy:
   grep -c 'maintain_tests' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
   Expected: 6

2. Installed copy still uses absolute paths (not tilde paths):
   grep 'gsd-tools.cjs' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
   Expected: lines contain /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs (absolute path, not ~/)

3. All 6 sub_activities covered in installed copy:
   grep -E 'discovering_tests|running_batch.*maintain|categorizing_batch|actioning_batch|verifying_batch.*maintain|complete.*maintain' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md | wc -l
   Expected: 6

4. Existing rows still intact in installed copy:
   grep 'executing_plan\|planning (activity=quick)' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
   Expected: both original rows present
  </verify>
  <done>
- 6 new routing rows present in the routing table in ~/.claude/qgsd/workflows/resume-project.md
- Each row covers one maintain_tests sub_activity: discovering_tests, running_batch, categorizing_batch, actioning_batch, verifying_batch, complete
- All ambiguous sub_activity values carry the (activity=maintain_tests) qualifier
- Absolute paths in the installed copy are preserved (not replaced with tilde paths)
- All pre-existing routing rows are unchanged
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Source file has 6 new rows:
   grep -c 'maintain_tests' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
   Expected: 6

2. Installed copy has 6 new rows:
   grep -c 'maintain_tests' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
   Expected: 6

3. Installed copy still uses absolute paths (cp was NOT used):
   grep 'gsd-tools.cjs' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
   Expected: /Users/jonathanborduas/.claude/qgsd/bin/gsd-tools.cjs (not ~/...)

4. All 6 sub_activities covered in BOTH files:
   grep -E 'discovering_tests|running_batch.*maintain|categorizing_batch|actioning_batch|verifying_batch.*maintain|complete.*maintain' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md | wc -l
   Expected: 6

   grep -E 'discovering_tests|running_batch.*maintain|categorizing_batch|actioning_batch|verifying_batch.*maintain|complete.*maintain' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md | wc -l
   Expected: 6

5. Original rows still present in both files:
   grep 'executing_plan' /Users/jonathanborduas/code/QGSD/get-shit-done/workflows/resume-project.md
   grep 'executing_plan' /Users/jonathanborduas/.claude/qgsd/workflows/resume-project.md
   Expected: match found in each
</verification>

<success_criteria>
- 6 new routing rows present in the routing table in get-shit-done/workflows/resume-project.md (source copy)
- 6 new routing rows present in the routing table in ~/.claude/qgsd/workflows/resume-project.md (QGSD installed copy — the file qgsd:resume-work actually reads)
- Each maintain_tests sub_activity (discovering_tests, running_batch, categorizing_batch, actioning_batch, verifying_batch, complete) has a routing row with a /qgsd:fix-tests recovery action in BOTH files
- All ambiguous sub_activities carry the (activity=maintain_tests) qualifier
- The installed copy retains its absolute paths — cp was NOT used; each file was edited in place independently
- No pre-existing routing rows were removed or reordered in either file
</success_criteria>

<output>
After completion, create .planning/phases/19-state-schema-activity-integration/19-02-SUMMARY.md
</output>
