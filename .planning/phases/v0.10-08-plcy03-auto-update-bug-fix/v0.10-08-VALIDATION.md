---
phase: v0.10-08
slug: v0.10-08-plcy03-auto-update-bug-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-25
---

# Phase v0.10-08 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.10-08`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.10-08`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node:test` (Node.js built-in, v25.6.1) |
| **Config file** | None — bare `node --test` invocation |
| **Quick run command** | `node --test bin/manage-agents.test.cjs` |
| **Full suite command** | `node --test bin/manage-agents.test.cjs` |
| **Estimated runtime** | ~0.3 seconds |
| **CI pipeline** | None |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/manage-agents.test.cjs`
- **After every plan wave:** Run `node --test bin/manage-agents.test.cjs`
- **Before `/qgsd:verify-work`:** Full suite must be green (≥125 tests pass)
- **Maximum acceptable task feedback latency:** ~0.3 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.10-08-01-01 | 01 | 0 | PLCY-03 | unit (RED scaffold) | `node --test bin/manage-agents.test.cjs` | ❌ W0 | ⬜ pending |
| v0.10-08-02-01 | 02 | 1 | PLCY-03 | unit (GREEN) | `node --test bin/manage-agents.test.cjs` | ✅ N/A | ⬜ pending |
| v0.10-08-02-02 | 02 | 1 | PLCY-03 | documentation | `cat .planning/phases/v0.10-05-policy-uis/v0.10-05-VERIFICATION.md` | ✅ N/A | ⬜ pending |
| v0.10-08-02-03 | 02 | 1 | PLCY-03 | documentation | `cat .planning/v0.10-MILESTONE-AUDIT.md` | ✅ N/A | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `bin/manage-agents.test.cjs` — add regression test stubs for `runAutoUpdateCheck` Map bracket notation bug (PLCY-03):
  - Stub 1: `REGRESSION: statuses[slot] on Map returns undefined — proves original bug` (demonstrates bug, asserts `undefined`)
  - Stub 2: `REGRESSION: statuses.get(binName) resolves correctly — proves fix direction` (asserts `.get()` returns entry)
  - Stub 3: `runAutoUpdateCheck: auto slot with injected statuses produces UPDATE_AVAILABLE not SKIP` (integration stub, initially incomplete/TODO)

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.
> These are surfaced during `/qgsd:verify-work` UAT.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Startup auto-update check writes UPDATE_AVAILABLE/OK (not SKIP) to `~/.claude/qgsd-update.log` for real auto-policy slot | PLCY-03 | Requires live startup with real `getUpdateStatuses()` network calls (npm/gh CLI) + real auto-policy slot configured in `~/.claude/qgsd.json` | 1. Configure one slot with `update_policy: auto`. 2. Restart shell / run `node bin/manage-agents.cjs`. 3. Check `~/.claude/qgsd-update.log` — last entry must NOT be `SKIP`. |
| `listAgents()` warning banner fires after real ERROR from auto-update check | PLCY-03 | Requires TTY, live log file with ERROR entry, visual confirmation | 1. Manually append `{"ts":"...","slot":"codex-1","status":"ERROR","detail":"test error"}` to log. 2. Run `node bin/manage-agents.cjs` in TTY. 3. Verify warning banner is visible. |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 1s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.10-08`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 1 | — | — | — | scaffold |
| 1 | 3 | `node --test bin/manage-agents.test.cjs` | — | — | ⬜ pending |

**Phase validation complete:** pending
