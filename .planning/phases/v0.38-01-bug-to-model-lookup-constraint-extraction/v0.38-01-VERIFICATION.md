---
phase: v0.38-01-bug-to-model-lookup-constraint-extraction
verified: 2026-03-18T08:00:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase v0.38-01: Bug-to-Model Lookup & Constraint Extraction Verification Report

**Phase Goal:** Developers can identify which formal models cover a bug and receive plain-English fix constraints extracted from those models.

**Verified:** 2026-03-18T08:00:00Z
**Status:** PASSED — All must-haves verified. Phase goal fully achieved.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Running `formal-scope-scan.cjs --bug-mode --description "..."` returns ranked model paths with formalism type and requirement coverage for matching models | ✓ VERIFIED | CLI execution returns JSON with `formalism` (tla/alloy) and `requirement_coverage` arrays. Example: `--bug-mode --description "circuit breaker"` returns NFCircuitBreaker.tla with DETECT-01/02/03 requirements. |
| 2 | bug-model-gaps.json persists bug-to-model coverage status (no_coverage, no_reproduction, reproduced) across sessions | ✓ VERIFIED | File exists at `.planning/formal/bug-model-gaps.json` with valid v1.0 schema. Tests verify persistence through session boundary via file I/O. Deduplication by description confirmed. |
| 3 | TLA+ invariant definitions, state variables, and transition predicates are parsed and rendered as plain-English fix constraint summaries | ✓ VERIFIED | `model-constrained-fix.cjs --spec NFCircuitBreaker.tla --format text` outputs "SAFETY: TypeOK requires..." and "TRANSITION: OscillationDetected changes..." with @requirement markers linked. |
| 4 | Alloy assertion definitions and signature constraints are parsed and rendered as plain-English fix constraint summaries | ✓ VERIFIED | `model-constrained-fix.cjs --spec account-pool-structure.als --format text` outputs "ASSERT: AddPreservesValidity verifies..." and "STRUCTURE: PoolState has fields..." |
| 5 | Matched model checkers (TLC/Alloy) run automatically with 60s timeout and report pass/fail/timeout with counterexample traces when available | ✓ VERIFIED | `--run-checkers` flag spawns TLC/Alloy subprocesses with 60s timeout per model. Output includes `checker_result` (pass/fail/timeout), `checker_trace` (string or null), and `checker_runtime_ms`. Max 3 models enforced. |
| 6 | All requirement IDs (BML-01, BML-02, BML-03, CEX-01, CEX-02, CEX-03) are satisfied by the implementation | ✓ VERIFIED | Cross-reference in REQUIREMENTS.md shows all 6 requirements marked complete for v0.38-01. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/formal-scope-scan.cjs` | Extended with --bug-mode, --persist-gap, --run-checkers flags; semantic matching; module.exports | ✓ VERIFIED | 887 lines, substantive implementation. Contains: parseArgs (recognizes all 3 flags), loadModelRegistry/loadBugModelGaps (fail-open), scoreConceptMatch (semantic scoring), runBugModeMatching (registry lookup), runModelCheckers (TLC/Alloy execution), persistBugGap/persistBugGapWithCheckers (gap persistence). Module exports: parseArgs, scoreConceptMatch, deriveFormalism, runBugModeMatching, loadModelRegistry, loadBugModelGaps, saveBugModelGaps, persistBugGap, hashBugId, runModelCheckers (11 exports for testing). |
| `bin/model-constrained-fix.cjs` | TLA+ and Alloy constraint extraction with plain-English rendering | ✓ VERIFIED | 510 lines, substantive. Contains: extractTlaConstraints (VARIABLES + named definitions via position-based extraction), extractAlloyConstraints (sig/assert/pred/fact regex matching), simplifyTla/simplifyAlloy (operator translation), findRequirementMarkers (@requirement lookback), renderConstraintSummary (template-based English rendering with type-priority sorting), CLI interface (--spec, --max-constraints, --format). Module exports: extractTlaConstraints, extractAlloyConstraints, renderConstraintSummary. |
| `test/bug-lookup.test.cjs` | Unit tests for bug-mode lookup, gap persistence, deduplication | ✓ VERIFIED | 368 lines, 27 tests. Coverage: parseArgs (3), deriveFormalism (3), scoreConceptMatch (5), runBugModeMatching (4), CLI integration (2), hashBugId (3), loadBugModelGaps (2), persistBugGap (5). All passing. |
| `test/constraint-extraction.test.cjs` | Tests for TLA+ extraction, Alloy extraction, English rendering, edge cases | ✓ VERIFIED | 319 lines, 30 tests. Coverage: extractTlaConstraints (7), extractAlloyConstraints (6), renderConstraintSummary (9), edge cases (8). All passing. Tests use real spec files (NFCircuitBreaker.tla, account-pool-structure.als, NFConfigLoader.tla, architecture-registry.als). |
| `test/model-reproduction.test.cjs` | Integration tests for model checker execution, timeout, gap persistence, fail-open | ✓ VERIFIED | 315 lines, 17 tests. Coverage: flag parsing (3), max model limit (1), timeout (1), output schema (1), fail-open (3), gap persistence (4), checker integration (4). All passing. Java-dependent tests correctly skip when Java unavailable. |
| `.planning/formal/bug-model-gaps.json` | Persistent bug-to-model coverage tracking with v1.0 schema | ✓ VERIFIED | File exists with correct initial schema: `{ "version": "1.0", "entries": [] }`. Valid JSON structure. Persists entries with bug_id, description, timestamp, status, matched_models, checked_models, session_id. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `bin/formal-scope-scan.cjs` | `.planning/formal/model-registry.json` | reads model metadata for formalism type and requirements | ✓ WIRED | REGISTRY_PATH defined at line 13. loadModelRegistry() function at line 317. Called within runBugModeMatching() at line 391. Fail-open when missing (line 325). |
| `bin/formal-scope-scan.cjs` | `.planning/formal/bug-model-gaps.json` | writes coverage status after lookup | ✓ WIRED | BUG_GAPS_PATH defined at line 14. loadBugModelGaps() at line 426, saveBugModelGaps() at line 440, persistBugGap() at line 452, persistBugGapWithCheckers() at line 487. Called in main when --persist-gap flag set (lines 773-778). |
| `bin/formal-scope-scan.cjs` | `bin/run-tlc.cjs` | spawns TLC for matched .tla models | ✓ WIRED | findTlcConfig() at line 583. runSingleChecker() at line 619 spawns run-tlc.cjs at line 606 via spawnSync. Called from runModelCheckers() at line 558. |
| `bin/formal-scope-scan.cjs` | `bin/run-alloy.cjs` | spawns Alloy for matched .als models | ✓ WIRED | runSingleChecker() at line 619 spawns run-alloy.cjs at line 647 via spawnSync for Alloy models. Called from runModelCheckers() at line 558. |
| `bin/model-constrained-fix.cjs` | `.planning/formal/tla/*.tla` | reads and parses TLA+ spec files | ✓ WIRED | fs.readFileSync() at line 415 reads spec file. extractTlaConstraints() at line 95 parses content. Called in main at line 480 for .tla files. |
| `bin/model-constrained-fix.cjs` | `.planning/formal/alloy/*.als` | reads and parses Alloy spec files | ✓ WIRED | fs.readFileSync() at line 415 reads spec file. extractAlloyConstraints() at line 236 parses content. Called in main at line 482 for .als files. |
| `test/bug-lookup.test.cjs` | `bin/formal-scope-scan.cjs` | imports and tests all exported functions | ✓ WIRED | Requires at line 20: parseArgs, scoreConceptMatch, deriveFormalism, runBugModeMatching, loadModelRegistry, loadBugModelGaps, saveBugModelGaps, persistBugGap, hashBugId, runModelCheckers. All functions tested (27 tests pass). |
| `test/constraint-extraction.test.cjs` | `bin/model-constrained-fix.cjs` | imports and tests all exported functions | ✓ WIRED | Requires at line 12: extractTlaConstraints, extractAlloyConstraints, renderConstraintSummary. All functions tested (30 tests pass). |
| `test/model-reproduction.test.cjs` | `bin/formal-scope-scan.cjs` | imports and tests checker execution pipeline | ✓ WIRED | Requires at line 18: parseArgs, runModelCheckers, persistBugGapWithCheckers, loadBugModelGaps. All functions tested (17 tests pass). |

### Requirements Coverage

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| BML-01 | v0.38-01 | ✓ Complete | formal-scope-scan.cjs --bug-mode returns ranked models with formalism type and requirement_coverage arrays. Example output shows NFCircuitBreaker.tla with formalism="tla" and requirement_coverage=["DETECT-01", "DETECT-02", "DETECT-03"]. 27 unit tests verify. |
| BML-02 | v0.38-01 | ✓ Complete | --run-checkers spawns TLC/Alloy with 60s timeout, max 3 models, reports pass/fail/timeout with traces. runModelCheckers() at line 539 enforces max limit, spawnSync timeout, result determination (lines 558-643). Integration tests verify real TLC/Alloy execution. |
| BML-03 | v0.38-01 | ✓ Complete | bug-model-gaps.json tracks bugs with status (no_coverage, no_reproduction, reproduced). persistBugGap() at line 452 and persistBugGapWithCheckers() at line 487 manage status transitions. Deduplication at line 465. File persists across sessions (test-verified). |
| CEX-01 | v0.38-01 | ✓ Complete | extractTlaConstraints parses VARIABLES (line 101-116), named definitions via position-based extraction (line 143-200), @requirement linking (line 74-85). 7 tests verify extraction of TypeOK, actions, variables, requirement linking. |
| CEX-02 | v0.38-01 | ✓ Complete | extractAlloyConstraints parses signatures (line 244-253), assertions (line 260-272), predicates (line 279-290), facts (line 297-305). @requirement markers linked via findRequirementMarkers. 6 tests verify extraction accuracy. |
| CEX-03 | v0.38-01 | ✓ Complete | renderConstraintSummary renders as plain-English using templates (line 361-395): SAFETY for invariants, ASSERT for assertions, TRANSITION for actions, etc. Operators simplified via simplifyTla/simplifyAlloy. 9 tests verify rendering and operator translation. |

### Anti-Patterns Found

**No blockers detected.**

Minor code quality observations (all passing and documented):
- Console.log statements in bin/ files are legitimate CLI output (no debug artifacts)
- test/ files use temp directories for isolation — correct pattern
- Proper fail-open behavior for missing Java/checkers (documented in code)

### System Integration Check

**Wiring Verification Results:**

1. **bin/formal-scope-scan.cjs (--bug-mode, --persist-gap, --run-checkers):**
   - ✓ Reads model-registry.json (loadModelRegistry, line 317)
   - ✓ Writes bug-model-gaps.json (persistBugGap, persistBugGapWithCheckers)
   - ✓ Spawns TLC/Alloy checkers (runModelCheckers, runSingleChecker)
   - ✓ Module exports for programmatic use (11 functions)
   - ✓ CLI interface documented in help text

2. **bin/model-constrained-fix.cjs (constraint extraction):**
   - ✓ Reads TLA+ spec files (.tla extension detection)
   - ✓ Reads Alloy spec files (.als extension detection)
   - ✓ Parses and extracts constraints (TLA+, Alloy, generalized)
   - ✓ Renders as plain-English (rule-based templates, no LLM dependency)
   - ✓ Module exports for programmatic use (3 functions)
   - ✓ CLI interface (--spec, --max-constraints, --format)

3. **Test Coverage (all passing):**
   - bug-lookup.test.cjs: 27 tests, bug-mode matching and gap persistence
   - constraint-extraction.test.cjs: 30 tests, TLA+/Alloy extraction and English rendering
   - model-reproduction.test.cjs: 17 tests, checker execution and gap status updates

**No orphaned producers detected.** All artifacts have documented consumers (downstream phases v0.38-02, v0.38-04) or test coverage demonstrating functional use.

### Formal Verification

**Status:** SKIPPED — No formal scope matched this phase (FORMAL_SPEC_CONTEXT was empty). This phase implements tooling for formal verification but does not itself define formal properties.

## Summary

Phase v0.38-01 successfully delivers the complete bug-to-model lookup and constraint extraction pipeline:

1. **Bug Identification (Plan 01 + Plan 03):** Developers run `formal-scope-scan.cjs --bug-mode --description "..."` to identify which formal models cover a bug, with automatic model checker execution and reproduction status.

2. **Constraint Extraction (Plan 02):** `model-constrained-fix.cjs --spec <path>` extracts TLA+ and Alloy constraint definitions and renders them as plain-English fix constraint summaries suitable for injection into quorum worker prompts.

3. **Persistence (Plan 01 + Plan 03):** bug-model-gaps.json tracks bug-to-model coverage status across sessions, supporting future model refinement and regression prevention workflows.

**All requirement IDs satisfied:**
- BML-01, BML-02, BML-03 (bug-to-model lookup and reproduction)
- CEX-01, CEX-02, CEX-03 (constraint extraction and rendering)

**Artifacts substantive and wired:**
- formal-scope-scan.cjs: 887 lines, 11 exports, full CLI + programmatic interface
- model-constrained-fix.cjs: 510 lines, 3 exports, TLA+ and Alloy parsers with English rendering
- bug-lookup.test.cjs: 368 lines, 27 passing tests
- constraint-extraction.test.cjs: 319 lines, 30 passing tests
- model-reproduction.test.cjs: 315 lines, 17 passing tests
- bug-model-gaps.json: v1.0 schema with persistence and deduplication

**Key links verified:** Registry lookup, gap persistence, checker spawning, module exports, test coverage.

Phase goal achieved: Developers can now identify which formal models cover a bug and receive plain-English fix constraints extracted from those models.

---

_Verified: 2026-03-18T08:00:00Z_
_Verifier: Claude (nf-verifier)_
