---
phase: v0.34-06-e2e-integration-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [test/e2e-semantic-promotion-pipeline.test.cjs]
autonomous: true
requirements: [SEM-03, SEM-04, PROMO-02, PROMO-03, PROMO-04]
formal_artifacts: none

must_haves:
  truths:
    - "Test runs compute-semantic-scores enrichment and verifies gate files contain semantic_score fields with schema_version '3'"
    - "Test simulates 3 consecutive clean sessions (wiring >= 1.0, semantic >= 0.8, formal pass) and verifies auto-promotion triggers for SOFT_GATE -> HARD_GATE"
    - "Test verifies promotion-changelog.json entries contain all PROMO-04 required fields: model, from_level, to_level, timestamp, session_id, trigger, consecutive_clean_sessions, evidence_readiness (with score/total keys)"
    - "Test verifies regression (semantic score drop below 0.8) resets consecutive_clean_sessions to 0"
    - "Test verifies semantic_score fields persist across multiple gate file rewrite iterations (not overwritten to undefined)"
  artifacts:
    - test/e2e-semantic-promotion-pipeline.test.cjs
  key_links:
    - "test imports { enrichGateFile } from bin/compute-semantic-scores.cjs"
    - "test imports { autoPromote } from bin/promote-gate-maturity.cjs"
    - "test imports { checkCleanSession } from bin/nf-solve.cjs (or inlines identical threshold logic with comment)"
---

<objective>
Create an end-to-end integration test that validates the complete semantic scoring -> gate enrichment -> auto-promotion pipeline.

Purpose: Verify that compute-semantic-scores.cjs enrichment, checkCleanSession() evaluation, consecutive_clean_sessions state tracking, and auto-promotion logic work together correctly across multiple session cycles. This is the final gap-closure phase for v0.34.

Output: `test/e2e-semantic-promotion-pipeline.test.cjs` — a single integration test file with 5 test cases covering the full pipeline.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.34-06-e2e-integration-test/v0.34-06-RESEARCH.md
@test/auto-promotion-integration.test.cjs
@bin/compute-semantic-scores.cjs
@bin/promote-gate-maturity.cjs
@bin/nf-solve.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test file with fixture helpers and semantic enrichment tests</name>
  <files>test/e2e-semantic-promotion-pipeline.test.cjs</files>
  <action>
Create `test/e2e-semantic-promotion-pipeline.test.cjs` using node:test + node:assert/strict (matching the pattern from `test/auto-promotion-integration.test.cjs`).

**Imports:**
- `{ enrichGateFile }` from `../bin/compute-semantic-scores.cjs` — use the real enrichment function, do NOT hand-roll enrichment logic
- `{ autoPromote }` from `../bin/promote-gate-maturity.cjs` — use the real auto-promotion function
- `{ checkCleanSession }` from `../bin/nf-solve.cjs` — use the real clean session evaluator in `simulateSolveIteration()` instead of ad-hoc threshold logic

**Helper: `createFixtureDir(opts)`** — Creates temp directory mimicking `.planning/formal/` structure:
- `gates/gate-a-grounding.json` with `{ schema_version: '2', wiring_evidence_score: opts.wiringA || 1.0 }`
- `gates/gate-b-abstraction.json` with `{ schema_version: '2', wiring_purpose_score: opts.wiringB || 1.0 }`
- `gates/gate-c-validation.json` with `{ schema_version: '2', wiring_coverage_score: opts.wiringC || 1.0 }`
- `model-registry.json` with one SOFT_GATE model: `{ '.alloy/quorum-votes.als': { gate_maturity: 'SOFT_GATE', evidence_readiness: { score: 3, total: 5 }, source_layer: 'L3', last_updated: <ISO timestamp> } }`
- `per-model-gates.json` with all gates passing: `{ models: { '.alloy/quorum-votes.als': { gate_a: { pass: true, score: 1.0 }, gate_b: { pass: true, score: 1.0 }, gate_c: { pass: true, score: 1.0 } } } }`
- `promotion-changelog.json` — empty array `[]`
- `solve-state.json` — `{ consecutive_clean_sessions: opts.cleanSessions || 0 }`
- `check-results.ndjson` — `{ "result": "no_counterexample" }` (one line)
- Returns `{ tmpDir, gatesDir, registryPath, changelogPath, perModelGatesPath, solveStatePath }`

**Helper: `cleanup(tmpDir)`** — `fs.rmSync(tmpDir, { recursive: true, force: true })` in try/catch

**Helper: `enrichGates(gatesDir, scores)`** — Takes `{ A: number, B: number, C: number }`, reads each gate file, applies `enrichGateFile()` from compute-semantic-scores.cjs, writes back. This uses the REAL enrichment function. Pattern:
```javascript
function enrichGates(gatesDir, scores) {
  const files = {
    A: 'gate-a-grounding.json',
    B: 'gate-b-abstraction.json',
    C: 'gate-c-validation.json',
  };
  for (const [label, filename] of Object.entries(files)) {
    const filePath = path.join(gatesDir, filename);
    const gateData = JSON.parse(fs.readFileSync(filePath, 'utf8'));
    const enriched = enrichGateFile(gateData, scores[label], { evaluated_candidates: 1, confirmed: 1, rejected: 0, maybe: 0, computed: new Date().toISOString() });
    fs.writeFileSync(filePath, JSON.stringify(enriched, null, 2));
  }
}
```

**Helper: `simulateSolveIteration(tmpDir, opts)`** — Simulates one nf-solve.cjs iteration.

**Threshold reference** (must match nf-solve.cjs `checkCleanSession()`): wiring >= 1.0, semantic >= 0.8, no formal counterexamples.

**Implementation approach:** Instead of computing cleanliness ad-hoc, this helper should invoke the real `checkCleanSession()` imported from `../bin/nf-solve.cjs`. Since `checkCleanSession()` reads from the `.planning/formal/gates/` directory relative to the repo ROOT, the helper must temporarily set `process.env.NF_ROOT` (or equivalent) to `tmpDir` so `checkCleanSession()` reads the fixture files. If `checkCleanSession()` uses a hardcoded ROOT, wrap the call by temporarily monkey-patching the module's path resolution, OR inline the identical logic with an explicit comment: `// Mirrors nf-solve.cjs checkCleanSession() thresholds: wiring >= 1.0, semantic >= 0.8, formalPass`.

Steps:
1. If `opts.semantic` provided (e.g., `{ A: 0.85, B: 0.9, C: 0.88 }`), call `enrichGates()` on the gates directory
2. Evaluate cleanliness using the same thresholds as `checkCleanSession()`: all wiring >= 1.0 AND all semantic >= 0.8 AND `opts.formalPass !== false`
3. Read solve-state.json, get `prevClean = state.consecutive_clean_sessions || 0`
4. Compute `newClean = isClean ? prevClean + 1 : 0`
5. Write updated solve-state.json with `{ consecutive_clean_sessions: newClean }`
6. Return `{ isClean, consecutive: newClean }`

**Test 1: `E2E-SEM-1: gate files enriched with semantic_score via enrichGateFile()`** (validates SEM-03, SEM-04):
- Create fixture dir (defaults: wiring scores all 1.0, no semantic_score, schema_version '2')
- Assert gate-a-grounding.json has NO semantic_score and schema_version '2' before enrichment
- Call `enrichGates(gatesDir, { A: 0.85, B: 0.9, C: 0.88 })`
- Assert gate-a has `semantic_score === 0.85`, `schema_version === '3'`
- Assert gate-b has `semantic_score === 0.9`, `schema_version === '3'`
- Assert gate-c has `semantic_score === 0.88`, `schema_version === '3'`
- Assert wiring_evidence_score is PRESERVED (not lost during enrichment)
- Cleanup

**Test 2: `E2E-SEM-2: semantic_score fields persist across multiple enrichment cycles (re-enrichment idempotency)`** (validates SEM-04 persistence):
- Create fixture dir
- **Step 1 — Enrichment cycle 1:** enrich gates with scores { A: 0.85, B: 0.9, C: 0.88 }. Assert each gate now has `semantic_score` matching the input value and `schema_version === '3'`.
- **Step 2 — Simulate external gate rewrite:** read each gate file, modify wiring score slightly (e.g., set to 1.0), write back using `{ ...gateData, wiring_evidence_score: 1.0 }` (spread operator, NOT calling enrichGateFile) — this simulates compute-per-model-gates.cjs preserving fields via spread.
- **Step 3 — Assert preservation:** Assert `semantic_score` is STILL PRESENT and unchanged after the rewrite (spread preserves it).
- **Step 4 — Enrichment cycle 2:** enrich again with DIFFERENT scores { A: 0.9, B: 0.95, C: 0.92 }. Assert new `semantic_score` values took effect (old values overwritten), `semantic_metadata` is updated, and `schema_version` is still '3'.
- Cleanup
  </action>
  <verify>
Run: `node --test test/e2e-semantic-promotion-pipeline.test.cjs 2>&1 | head -30`
Expected: Tests E2E-SEM-1 and E2E-SEM-2 pass (no failures). File imports enrichGateFile from compute-semantic-scores.cjs successfully.
  </verify>
  <done>
- test/e2e-semantic-promotion-pipeline.test.cjs exists with helpers and 2 passing semantic enrichment tests
- Tests use real enrichGateFile() function (not hand-rolled enrichment)
- Gate file schema_version bumps to '3' after enrichment
- Wiring scores preserved through enrichment
  </done>
</task>

<task type="auto">
  <name>Task 2: Add multi-cycle auto-promotion and regression tests</name>
  <files>test/e2e-semantic-promotion-pipeline.test.cjs</files>
  <action>
Append 3 additional tests to `test/e2e-semantic-promotion-pipeline.test.cjs`:

**Test 3: `E2E-PROMO-1: 3 consecutive clean sessions trigger auto-promotion`** (validates PROMO-02, PROMO-03):
- Create fixture dir with `cleanSessions: 0`
- Iteration 1: `simulateSolveIteration(tmpDir, { semantic: { A: 0.85, B: 0.9, C: 0.88 } })` — assert isClean=true, consecutive=1
- Iteration 2: same — assert isClean=true, consecutive=2
- Iteration 3: same — assert isClean=true, consecutive=3
- Now call `autoPromote()` from promote-gate-maturity.cjs with the real function:
  ```javascript
  const registry = JSON.parse(fs.readFileSync(registryPath, 'utf8'));
  const perModelGates = JSON.parse(fs.readFileSync(perModelGatesPath, 'utf8'));
  const changelog = JSON.parse(fs.readFileSync(changelogPath, 'utf8'));
  const solveState = { consecutive_clean_sessions: 3 };
  const result = autoPromote(solveState, perModelGates, registry, changelog, {
    checkResults: [{ result: 'pass', tool: 'alloy', check_id: 'alloy:quorum-votes' }],
  });
  ```
- Assert `result.promoted.length === 1` (one model promoted)
- Assert `registry['.alloy/quorum-votes.als'].gate_maturity === 'HARD_GATE'` (registry object is mutated in-place by autoPromote)
- Assert `changelog.length === 1`
- Assert `changelog[0].trigger === 'auto_promotion'`
- Assert `changelog[0].consecutive_clean_sessions === 3`
- Assert `changelog[0].from_level === 'SOFT_GATE'` and `changelog[0].to_level === 'HARD_GATE'`
- Cleanup

**Test 4: `E2E-PROMO-2: regression resets consecutive_clean_sessions to 0`** (validates PROMO-02 reset):
- Create fixture dir
- Run 3 clean iterations (semantic scores all >= 0.8, wiring all 1.0)
- Assert consecutive=3
- Run iteration 4 with semantic regression: `{ semantic: { A: 0.75, B: 0.9, C: 0.88 } }` (A drops below 0.8 threshold)
- Assert isClean=false, consecutive=0
- Verify solve-state.json on disk has `consecutive_clean_sessions === 0`
- Cleanup

**Test 5: `E2E-PROMO-3: promotion-changelog.json has all PROMO-04 required fields`** (validates PROMO-04):
- Create fixture dir with `cleanSessions: 3` (skip the iteration simulation — focus on changelog validation)
- Call `autoPromote()` with `{ consecutive_clean_sessions: 3 }` and appropriate fixtures
- Assert `changelog.length === 1`
- Assert entry has ALL PROMO-04 fields (8 fields total):
  - `typeof entry.model === 'string' && entry.model.length > 0`
  - `entry.from_level === 'SOFT_GATE'`
  - `entry.to_level === 'HARD_GATE'`
  - `typeof entry.timestamp === 'string'` AND `!isNaN(Date.parse(entry.timestamp))` (valid ISO 8601)
  - `'session_id' in entry` (field exists, may be null)
  - `entry.trigger === 'auto_promotion'`
  - `entry.consecutive_clean_sessions >= 3`
  - `'evidence_readiness' in entry` AND `typeof entry.evidence_readiness === 'object'` AND `'score' in entry.evidence_readiness` AND `'total' in entry.evidence_readiness` (autoPromote() writes evidence_readiness from registry model)
- Cleanup

**Important:** All 3 tests use real `autoPromote()` from promote-gate-maturity.cjs — NOT mocked promotion logic.
  </action>
  <verify>
Run: `node --test test/e2e-semantic-promotion-pipeline.test.cjs 2>&1 | tail -20`
Expected: All 5 tests pass. Output shows "tests 5" and "pass 5".
Also verify: `grep -c 'autoPromote' test/e2e-semantic-promotion-pipeline.test.cjs` returns >= 3 (real function used, not mocked).
  </verify>
  <done>
- All 5 E2E tests pass: 2 semantic enrichment + 3 auto-promotion/regression
- Tests use real autoPromote() from promote-gate-maturity.cjs (not mocked)
- Tests use real enrichGateFile() from compute-semantic-scores.cjs (not mocked)
- Regression test confirms consecutive_clean_sessions resets to 0 on semantic score drop
- Changelog metadata test validates all PROMO-04 required fields
- `npm test` (full suite) still passes with the new test file included
  </done>
</task>

</tasks>

<verification>
1. `node --test test/e2e-semantic-promotion-pipeline.test.cjs` — all 5 tests pass
2. `npm test` — full suite passes (no regressions from new test file)
3. `grep 'enrichGateFile' test/e2e-semantic-promotion-pipeline.test.cjs` — confirms real function imported
4. `grep 'autoPromote' test/e2e-semantic-promotion-pipeline.test.cjs` — confirms real function imported
5. Gate files in test fixtures have both `wiring_*_score` AND `semantic_score` after enrichment (SEM-04)
6. Auto-promotion fires only at `consecutive_clean_sessions >= 3` (PROMO-03)
7. Changelog entries contain all 8 required PROMO-04 fields including evidence_readiness with score/total keys
</verification>

<success_criteria>
- `node --test test/e2e-semantic-promotion-pipeline.test.cjs` exits 0 with 5/5 tests passing
- `npm test` exits 0 (no regressions)
- No mocked enrichment or promotion logic — tests use real exported functions from bin/ scripts
</success_criteria>
