---
phase: v0.10-04-live-health-dashboard
plan: "01"
subsystem: testing
tags: [node-test, tdd, dashboard, health-check, pure-functions]

# Dependency graph
requires:
  - phase: v0.10-03-credential-management
    provides: classifyProbeResult and writeKeyStatus exported via _pure from manage-agents.cjs
provides:
  - "11 failing test stubs for buildDashboardLines (8 cases) and formatTimestamp (3 cases) in manage-agents.test.cjs"
  - "Interface contracts defined for Wave 1 implementation of live health dashboard"
affects: [v0.10-04-02-PLAN, v0.10-04-03-PLAN]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD RED phase: test stubs committed before any implementation exists — non-zero exit confirms RED state"
    - "_pure destructure pattern: new functions added to destructure line at top of test file to expose them for testing"

key-files:
  created: []
  modified:
    - bin/manage-agents.test.cjs

key-decisions:
  - "Tests use em-dash Unicode literal (\\u2014) rather than the HTML entity — matches JS string comparison semantics"
  - "formatTimestamp timezone-safe approach: test format structure (length, colon positions) not exact value to avoid UTC vs local time flakiness"
  - "buildDashboardLines stale threshold: 60s — tests use 70s (stale) and 10s (fresh) as deterministic boundary cases"

patterns-established:
  - "Wave 0 TDD: test stubs define the exact API surface (signature, return type, field names) before implementation"
  - "RED confirmation: run tests and assert exit code 1 + TypeError (not a function) before committing"

requirements-completed: [DASH-01, DASH-02]

# Metrics
duration: 1min
completed: 2026-02-24
---

# Phase v0.10-04 Plan 01: Wave 0 Failing Test Stubs Summary

**11 TDD RED stubs defining buildDashboardLines and formatTimestamp interface contracts for the live health dashboard**

## Performance

- **Duration:** 1 min
- **Started:** 2026-02-24T20:45:26Z
- **Completed:** 2026-02-24T20:46:39Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Extended `_pure` destructure in `bin/manage-agents.test.cjs` to include `buildDashboardLines` and `formatTimestamp`
- Added 3 failing `formatTimestamp` test cases: null returns em-dash, Date.now() returns HH:MM:SS, known epoch validates format structure
- Added 8 failing `buildDashboardLines` test cases: empty slots, healthy UP slot, unhealthy DOWN slot, subprocess error label, missing health entry, null lastUpdated em-dash, stale warning at 70s, no stale at 10s
- Confirmed RED state: 11 failures all `TypeError: ... is not a function`, exit code 1, 77 prior tests remain passing

## Task Commits

Each task was committed atomically:

1. **Task 1: Wave 0 — Add failing test stubs for buildDashboardLines and formatTimestamp** - `4405e5f` (test)

## Files Created/Modified
- `bin/manage-agents.test.cjs` - Extended destructure line + appended 11 new failing test cases after writeKeyStatus section

## Decisions Made
- Tests use `\u2014` Unicode literal for em-dash (consistent with JS string comparison semantics)
- `formatTimestamp` tests avoid UTC vs local timezone flakiness by checking format structure (string length, colon positions) rather than exact HH:MM:SS value for the known-epoch test
- `buildDashboardLines` stale boundary: 70s for stale (>60s), 10s for fresh (<60s) — clear deterministic margin around the 60s threshold

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None — test file accepted new destructure entries and appended test block without conflict.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Wave 0 complete: 11 RED stubs committed at `4405e5f`
- Wave 1 (Plan 02) can now implement `buildDashboardLines` and `formatTimestamp` in `manage-agents.cjs`, exporting them via `_pure`
- Success criterion for Wave 1: `node --test bin/manage-agents.test.cjs` exits 0 with all 88 tests passing

---
*Phase: v0.10-04-live-health-dashboard*
*Completed: 2026-02-24*
