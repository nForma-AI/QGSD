---
phase: 50-debug-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - commands/nf/debug.md
autonomous: true
requirements:
  - DBUG-03
  - DBUG-04

must_haves:
  truths:
    - "Constraints from Step A.8 appear in the quorum worker prompt template (Step C) as a [FORMAL CONSTRAINTS] block"
    - "When Loop 1 converges, the TSV trace path is included in the debug artifact (Step F)"
    - "When a reproducing model is found (A.6 or A.7), its path is recorded as a formal model deliverable in the debug artifact"
    - "The FORMAL row in the NEXT STEP table (Step E) shows model path and top constraint when verdict is 'reproduced'"
    - "Constraint text from A.8 is injected verbatim into each quorum worker's prompt — workers cannot propose fixes violating these constraints"
    - "When no model exists (verdict=no-model), gap persistence still triggers (existing behavior preserved)"
  artifacts:
    - path: "commands/nf/debug.md"
      provides: "Constraint injection into worker prompts and formal model artifact deliverable"
      contains: "REPRODUCING_MODEL"
  key_links:
    - from: "commands/nf/debug.md (Step A.8)"
      to: "commands/nf/debug.md (Step C worker prompts)"
      via: "$CONSTRAINTS array populated by A.8 injected into worker prompt template"
      pattern: "FORMAL CONSTRAINTS"
    - from: "commands/nf/debug.md (Step A.7)"
      to: "commands/nf/debug.md (Step F artifact)"
      via: "$TSV_LOG and $REPRODUCING_MODEL written into artifact file"
      pattern: "reproducing_model|tsv_log|refinement"
---

<objective>
Wire the constraint output from the new Steps A.5-A.8 into quorum worker prompts (Step C) and create formal model artifact tracking in the debug output (Steps E and F). Ensure constraints extracted by Loop 1 are injected into every quorum worker's prompt as verified properties, and that the debug artifact records the reproducing model path and TSV trace as deliverables.

Purpose: Close the feedback loop — constraints extracted from formal models must actually influence the quorum workers' debugging advice, and the reproducing model must be tracked as a first-class deliverable (not just a side effect).
Output: Updated commands/nf/debug.md with constraint injection in Step C and model artifact tracking in Steps E and F.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-debug-integration/50-01-SUMMARY.md
@commands/nf/debug.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire constraint injection into quorum worker prompts and update artifact tracking</name>
  <files>commands/nf/debug.md</files>
  <action>
Read the current debug.md (as modified by Plan 01). Make the following targeted updates:

**1. Step B (Bundle Assembly) — Add constraint summary to bundle**

After the existing FORMAL VERDICT line in $BUNDLE composition, add:
```
CONSTRAINTS: ${$CONSTRAINTS.length > 0 ? $CONSTRAINTS.map((c,i) => `${i+1}. ${c.english}`).join('\n  ') : 'none extracted'}
REPRODUCING MODEL: ${$REPRODUCING_MODEL || 'none'}
TSV TRACE: ${$TSV_LOG || 'none'}
```

**2. Step C (Worker Prompts) — Ensure constraint injection uses A.8 output**

The current Step C already has a pattern for injecting $FORMAL_CONTEXT.constraints into worker prompts (lines ~94-103). Update this to use the $CONSTRAINTS array from Step A.8 instead of $FORMAL_CONTEXT.constraints:

Replace the conditional:
  `If $FORMAL_CONTEXT.constraints is non-empty, insert the following block here:`

With:
  `If $CONSTRAINTS is non-empty (populated by Step A.8), insert the following block here:`

And update the constraint rendering to use the english text from model-constrained-fix.cjs output:
```
[FORMAL CONSTRAINTS]
The following constraints were extracted from formal model $REPRODUCING_MODEL:
${$CONSTRAINTS.map((c, i) => `- ${c.english}`).join('\n')}

These are verified properties of the system. Do NOT propose fixes that violate these constraints.
[END FORMAL CONSTRAINTS]
```

This ensures workers see plain-English constraint summaries from model-constrained-fix.cjs rather than raw formal text.

**3. Step E (NEXT STEP Table) — Update FORMAL row rendering**

Update the FORMAL row rendering rules to use the new variables:

- **reproduced** (from A.6 or A.7): Show model path and top constraint:
  `| FORMAL | HIGH (model) | {$REPRODUCING_MODEL} reproduces bug. Top constraint: {$CONSTRAINTS[0].english || 'see model'} |`

- **not-reproduced** (A.7 did not converge): Show refinement attempt info:
  `| FORMAL | LOW (model) | Refinement attempted (${result.iterations} iters). Model incomplete. TSV: ${$TSV_LOG} |`

- **no-model**: Keep existing behavior (gap tracking).

**4. Step F (Artifact) — Add formal model deliverable fields**

In the artifact written to .planning/quick/quorum-debug-latest.md, add new sections:

```
## formal model deliverable
reproducing_model: ${$REPRODUCING_MODEL || 'none'}
formal_verdict: ${$FORMAL_VERDICT}
constraints_extracted: ${$CONSTRAINTS.length}
tsv_trace: ${$TSV_LOG || 'none'}
refinement_iterations: ${refinement result iterations || 'N/A'}
converged: ${whether Loop 1 converged || 'N/A'}

## constraints
${$CONSTRAINTS.map((c, i) => `${i+1}. [${c.type}] ${c.english}`).join('\n') || 'none'}
```

This ensures the debug artifact is a complete record of the formal model investigation, not just the quorum worker responses.

**5. Preserve existing gap persistence (Step G area)**

The existing gap persistence logic (when $FORMAL_VERDICT is "no-model") must remain unchanged. Do NOT modify the gap persistence node script. Only ensure the $FORMAL_VERDICT variable is correctly set by the new Steps A.5-A.8 before it reaches this section.
  </action>
  <verify>
    grep -c "FORMAL CONSTRAINTS" commands/nf/debug.md  # Should return >= 2 (injection block + end marker)
    grep -c "REPRODUCING_MODEL" commands/nf/debug.md  # Should return >= 3 (A.6/A.7 set, E render, F artifact)
    grep -c "reproducing_model" commands/nf/debug.md  # Should return >= 1 (artifact section)
    grep -c "tsv_trace\|TSV_LOG" commands/nf/debug.md  # Should return >= 2 (set in A.7, used in F)
    grep "formal model deliverable" commands/nf/debug.md  # Artifact section header
    grep "constraints_extracted" commands/nf/debug.md  # Artifact constraint count
    grep "bug-model-gaps" commands/nf/debug.md  # Gap persistence preserved
  </verify>
  <done>
    Step C worker prompts inject $CONSTRAINTS from A.8 as plain-English [FORMAL CONSTRAINTS] block. Step E FORMAL row shows reproducing model path and top constraint. Step F artifact includes formal model deliverable section with reproducing_model, formal_verdict, constraints_extracted, tsv_trace, and constraints list. Gap persistence for no-model cases is preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end variable flow validation</name>
  <files>commands/nf/debug.md</files>
  <action>
Read the final debug.md and trace the complete variable flow to ensure no broken references:

1. **$EXISTING_MODELS**: Set in A.5, consumed in A.6 (skip condition). Verify A.6 has the skip-if-empty guard.

2. **$REPRODUCING_MODEL**: Set in A.6 (if reproduced) or A.7 (if converged or best-effort). Consumed in A.8 (--spec arg), Step E (FORMAL row), Step F (artifact). Verify all consumers handle the case where it is empty/null.

3. **$FORMAL_VERDICT**: Set in A.6/A.7/A.8 to one of "reproduced", "not_reproduced", "no-model". Consumed in Step B (bundle), Step E (FORMAL row rendering), Step G (gap persistence). Verify the three-way branch in Step E matches these exact values.

4. **$CONSTRAINTS**: Set in A.8 as array of {type, english, formal, ...} objects from model-constrained-fix.cjs JSON output. Consumed in Step B (bundle summary), Step C (worker prompt injection), Step E (top constraint text), Step F (artifact constraints list). Verify all consumers handle empty array gracefully.

5. **$TSV_LOG**: Set in A.7 from result.resultsLog. Consumed in Step E (not-reproduced row), Step F (artifact). Verify consumers handle null/undefined.

6. **Verify no orphaned references**: Search for any remaining references to `debug-formal-context.cjs` or `$FORMAL_CONTEXT.constraints` (should use $CONSTRAINTS instead). The only remaining $FORMAL_CONTEXT usage should be the full JSON object used in Step E's divergence note.

If any broken reference is found, fix it in debug.md.
  </action>
  <verify>
    grep -c "EXISTING_MODELS" commands/nf/debug.md  # >= 2 (set + consumed)
    grep -c "REPRODUCING_MODEL" commands/nf/debug.md  # >= 4 (set + A.8 + E + F)
    grep -c "FORMAL_VERDICT" commands/nf/debug.md  # >= 4 (set + B + E + G)
    grep -c "debug-formal-context" commands/nf/debug.md  # 0 (fully removed)
  </verify>
  <done>
    All variable flows trace correctly from producer (Steps A.5-A.8) to consumers (Steps B, C, E, F, G). No orphaned references to debug-formal-context.cjs remain. All consumers handle empty/null values gracefully. The constraint injection path from A.8 through C to worker prompts is verified end-to-end.
  </done>
</task>

</tasks>

<verification>
- Constraint injection: `grep "FORMAL CONSTRAINTS" commands/nf/debug.md` shows the injection block in Step C
- Model deliverable: `grep "reproducing_model" commands/nf/debug.md` shows artifact tracking in Step F
- TSV trace tracking: `grep "tsv_trace\|TSV_LOG" commands/nf/debug.md` shows trace path in artifact
- No stale references: `grep "debug-formal-context" commands/nf/debug.md` returns nothing
- Variable consistency: all $FORMAL_VERDICT, $CONSTRAINTS, $REPRODUCING_MODEL references have both producer and consumer
</verification>

<success_criteria>
- Constraints from Loop 1 / model-constrained-fix.cjs are injected into quorum worker prompts as [FORMAL CONSTRAINTS] block
- Debug artifact (.planning/quick/quorum-debug-latest.md) includes formal model deliverable section with model path, verdict, constraints, and TSV trace
- FORMAL row in NEXT STEP table shows reproducing model info when available
- All variable flows are consistent — no broken references between steps
- Gap persistence for no-model cases is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/50-debug-integration/50-02-SUMMARY.md`
</output>
