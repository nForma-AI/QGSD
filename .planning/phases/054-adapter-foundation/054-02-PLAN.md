---
phase: 054-adapter-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/nf-solve.cjs
autonomous: true
requirements:
  - CDIAG-01
  - CDIAG-04
formal_artifacts: none

must_haves:
  truths:
    - "queryEdgesSync() in nf-solve.cjs resolves symbol-level function call edges via getImplementation instead of file-name string matching -- edges for a changed function connect to its actual callers, not all importers of its module"
    - "After remediation modifies or creates files, the coderlm server is re-indexed before the next solve iteration begins, so subsequent queries reflect the current codebase state"
  artifacts:
    - path: "bin/nf-solve.cjs"
      provides: "Updated queryEdgesSync using getImplementationSync + reindex call after autoClose"
      contains: "getImplementationSync"
  key_links:
    - from: "bin/nf-solve.cjs"
      to: "bin/coderlm-adapter.cjs"
      via: "adapter.getImplementationSync() in queryEdgesSync"
      pattern: "getImplementationSync"
    - from: "bin/nf-solve.cjs autoClose result"
      to: "coderlm-lifecycle reindex"
      via: "reindex() call after autoClose() when actions_taken.length > 0"
      pattern: "reindex"
---

<objective>
Refactor queryEdgesSync() to use getImplementation for symbol-level edge discovery, and add a coderlm reindex call after autoClose() so subsequent solve iterations query a current call graph.

Purpose: CDIAG-01 requires symbol-level precision (getImplementation reveals which callers actually call the function, not just which modules import it). CDIAG-04 requires reindexing after remediation so that new/modified files are visible to coderlm in the next iteration.

Output: Updated bin/nf-solve.cjs with symbol-precise queryEdgesSync and post-autoClose reindex.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@bin/coderlm-adapter.cjs
@bin/coderlm-lifecycle.cjs
@.planning/phases/054-adapter-foundation/054-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getImplementationSync to coderlm adapter and refactor queryEdgesSync</name>
  <files>bin/coderlm-adapter.cjs, bin/nf-solve.cjs</files>
  <action>
    PRE-FLIGHT — Verify /implementation endpoint API contract:

    Before implementing getImplementationSync, read the existing async getImplementation() method in bin/coderlm-adapter.cjs (search for 'getImplementation'). Note the exact JSON response shape it parses — the current adapter at line ~313 calls GET /implementation?symbol=... and parses { file, line } from the response body. The plan originally assumed the response shape was { implementation: { file, startLine, endLine, callers: [...] } }, but the live adapter parses { file, line } only.

    Use the ACTUAL parsed shape from the existing getImplementation() method when implementing getImplementationSync — do NOT invent a shape. If the live endpoint returns { file, line } (not a nested `implementation` object), implement getImplementationSync to match that same shape and document the mismatch from the plan's assumption in a code comment.

    STEP A — Add getImplementationSync to bin/coderlm-adapter.cjs:

    Following the exact spawnSync pattern used by getCallersSync (search for 'getCallersSync' in the file), add a getImplementationSync(symbol) method to the adapter object returned by createAdapter():

    ```
    getImplementationSync(symbol) {
      if (!enabled) return { error: 'disabled' };
      const script = `
        const http = require('http');
        // ... same httpGet-via-inline-script pattern as getCallersSync
        // GET /implementation?symbol=<symbol>
        // Parse response using the SAME shape as the existing async getImplementation()
      `;
      const r = spawnSync(process.execPath, ['-e', script], { encoding: 'utf8', timeout: /* match existing */ });
      if (r.status !== 0 || !r.stdout) return { error: 'sync-spawn-failed' };
      try { return JSON.parse(r.stdout); } catch { return { error: 'parse' }; }
    }
    ```

    On error/disabled/timeout: return { error: string } (fail-open, never throw).

    STEP B — Build LAYER_SYMBOL_MAP in bin/nf-solve.cjs:

    Read the existing LAYER_SCRIPT_MAP constant (around line 131). For each entry that points to a DISTINCT script file (not 'bin/nf-solve.cjs' inline handlers), open that script file and inspect its module.exports to find the primary exported function name. Do NOT guess symbol names.

    Example discovery process:
    - For 'bin/formal-test-sync.cjs': run `node -e "const m = require('./bin/formal-test-sync.cjs'); console.log(Object.keys(m))"`
    - For 'bin/git-heatmap.cjs': run `node -e "const m = require('./bin/git-heatmap.cjs'); console.log(Object.keys(m))"`
    - For 'bin/hazard-model.cjs': run `node -e "const m = require('./bin/hazard-model.cjs'); console.log(Object.keys(m))"`
    - Repeat for all other unique scripts in LAYER_SCRIPT_MAP

    Build LAYER_SYMBOL_MAP only from verified export names. Inline handlers (entries pointing to 'bin/nf-solve.cjs') should be omitted from LAYER_SYMBOL_MAP since they have no distinct symbol to query.

    The number of entries in LAYER_SYMBOL_MAP equals the number of LAYER_SCRIPT_MAP entries that point to DISTINCT non-inline scripts. Do not pad or guess.

    STEP C — Refactor queryEdgesSync in bin/nf-solve.cjs (near line 158):

    New approach using getImplementationSync for symbol precision:
    1. For each active layer key present in LAYER_SYMBOL_MAP, call adapter.getImplementationSync(LAYER_SYMBOL_MAP[key])
    2. The response's callers array (if present) contains { file, line } objects — map each caller's file path back to a layer key using LAYER_SCRIPT_MAP reverse lookup
    3. Use path.resolve() comparison for reverse lookup (not substring matching) to avoid false matches on similar filenames
    4. Build edges: { from: callerLayerKey, to: targetLayerKey }

    If getImplementationSync returns { error } for a symbol, skip it (fail-open: that layer contributes no edges).
    Wrap the entire for-loop body in try/catch returning empty edges on any exception (CADP-02 compliance).

    If the /implementation response does NOT include a callers array (it only returns { file, line } per the pre-flight check), fall back to calling getCallersSync(symbol) to get callers, then use path.resolve() to map caller files back to layer keys. Document this fallback in the code comment.

    Verify: grep 'getImplementationSync' bin/coderlm-adapter.cjs and bin/nf-solve.cjs both return matches.
  </action>
  <verify>
    grep -n 'getImplementationSync' bin/coderlm-adapter.cjs
    grep -n 'getImplementationSync\|LAYER_SYMBOL_MAP' bin/nf-solve.cjs
    node -e "const a = require('./bin/coderlm-adapter.cjs'); const ad = a.createAdapter({enabled:false}); console.log(typeof ad.getImplementationSync)"
  </verify>
  <done>
    getImplementationSync method exists on adapter and returns { error: 'disabled' } when enabled=false.
    queryEdgesSync in nf-solve.cjs calls getImplementationSync (or documented fallback) instead of file-level string matching.
    LAYER_SYMBOL_MAP constant defined with entries derived from actual module.exports of the distinct layer scripts (no guessed names).
    Exact path.resolve() comparison used in reverse lookup (not substring).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add coderlm reindex after autoClose when files are modified (CDIAG-04)</name>
  <files>bin/nf-solve.cjs, bin/coderlm-lifecycle.cjs</files>
  <action>
    STEP A — Add reindex() to bin/coderlm-lifecycle.cjs:

    Add an exported function reindex(opts) that sends a POST request to the running coderlm server's /reindex endpoint. Use POST (not GET) — coderlm's /reindex endpoint is a mutating operation and follows REST conventions.

    For the HTTP POST sync call, implement using the same spawnSync child-node pattern as healthCheckSync (around line 83 of coderlm-lifecycle.cjs) — spawn a child node process that makes the async HTTP call and writes JSON to stdout.

    ```
    function reindex(opts) {
      try {
        opts = opts || {};
        const port = opts.port || DEFAULT_PORT;
        const script = `
          const http = require('http');
          const req = http.request({ hostname: 'localhost', port: ${port}, path: '/reindex', method: 'POST' }, (res) => {
            let body = '';
            res.on('data', d => body += d);
            res.on('end', () => {
              try { process.stdout.write(JSON.stringify({ status: res.statusCode, body })); }
              catch (e) { process.stdout.write(JSON.stringify({ status: res.statusCode, body: '' })); }
            });
          });
          req.on('error', (e) => process.stdout.write(JSON.stringify({ error: e.message })));
          req.end();
        `;
        const r = spawnSync(process.execPath, ['-e', script], { encoding: 'utf8', timeout: 5000 });
        if (!r.stdout) return { error: 'no response' };
        const result = JSON.parse(r.stdout);
        if (result.error) return { error: result.error };
        if (result.status >= 200 && result.status < 300) return { ok: true };
        return { error: 'reindex failed: status ' + result.status };
      } catch (e) {
        return { error: e.message };  // fail-open
      }
    }
    ```

    Export reindex from module.exports.

    STEP B — Wire reindex into bin/nf-solve.cjs after autoClose(), ONLY when remediation modified files:

    In the solve loop body (around line 5941 where autoClose() is called):

    ```
    const closeResult = autoClose(residual, oscillatingSet, waveOrder);
    iterations[iterations.length - 1].actions = closeResult.actions_taken;
    iterations[iterations.length - 1].wave_order = waveOrder;

    // CDIAG-04: Re-index coderlm only when autoClose actually modified files
    // Guard: actions_taken.length > 0 indicates remediation wrote/changed files
    if (closeResult.actions_taken && closeResult.actions_taken.length > 0) {
      try {
        const reindexResult = reindex({ port: 8787 });
        if (reindexResult.ok) {
          process.stderr.write(TAG + ' coderlm reindexed after remediation\n');
        } else {
          process.stderr.write(TAG + ' coderlm reindex skipped: ' + (reindexResult.error || 'unavailable') + '\n');
        }
      } catch (e) {
        process.stderr.write(TAG + ' coderlm reindex failed (non-fatal): ' + e.message + '\n');
      }
    }
    ```

    The guard ensures reindex is NOT called on iterations where autoClose found nothing to do (no actions_taken), avoiding pointless reindex overhead on convergence iterations.

    Update the require at the top of nf-solve.cjs (around line 60) to include reindex:
    `const { ensureRunning, touchLastQuery, checkIdleStop, reindex } = require('./coderlm-lifecycle.cjs');`

    Verify: grep 'reindex' bin/nf-solve.cjs returns the integration lines.
    Verify: grep 'reindex' bin/coderlm-lifecycle.cjs returns function definition and export.
    Run full test suite: npm run test:ci — confirm 0 new failures.
  </action>
  <verify>
    grep -n 'reindex' bin/coderlm-lifecycle.cjs | grep -E "function reindex|module.exports"
    grep -n 'reindex' bin/nf-solve.cjs | grep -v "//.*reindex" | head -5
    node -e "const lc = require('./bin/coderlm-lifecycle.cjs'); console.log(typeof lc.reindex)"
    npm run test:ci 2>&amp;1 | tail -5
  </verify>
  <done>
    reindex() exported from bin/coderlm-lifecycle.cjs (typeof === 'function').
    reindex() called via POST to /reindex in nf-solve.cjs after autoClose(), guarded by actions_taken.length > 0.
    Full test suite passes (0 new failures vs pre-task baseline).
    stderr shows 'coderlm reindexed after remediation' or 'reindex skipped' — never throws to caller.
    reindex() NOT called when actions_taken is empty (no pointless overhead on convergence iterations).
  </done>
</task>

</tasks>

<verification>
grep -n 'getImplementationSync' bin/coderlm-adapter.cjs
grep -n 'getImplementationSync\|LAYER_SYMBOL_MAP' bin/nf-solve.cjs
grep -n 'reindex' bin/coderlm-lifecycle.cjs | grep -E "^[0-9]+:(function|module)"
node -e "const a = require('./bin/coderlm-adapter.cjs'); const ad = a.createAdapter({enabled:false}); console.log(ad.getImplementationSync('foo'))"
node -e "const lc = require('./bin/coderlm-lifecycle.cjs'); console.log(typeof lc.reindex)"
npm run test:ci 2>&amp;1 | tail -3
</verification>

<success_criteria>
- getImplementationSync exists on adapter, fail-open (returns { error } when disabled, never throws)
- getImplementationSync response shape matches the actual live /implementation endpoint (verified via pre-flight read of existing getImplementation())
- queryEdgesSync uses getImplementationSync + LAYER_SYMBOL_MAP for symbol-level edge discovery
- LAYER_SYMBOL_MAP entries derived from actual module.exports of each distinct layer script (not guessed)
- Exact path.resolve() comparison used for caller file -> layer key reverse lookup
- reindex() exported from coderlm-lifecycle.cjs, uses POST to /reindex
- reindex() called after autoClose() in nf-solve.cjs, ONLY when actions_taken.length > 0
- Full test suite passes (0 regressions)
</success_criteria>

<output>
After completion, create .planning/phases/054-adapter-foundation/054-02-SUMMARY.md
</output>
