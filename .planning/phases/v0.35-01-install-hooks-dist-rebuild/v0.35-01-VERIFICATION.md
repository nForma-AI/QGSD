---
phase: v0.35-01-install-hooks-dist-rebuild
verified: 2026-03-13T21:00:00Z
status: passed
score: 3/3 must-haves verified
note: Retroactive verification — phase executed before nf-verifier was in auto-advance pipeline
---

# Phase v0.35-01: Install hooks/dist Rebuild Verification Report

**Phase Goal:** Fresh source clones auto-rebuild hooks/dist/ during install without requiring manual `npm run build:hooks`

**Verified:** 2026-03-13T21:00:00Z
**Status:** PASSED
**Re-verification:** No — retroactive verification (phase completed 2026-03-12, verified after nf-verifier pipeline addition)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Running `node bin/install.js --claude --global` on a fresh clone (no hooks/dist/) succeeds without error | ✓ VERIFIED | `test/install-fresh-clone.test.cjs` test "install succeeds when hooks/dist/ is initially missing" passes. Test removes hooks/dist/ then runs install via execFileSync — no throw. |
| 2 | If hooks/dist/ is missing or stale, install either auto-rebuilds it or prints a clear actionable message telling the user exactly what command to run | ✓ VERIFIED | `buildHooksIfMissing()` at bin/install.js:1629 calls `scripts/build-hooks.js` via execFileSync. On failure, stderr message includes exact `npm run build:hooks` command (line 2046). |
| 3 | After install completes, all hooks in ~/.claude/hooks/ are functional (not empty or stale copies) | ✓ VERIFIED | Test "installed hooks directory contains required hooks" asserts nf-stop.js, nf-prompt.js, nf-circuit-breaker.js present in installed hooks dir. Test "hooks/dist/ contains files matching HOOKS_TO_COPY count" cross-validates file count. |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/install.js` | Auto-rebuild logic for hooks/dist/ when missing, containing `buildHooksIfMissing` | ✓ VERIFIED | Function defined at line 1629, called at line 2040. Delegates to `scripts/build-hooks.js` via execFileSync (line 1634). Actionable error message at line 2046. |
| `test/install-fresh-clone.test.cjs` | Test proving fresh clone install works, min 40 lines | ✓ VERIFIED | File exists, 121 lines. Contains 5 test cases in 1 suite, all passing. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| bin/install.js | scripts/build-hooks.js | HOOKS_TO_COPY manifest shared or referenced | ✓ WIRED | Line 1634: `path.join(path.dirname(hooksDir), 'scripts', 'build-hooks.js')` — delegates build to canonical script |
| bin/install.js | hooks/dist/ | auto-rebuild creates dist/ before copy step | ✓ WIRED | `buildHooksIfMissing()` called at line 2040 before hooks copy block at line 2042+. Checks `fs.existsSync(distDir)`, rebuilds if missing. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| INST-01 | v0.35-01-01-PLAN.md | Auto-rebuild hooks/dist/ on fresh source checkout | ✓ SATISFIED | `buildHooksIfMissing()` detects missing dist/, runs build-hooks.js, verifies result. 5/5 tests pass. |
| INST-02 | v0.35-01-01-PLAN.md | Actionable error message if auto-rebuild fails | ✓ SATISFIED | Failure path prints `npm run build:hooks` command to stderr and pushes 'hooks' to failures array (line 2046). |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Status |
|------|------|---------|----------|--------|
| — | — | No anti-patterns found | — | — |

**Result:** No blockers or warnings found.

### Test Results Summary

**Fresh-clone tests:** 5/5 pass
- install succeeds when hooks/dist/ is initially missing ✓
- hooks/dist/ is rebuilt after install ✓
- installed hooks directory contains required hooks ✓
- re-install is idempotent ✓
- hooks/dist/ contains files matching HOOKS_TO_COPY count ✓

### Human Verification Required

None. All phase objectives have automated verification coverage.

---

## Implementation Summary

### Changes Made

**Task 1: Add buildHooksIfMissing() to installer**
- Added `buildHooksIfMissing(hooksDir, distDir)` function at line 1629
- Delegates to `scripts/build-hooks.js` via `execFileSync` to avoid duplicating HOOKS_TO_COPY manifest
- Silent on success, actionable stderr message on failure with exact `npm run build:hooks` command
- Integrated at line 2040, before existing hooks copy block
- Committed: `093cca1e`

**Task 2: Add fresh-clone install test**
- Created `test/install-fresh-clone.test.cjs` with 5 test cases
- Tests remove hooks/dist/, run install, verify rebuild, check idempotency
- After-hook restores hooks/dist/ via `node scripts/build-hooks.js`
- Committed: `20f4a7ad`

### Deviations

1 auto-fixed bug: test HOOKS_TO_COPY count adjusted for missing source hook (gsd-context-monitor.js removed by prior quick-283). No scope creep.

---

_Verified: 2026-03-13T21:00:00Z_
_Verifier: Claude (retroactive — phase predates nf-verifier in auto-advance pipeline)_
