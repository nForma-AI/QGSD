# v0.12-03-02 SUMMARY — Alloy 6 Spec + run-alloy.cjs

**Phase:** v0.12-03-static-analysis-suite
**Plan:** 02
**Completed:** 2026-02-25
**Duration:** ~5 min

## What Was Built

### Files Created
- `formal/alloy/quorum-votes.als` — Alloy 6 vote-counting model with pred MajorityReached, pred ValidRound, pred MinQuorumMet, assert NoSpuriousApproval, check command
- `bin/run-alloy.cjs` — CLI wrapper with JAVA_HOME gate, Java >=17 check, JAR existence check, ALS existence check, stdout-scanning for counterexample detection

## Verification

- `grep -c 'pred|assert|check' formal/alloy/quorum-votes.als` → 12 matches
- `grep 'pred MajorityReached' formal/alloy/quorum-votes.als` → exists
- `grep '^fact' formal/alloy/quorum-votes.als` → empty (no facts for vote-counting predicates)
- `node bin/run-alloy.cjs 2>&1` → exits 1 with "[run-alloy] org.alloytools.alloy.dist.jar not found" + download URL (expected — JAR gitignored)
- `node --test bin/run-alloy.test.cjs` → 4/4 GREEN

## Requirements Satisfied

| Requirement | Status |
|-------------|--------|
| ALY-01: quorum-votes.als with pred + check | COMPLETE |
| ALY-02: run-alloy.cjs with JAVA_HOME gate | COMPLETE |
| bin/run-alloy.test.cjs GREEN | COMPLETE |

## Key Decisions

- `stdio: 'pipe'` used (not `'inherit'`) so stdout can be scanned for "Counterexample" — Alloy 6 exits 0 even on counterexamples
- After scanning, stdout/stderr are manually written to process.stdout/stderr to mirror normal output behavior
- `pred` (not `fact`) used for all vote-counting predicates — ensures Alloy `check` can find counterexample instances
- Structure mirrors run-tlc.cjs exactly: JAVA_HOME gate → Java >=17 check → JAR check → ALS check → invoke
