---
phase: v0.23-01
slug: plan-phase-formal-integration
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-02
---

# Phase v0.23-01 — Validation Strategy

> Template created by `/qgsd:plan-phase v0.23-01` (step 5.5) after research.
> Populated by `/qgsd:plan-phase v0.23-01` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.23-01`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (node --test) |
| **Config file** | none — inline test files per module |
| **Quick run command** | `node --test bin/conformance-schema.test.cjs` |
| **Full suite command** | `node --test bin/*.test.cjs` |
| **Estimated runtime** | ~30 seconds |
| **CI pipeline** | .github/workflows/test.yml — exists |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/conformance-schema.test.cjs`
- **After every plan wave:** Run `node --test bin/*.test.cjs`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| {N}-01-01 | 01 | 1 | WFI-01 | unit | `node --test bin/conformance-schema.test.cjs` | ✅ / ❌ W0 | ⬜ pending |
| {N}-01-02 | 01 | 1 | WFI-02 | unit | `node --test bin/conformance-schema.test.cjs` | ✅ / ❌ W0 | ⬜ pending |
| {N}-02-01 | 02 | 2 | ENF-03 | integration | `node --test bin/conformance-schema.test.cjs` | ✅ N/A | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `bin/formal-gate.test.cjs` — stubs for WFI-01, WFI-02, ENF-03 formal gate integration tests
- [ ] test fixtures for keyword-match scan (mock formal/spec/ directory with sample specs)

*If none required: "Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed."*

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.
> These are surfaced during `/qgsd:verify-work` UAT.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| plan-phase formal gate skip warning when Java absent | ENF-03 | Requires Java uninstall or missing jar simulation | Run plan-phase on a module with specs but without Java; verify skip warning in output |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 30s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.23-01`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | {N} | — | — | — | scaffold |
| 1 | {N} | {command} | {N} | {N} | ✅ sampled |
| 2 | {N} | {command} | {N} | {N} | ✅ sampled |

**Phase validation complete:** pending
