---
phase: 53-skill-deprecation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/nf/model-driven-fix.md
  - core/workflows/model-driven-fix.md
  - commands/nf/solve-remediate.md
autonomous: true
requirements:
  - DEPR-01
  - DEPR-02
  - DEPR-03
formal_artifacts: none

must_haves:
  truths:
    - "Invoking /nf:model-driven-fix prints a deprecation notice that directs the user to /nf:debug instead of executing"
    - "The solve-remediate b_to_f layer dispatches through debug instead of model-driven-fix"
    - "A grep for model-driven-fix across all workflow/skill files returns only the deprecation shim and changelog references (no active dispatch paths remain)"
  artifacts:
    - path: "commands/nf/model-driven-fix.md"
      provides: "Deprecation shim that prints notice and exits without executing any model-driven-fix logic"
      contains: "deprecated"
    - path: "commands/nf/solve-remediate.md"
      provides: "b_to_f layer rewired to dispatch through /nf:debug instead of /nf:model-driven-fix"
      contains: "/nf:debug"
    - path: "core/workflows/model-driven-fix.md"
      provides: "Installed workflow copy replaced with deprecation shim matching commands/nf/model-driven-fix.md"
      contains: "deprecated"
  key_links:
    - from: "commands/nf/solve-remediate.md"
      to: "commands/nf/debug.md"
      via: "b_to_f layer dispatches /nf:debug instead of /nf:model-driven-fix"
      pattern: "/nf:debug"
---

<objective>
Deprecate /nf:model-driven-fix and rewire all consumers to the new debug integration points established in Phases 50-52. After this plan:
1. The model-driven-fix skill file becomes a deprecation shim that prints a notice and exits
2. The solve-remediate b_to_f layer dispatches through /nf:debug instead of /nf:model-driven-fix
3. No active dispatch paths to model-driven-fix remain outside the shim itself

Purpose: Complete the v0.41 deprecation phase by cleaning up the old model-driven-fix skill now that /nf:debug has absorbed all its capabilities.
Output: Updated commands/nf/model-driven-fix.md (shim), core/workflows/model-driven-fix.md (shim), commands/nf/solve-remediate.md (rewired b_to_f layer).
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/nf/model-driven-fix.md
@core/workflows/model-driven-fix.md
@commands/nf/solve-remediate.md
@.planning/phases/53-skill-deprecation/53-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace model-driven-fix skill with deprecation shim (DEPR-01)</name>
  <files>commands/nf/model-driven-fix.md, core/workflows/model-driven-fix.md</files>
  <action>
Replace the entire contents of `commands/nf/model-driven-fix.md` with a deprecation shim. The shim must:

1. Keep the `<purpose>` tag but change its text to indicate this skill is deprecated
2. Replace the entire `<process>` block with a single step that prints a deprecation notice and exits
3. The deprecation notice must clearly state: "This command is deprecated. Use `/nf:debug` instead — it includes all formal model capabilities."
4. The shim does NOT execute any model-driven-fix logic — it only prints and exits

New content for `commands/nf/model-driven-fix.md`:
```markdown
<purpose>
DEPRECATED — This skill has been replaced by /nf:debug which includes all formal model capabilities (discovery, reproduction, refinement, constraint extraction, solution simulation, constrained fix, and pre-verification).
</purpose>

<process>

## Deprecation Notice

Display the following notice and exit immediately. Do NOT execute any further steps.

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 nForma ► DEPRECATED COMMAND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  /nf:model-driven-fix is deprecated.

  Use /nf:debug instead — it includes all formal model
  capabilities (discovery, reproduction, refinement,
  constraint extraction, solution simulation).

  Example:
    /nf:debug "your bug description" --files=path/to/file.js

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Exit workflow. Do not proceed.

</process>
```

Then apply the identical replacement to `core/workflows/model-driven-fix.md` so the installed copy matches.
  </action>
  <verify>
1. Read commands/nf/model-driven-fix.md and confirm it contains ONLY the deprecation shim (no process steps beyond printing the notice)
2. Read core/workflows/model-driven-fix.md and confirm it is identical to commands/nf/model-driven-fix.md
3. Confirm the shim text includes "/nf:debug" as the replacement command
4. Confirm no executable logic remains (no Phase 0-6 steps, no argument parsing, no tool invocations)
  </verify>
  <done>commands/nf/model-driven-fix.md and core/workflows/model-driven-fix.md replaced with deprecation shim that prints notice directing to /nf:debug and exits without executing any logic.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire solve-remediate b_to_f layer to dispatch /nf:debug (DEPR-02)</name>
  <files>commands/nf/solve-remediate.md</files>
  <action>
In `commands/nf/solve-remediate.md`, update the b_to_f layer (around lines 173 and 238-251) to dispatch through `/nf:debug` instead of `/nf:model-driven-fix`:

1. **Line ~173 (layer table):** Change the b_to_f row from:
   `| b_to_f | 3a-extra. B->F Gaps | Yes — dispatches /nf:close-formal-gaps + /nf:model-driven-fix |`
   to:
   `| b_to_f | 3a-extra. B->F Gaps | Yes — dispatches /nf:close-formal-gaps + /nf:debug |`

2. **Line ~243 (dispatch command):** Change the dispatch from:
   ```
   /nf:model-driven-fix --bug-context "{test_file_path}: test fails but formal model passes" --model-paths={model_paths}
   ```
   to:
   ```
   /nf:debug "{test_file_path}: test fails but formal model passes" --files={model_paths}
   ```
   Note the argument format change: `/nf:debug` takes the bug description as its first positional argument and uses `--files=` instead of `--bug-context` and `--model-paths`.

3. **Line ~247 (note about model-driven-fix Phase 3):** Change:
   `Note: model-driven-fix Phase 3 now uses autoresearch-refine.cjs for iterative`
   to:
   `Note: /nf:debug uses autoresearch-refine.cjs for iterative`

4. **Line ~251 (log message):** Change:
   `"B->F: dispatching model-driven-fix (autoresearch-refine, in-memory rollback) for blind spot {bug_id} ({N} models to refine)"`
   to:
   `"B->F: dispatching /nf:debug (autoresearch-refine, in-memory rollback) for blind spot {bug_id} ({N} models to refine)"`

5. **Any other references** to model-driven-fix in solve-remediate.md: update to /nf:debug with appropriate argument mapping.
  </action>
  <verify>
1. Grep commands/nf/solve-remediate.md for "model-driven-fix" — should return zero matches
2. Grep commands/nf/solve-remediate.md for "/nf:debug" — should find the b_to_f dispatch references
3. Confirm the b_to_f layer table row shows "/nf:debug" not "/nf:model-driven-fix"
4. Confirm the dispatch command uses /nf:debug argument format (positional bug description + --files=)
  </verify>
  <done>solve-remediate b_to_f layer rewired: all dispatches now go through /nf:debug instead of /nf:model-driven-fix, with argument format updated to match the debug skill's interface.</done>
</task>

<task type="auto">
  <name>Task 3: Verify no active dispatch paths remain (DEPR-03)</name>
  <files>commands/nf/solve-remediate.md</files>
  <action>
Run a comprehensive grep for "model-driven-fix" across all workflow and skill files to verify no active dispatch paths remain:

```bash
grep -r "model-driven-fix" commands/ core/ --include="*.md" --include="*.js" --include="*.cjs" --include="*.mjs"
```

**Expected results — only these references are acceptable:**
1. `commands/nf/model-driven-fix.md` — the deprecation shim itself (contains "deprecated" and "/nf:debug")
2. `core/workflows/model-driven-fix.md` — the installed workflow shim (identical to above)

**NOT acceptable:**
- Any dispatch/invocation of `/nf:model-driven-fix` as an active command
- Any reference that routes execution through model-driven-fix logic
- Any `--bug-context` or `--model-paths` flags (these are model-driven-fix-specific)

If any unexpected references are found, update them:
- Active dispatch paths → rewire to `/nf:debug` with appropriate argument mapping
- Documentation references → update to note the deprecation

After cleanup, re-run the grep to confirm only the shim files remain.
  </action>
  <verify>
1. Run: `grep -r "model-driven-fix" commands/ core/ --include="*.md" --include="*.js" --include="*.cjs" --include="*.mjs" -l`
2. Result should list ONLY: commands/nf/model-driven-fix.md and core/workflows/model-driven-fix.md
3. Confirm those files contain only the deprecation shim text, not any active logic
4. Run: `grep -r "\-\-bug-context\|\-\-model-paths" commands/ core/ --include="*.md"` — should return zero matches (these flags are model-driven-fix-specific and should not appear in any active dispatch)
  </verify>
  <done>Comprehensive grep confirms no active dispatch paths to model-driven-fix remain. Only the deprecation shim files reference the old skill name, and they redirect to /nf:debug.</done>
</task>

</tasks>

<verification>
- [ ] commands/nf/model-driven-fix.md is a deprecation shim that prints notice and exits
- [ ] core/workflows/model-driven-fix.md matches the shim in commands/nf/
- [ ] The deprecation notice directs users to /nf:debug
- [ ] No model-driven-fix logic executes when the shim runs
- [ ] solve-remediate b_to_f layer dispatches /nf:debug not /nf:model-driven-fix
- [ ] Dispatch argument format matches /nf:debug interface (positional description + --files=)
- [ ] grep for model-driven-fix in commands/ and core/ returns only the two shim files
- [ ] No --bug-context or --model-paths flags remain in active dispatch paths
- [ ] Existing formal_coverage_auto_detection and Loop 2 behaviors are unaffected
</verification>

<success_criteria>
- Invoking /nf:model-driven-fix prints a deprecation notice directing to /nf:debug instead of executing
- The solve-remediate b_to_f layer dispatches through debug instead of model-driven-fix
- A grep for model-driven-fix across all workflow/skill files returns only the deprecation shim and changelog references (no active dispatch paths remain)
</success_criteria>

<output>
After completion, create `.planning/phases/53-skill-deprecation/53-01-SUMMARY.md`
</output>
