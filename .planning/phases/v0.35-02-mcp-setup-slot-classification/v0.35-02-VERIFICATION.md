---
phase: v0.35-02-mcp-setup-slot-classification
verified: 2026-03-13T21:00:00Z
status: passed
score: 3/3 must-haves verified
note: Retroactive verification — phase executed before nf-verifier was in auto-advance pipeline
---

# Phase v0.35-02: MCP Setup Slot Classification Verification Report

**Phase Goal:** Declarative `auth_type` field in providers.json replaces name-prefix inference for slot classification, wired through mcp-setup first-run and re-run flows

**Verified:** 2026-03-13T21:00:00Z
**Status:** PASSED
**Re-verification:** No — retroactive verification (phase completed 2026-03-12, verified after nf-verifier pipeline addition)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Running `/nf:mcp-setup` re-run shows codex-1 classified as 'subscription' (CLI agent), not 'api' or 'provider-backed' | ✓ VERIFIED | codex-1 has `auth_type: "sub"` in providers.json. mcp-setup.md re-run flow syncs auth_type from providers.json before displaying agent roster (12 auth_type references in workflow). |
| 2 | Slot classification reads `auth_type` field from providers.json for every slot, not inferring type from the slot name prefix | ✓ VERIFIED | Test "no name-prefix inference needed for classification" passes — proves `providerMap[slotName].auth_type` works without regex/startsWith/includes. All 12 providers have explicit auth_type field (0 missing). |
| 3 | All slot types in providers.json (subscription CLI, API-backed) display their correct category in the setup wizard menu | ✓ VERIFIED | Test "auth_type matches display_type pattern" cross-validates: CLI display_type slots → auth_type "sub", claude-code-router slots → auth_type "api". 6 sub + 6 api = 12 total. |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/providers.json` | auth_type field for all 12 provider slots, containing `auth_type` | ✓ VERIFIED | All 12 providers have auth_type. 6 "sub": codex-1, codex-2, gemini-1, gemini-2, opencode-1, copilot-1. 6 "api": claude-1 through claude-6. Zero missing. |
| `commands/nf/mcp-setup.md` | Setup workflow that reads auth_type from providers.json and writes to nf.json | ✓ VERIFIED | 12 occurrences of `auth_type` in workflow file. Covers first-run onboarding (Step 3d) and re-run agent menu sync. |
| `test/mcp-setup-auth-type.test.cjs` | Tests proving auth_type schema completeness and correct classification, min 50 lines | ✓ VERIFIED | File exists, 106 lines. Contains 6 test cases in 1 suite, all passing. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| bin/providers.json | commands/nf/mcp-setup.md | Setup reads auth_type from providers.json during onboarding | ✓ WIRED | mcp-setup.md references auth_type in both first-run (Step 3d) and re-run sync sections |
| commands/nf/mcp-setup.md | ~/.claude/nf.json | Setup writes auth_type to agent_config[slotName].auth_type | ✓ WIRED | Workflow instructs writing auth_type per slot to nf.json agent_config |
| hooks/nf-prompt.js | ~/.claude/nf.json | Reads auth_type at runtime for T1/T2 slot separation | ✓ WIRED | nf-prompt.js line 506 reads `agentCfg.*auth_type` for tiered dispatch |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SETUP-01 | v0.35-02-01-PLAN.md | All 12 providers have explicit auth_type field in providers.json | ✓ SATISFIED | `providers.json` verification: 0 missing auth_type entries. 6 "sub" + 6 "api" = 12 total. |
| SETUP-02 | v0.35-02-01-PLAN.md | mcp-setup workflow writes auth_type to nf.json during first-run and re-run | ✓ SATISFIED | 12 auth_type references in commands/nf/mcp-setup.md covering both flow paths. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Status |
|------|------|---------|----------|--------|
| — | — | No anti-patterns found | — | — |

**Result:** No blockers or warnings found.

### Test Results Summary

**auth_type schema tests:** 6/6 pass
- every provider in providers.json has auth_type field ✓
- auth_type values are only "sub" or "api" ✓
- subscription CLI slots have auth_type=sub ✓
- API-backed ccr slots have auth_type=api ✓
- auth_type matches display_type pattern ✓
- no name-prefix inference needed for classification ✓

### Human Verification Required

None. All phase objectives have automated verification coverage.

---

## Implementation Summary

### Changes Made

**Task 1: Add auth_type to providers.json and wire in mcp-setup workflow**
- Added `auth_type` field to all 12 providers in `bin/providers.json` (6 "sub", 6 "api")
- Wired auth_type sync into `commands/nf/mcp-setup.md` first-run (Step 3d) and re-run flows
- Re-run syncs ALL configured agents, fixing existing installations on next mcp-setup re-run
- Committed: `1c42cfe0`

**Task 2: Add auth_type schema and classification tests**
- Created `test/mcp-setup-auth-type.test.cjs` with 6 test cases
- Cross-validates auth_type against display_type (independent signal)
- Proves direct-lookup pattern works without name-prefix inference
- Committed: `441812d8`

### Deviations

None — plan executed exactly as written.

### Known Pre-existing Issues

- verify-hooks-sync test failure from quick-283 gsd-context-monitor rename — not caused by this phase, out of scope.

---

_Verified: 2026-03-13T21:00:00Z_
_Verifier: Claude (retroactive — phase predates nf-verifier in auto-advance pipeline)_
