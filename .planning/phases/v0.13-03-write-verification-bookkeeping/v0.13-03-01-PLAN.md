---
phase: v0.13-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md
autonomous: true
requirements: [LOOP-01, LOOP-02, LOOP-03, STATE-01]

must_haves:
  truths:
    - ".planning/phases/v0.13-01-loop-wiring/VERIFICATION.md exists with status: passed"
    - "All four requirements (LOOP-01, LOOP-02, LOOP-03, STATE-01) appear in the Requirements Check table with Result: PASS"
    - "Each requirement has an evidence citation pointing to a qgsd-core/ source file and line number"
    - "Verification Commands Run section contains actual grep commands with expected output"
  artifacts:
    - path: ".planning/phases/v0.13-01-loop-wiring/VERIFICATION.md"
      provides: "Formal verification artifact for v0.13-01 Loop Wiring phase"
      contains: "phase: v0.13-01"
  key_links:
    - from: ".planning/phases/v0.13-01-loop-wiring/VERIFICATION.md"
      to: "qgsd-core/workflows/transition.md"
      via: "evidence citations"
      pattern: "transition\\.md.*line"
    - from: ".planning/phases/v0.13-01-loop-wiring/VERIFICATION.md"
      to: "qgsd-core/workflows/audit-milestone.md"
      via: "evidence citations"
      pattern: "audit-milestone\\.md.*line"
---

<objective>
Write the formal VERIFICATION.md artifact for Phase v0.13-01 (Loop Wiring), closing the orphaned-requirement status for LOOP-01, LOOP-02, LOOP-03, and STATE-01.

Purpose: The v0.13 milestone audit scored 0/8 requirements because neither phase produced a VERIFICATION.md. Per CLAUDE.md Design Principles: "Skipping these is the failure mode. The audit trail is the enforcement." This plan produces the missing artifact for v0.13-01.

Output: `.planning/phases/v0.13-01-loop-wiring/VERIFICATION.md` with YAML frontmatter, Requirements Check table, and Verification Commands Run section citing actual source file line numbers.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.13-01-loop-wiring/v0.13-01-01-SUMMARY.md
@.planning/phases/v0.13-03-write-verification-bookkeeping/v0.13-03-RESEARCH.md
@.planning/v0.13-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run grep evidence checks against v0.13-01 source files</name>
  <files>.planning/phases/v0.13-01-loop-wiring/VERIFICATION.md</files>
  <action>
Run the following grep commands against the qgsd-core/ source files (from /Users/jonathanborduas/code/QGSD) to gather the evidence lines needed for the VERIFICATION.md:

LOOP-01 evidence (transition.md Route B IS_GAP_CLOSURE=0 → audit-milestone):
```
grep -n "IS_GAP_CLOSURE=0" qgsd-core/workflows/transition.md
grep -n "audit-milestone" qgsd-core/workflows/transition.md
grep -c "complete-milestone" qgsd-core/workflows/transition.md
```

LOOP-02 evidence (IS_GAP_CLOSURE detection + Gap Closure marker routing):
```
grep -n "IS_GAP_CLOSURE" qgsd-core/workflows/transition.md
grep -n "Gap Closure" qgsd-core/workflows/transition.md
```

LOOP-03 evidence (audit-milestone auto-spawns plan-milestone-gaps Task):
```
grep -n "general-purpose" qgsd-core/workflows/audit-milestone.md
grep -n "plan-milestone-gaps" qgsd-core/workflows/audit-milestone.md
grep -n "plan_exists_not_executed" qgsd-core/workflows/audit-milestone.md
```

STATE-01 evidence (Step 6b gsd-tools state record-session):
```
grep -n "state record-session" qgsd-core/workflows/audit-milestone.md
grep -n "## 6b" qgsd-core/workflows/audit-milestone.md
```

Record exact output (file, line number, matched text) for each command. These become the evidence citations and "Verification Commands Run" output in the VERIFICATION.md.
  </action>
  <verify>All grep commands return at least one match (or zero for the complete-milestone check). No command errors.</verify>
  <done>Evidence line numbers captured for all 4 requirements before writing the VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Write VERIFICATION.md for v0.13-01</name>
  <files>.planning/phases/v0.13-01-loop-wiring/VERIFICATION.md</files>
  <action>
Write `.planning/phases/v0.13-01-loop-wiring/VERIFICATION.md` using the project's standard format (same structure as `v0.9-05-VERIFICATION.md`).

Use the evidence captured in Task 1 to populate the line number citations.

The file MUST contain:

**YAML frontmatter:**
```yaml
---
phase: v0.13-01
status: passed
verified: 2026-02-25
verifier: Claude Sonnet 4.6
---
```

**Phase Goal Verification section:**
```markdown
## Phase Goal Verification

**Phase:** v0.13-01 — Loop Wiring
**Goal:** Wire audit-milestone into the last-phase transition chain; detect gap-closure re-audit vs. primary completion path; audit-milestone auto-spawns plan-milestone-gaps on gaps_found; STATE.md updated with audit result.
```

**Requirements Check table** (all 4 requirements must be PASS):
```markdown
## Requirements Check

| Requirement | Description | Result |
|-------------|-------------|--------|
| LOOP-01 | Last-phase transition calls audit-milestone before complete-milestone (primary path) | PASS |
| LOOP-02 | Gap Closure marker detection routes to audit-milestone re-audit path | PASS |
| LOOP-03 | audit-milestone auto-spawns plan-milestone-gaps Task when gaps_found + missing_no_plan | PASS |
| STATE-01 | audit-milestone updates STATE.md via gsd-tools state record-session after Step 6 | PASS |
```

**Per-requirement evidence grid** — for each requirement, include:
- File: `qgsd-core/workflows/transition.md` or `qgsd-core/workflows/audit-milestone.md`
- Line numbers from Task 1 grep output
- Brief description of what the line implements

Example format:
```markdown
### LOOP-01 — Last-phase transition calls audit-milestone (primary path)
**File:** `qgsd-core/workflows/transition.md`
**Evidence:** Lines {from Task 1} — Step 2b (IS_GAP_CLOSURE=0 block) invokes `/qgsd:audit-milestone` in both yolo and interactive sub-paths; `complete-milestone` does not appear in Route B (0 matches).
```

**Verification Commands Run section** with actual commands + expected output:
```markdown
## Verification Commands Run

```bash
# LOOP-01: Route B IS_GAP_CLOSURE=0 → audit-milestone
grep -n "IS_GAP_CLOSURE=0" qgsd-core/workflows/transition.md
→ {line N}: {matched text}

grep -c "complete-milestone" qgsd-core/workflows/transition.md
→ 0 (Route B no longer invokes complete-milestone directly)

# LOOP-02: IS_GAP_CLOSURE detection
grep -n "IS_GAP_CLOSURE" qgsd-core/workflows/transition.md
→ {lines}: {matched text}

# LOOP-03: auto-spawn Task in offer_next
grep -n "general-purpose" qgsd-core/workflows/audit-milestone.md
→ {line N}: {matched text}

grep -n "plan-milestone-gaps" qgsd-core/workflows/audit-milestone.md
→ {lines}: {matched text}

# STATE-01: Step 6b state record-session
grep -n "state record-session" qgsd-core/workflows/audit-milestone.md
→ {line N}: {matched text}

grep -n "## 6b" qgsd-core/workflows/audit-milestone.md
→ {line N}: {matched text}
```
```

All citations MUST use `qgsd-core/` source paths (NOT `~/.claude/` installed paths). All 4 requirements must show PASS. Today's date is 2026-02-25.
  </action>
  <verify>
```bash
test -f .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md && echo "PASS" || echo "FAIL"
grep -c "LOOP-01" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md   # expect: ≥1
grep -c "LOOP-02" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md   # expect: ≥1
grep -c "LOOP-03" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md   # expect: ≥1
grep -c "STATE-01" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md  # expect: ≥1
grep -c "PASS" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md      # expect: ≥4
grep -c "status: passed" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md  # expect: 1
grep -c "qgsd-core/" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md     # expect: ≥1
grep -c "\.claude/qgsd" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md  # expect: 0 (no installed-path citations)
```
  </verify>
  <done>
VERIFICATION.md exists at `.planning/phases/v0.13-01-loop-wiring/VERIFICATION.md` with:
- YAML frontmatter: phase: v0.13-01, status: passed, verified: 2026-02-25
- Requirements Check table: LOOP-01, LOOP-02, LOOP-03, STATE-01 all PASS
- Per-requirement evidence grid with qgsd-core/ source file + line number citations
- Verification Commands Run section with actual grep output
- Zero references to installed ~/.claude/ paths in evidence citations
  </done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md` — file exists
2. `grep -c "status: passed" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md` — returns 1
3. `grep -c "PASS" .planning/phases/v0.13-01-loop-wiring/VERIFICATION.md` — returns ≥4
4. All 4 requirement IDs (LOOP-01, LOOP-02, LOOP-03, STATE-01) present
5. No `~/.claude/` path references in evidence citations (only `qgsd-core/`)
</verification>

<success_criteria>
`.planning/phases/v0.13-01-loop-wiring/VERIFICATION.md` exists with:
- YAML frontmatter with `status: passed` and `verified: 2026-02-25`
- All four v0.13-01 requirements (LOOP-01, LOOP-02, LOOP-03, STATE-01) listed as PASS
- Evidence citations pointing to `qgsd-core/workflows/transition.md` and `qgsd-core/workflows/audit-milestone.md` with actual line numbers
- Verification Commands Run section with real grep output
</success_criteria>

<output>
After completion, create `.planning/phases/v0.13-03-write-verification-bookkeeping/v0.13-03-01-SUMMARY.md`
</output>
