---
phase: v0.27-03
plan: 03
type: execute
wave: 3
depends_on:
  - v0.27-03-01
  - v0.27-03-02
files_modified:
  - bin/observe-debt-writer.cjs
  - bin/observe-debt-writer.test.cjs
  - bin/debt-dedup-integration.test.cjs
autonomous: true
formal_artifacts: none
requirements:
  - FP-03
  - FP-04
  - DEBT-02
  - DEBT-05

must_haves:
  truths:
    - "observe-debt-writer integrates dedup and formal-ref linking after writing new observations"
    - "End-to-end pipeline: observations from multiple sources are deduplicated by fingerprint, near-duplicates merged by Levenshtein, and formal references auto-linked"
    - "Dedup output reports merged count in default mode and per-merge detail in verbose mode"
    - "Integration test proves full lifecycle: write -> dedup -> link -> verify merged ledger"
  artifacts:
    - path: "bin/observe-debt-writer.cjs"
      provides: "Updated observe-debt-writer with integrated dedup and formal-ref linking"
      exports: ["writeObservationsToDebt"]
    - path: "bin/observe-debt-writer.test.cjs"
      provides: "Extended tests covering dedup integration"
      min_lines: 80
    - path: "bin/debt-dedup-integration.test.cjs"
      provides: "End-to-end integration test for complete dedup pipeline"
      min_lines: 80
  key_links:
    - from: "bin/observe-debt-writer.cjs"
      to: "bin/debt-dedup.cjs"
      via: "calls deduplicateEntries after writing new observations"
      pattern: "require.*debt-dedup"
    - from: "bin/observe-debt-writer.cjs"
      to: "bin/formal-ref-linker.cjs"
      via: "calls linkFormalRefs after dedup completes"
      pattern: "require.*formal-ref-linker"
    - from: "bin/debt-dedup-integration.test.cjs"
      to: "bin/observe-debt-writer.cjs"
      via: "exercises full pipeline end-to-end"
      pattern: "writeObservationsToDebt"
---

<objective>
Integrate the dedup engine and formal reference linker into the observe-debt-writer pipeline, and create a comprehensive end-to-end integration test.

Purpose: Connects all the pieces built in Plans 01 and 02 into the existing observe workflow. After this plan, `/qgsd:observe` will automatically deduplicate cross-source observations and link them to formal references.

Output: Updated observe-debt-writer.cjs with integrated dedup + formal-ref, extended tests, and end-to-end integration test.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v0.27-03-cross-source-deduplication/v0.27-03-RESEARCH.md
@.planning/phases/v0.27-03-cross-source-deduplication/v0.27-03-CONTEXT.md
@.planning/phases/v0.27-03-cross-source-deduplication/v0.27-03-01-SUMMARY.md
@.planning/phases/v0.27-03-cross-source-deduplication/v0.27-03-02-SUMMARY.md
@bin/observe-debt-writer.cjs
@bin/debt-dedup.cjs
@bin/formal-ref-linker.cjs
@bin/debt-ledger.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate dedup and formal-ref linking into observe-debt-writer</name>
  <files>
    bin/observe-debt-writer.cjs
    bin/observe-debt-writer.test.cjs
  </files>
  <action>
Update bin/observe-debt-writer.cjs to add dedup and formal-ref linking after the initial write pass:

1. Add imports at top:
   - const { deduplicateEntries } = require('./debt-dedup.cjs');
   - const { linkFormalRefs } = require('./formal-ref-linker.cjs');

2. After the existing write loop (after all observations are processed), add:
   - Call deduplicateEntries(ledger.debt_entries, { threshold: options.threshold || 0.85 })
   - Replace ledger.debt_entries with deduplicated result
   - Call linkFormalRefs(deduplicated entries, { requirementsPath, specDir })
   - Replace ledger.debt_entries with linked result

3. Update return value to include dedup and linking stats:
   - Add merged: dedupResult.mergeCount
   - Add linked: linkResult.linkedCount
   - Add mergeLog: dedupResult.mergeLog (for verbose output)
   - Add linkLog: linkResult.linkLog (for verbose output)

4. Update writeObservationsToDebt signature to accept options parameter:
   - writeObservationsToDebt(observations, ledgerPath, options = {})
   - options.threshold — Levenshtein threshold (default 0.85)
   - options.verbose — include detailed merge/link logs in return value
   - options.requirementsPath — custom requirements.json path (for testing)
   - options.specDir — custom spec directory path (for testing)

5. Write updated ledger only if changes occurred (written > 0 || updated > 0 || merged > 0 || linked > 0)

Update bin/observe-debt-writer.test.cjs with new tests:
- Two observations from different sources with matching fingerprints: results in 1 entry (not 2) with occurrences: 2
- Two observations from different sources with similar titles (>= 0.85): merged by Levenshtein
- Observation with title matching a requirement gets formal_ref auto-detected
- Return value includes merged and linked counts
- Existing tests continue to pass (no regression)
- Options parameter correctly passes threshold to dedup engine
  </action>
  <verify>node --test bin/observe-debt-writer.test.cjs</verify>
  <done>
- observe-debt-writer calls deduplicateEntries after writing new observations
- observe-debt-writer calls linkFormalRefs after dedup completes
- Return value includes merged count, linked count, and logs
- Threshold is configurable via options
- All existing and new tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration test for complete dedup pipeline</name>
  <files>
    bin/debt-dedup-integration.test.cjs
  </files>
  <action>
Create bin/debt-dedup-integration.test.cjs exercising the full pipeline end-to-end:

Test 1 — Cross-source fingerprint dedup:
- Create temp directory with empty debt.json
- Write 3 observations: obs1 from "github" and obs2 from "sentry" with same exception_type + function_name + message (matching fingerprint), obs3 from "bash" with different issue
- Call writeObservationsToDebt with temp ledger path
- Assert: debt.json has 2 entries (not 3), merged entry has occurrences: 2, source_entries has both github + sentry sources
- Clean up temp directory

Test 2 — Levenshtein near-duplicate merge:
- Create temp with empty debt.json
- Write 2 observations: "TypeError in authentication handler" from github and "TypeError in auth handler" from sentry (different fingerprints due to different function_name, but similar titles >= 0.85)
- Call writeObservationsToDebt
- Assert: debt.json has 1 entry (merged), source_entries has both sources, primary is the one with higher occurrence count (both 1, so first wins)

Test 3 — Formal reference auto-linking:
- Create temp with empty debt.json and mock requirements.json with 3 entries (DEBT-01 about "schema validation", FP-01 about "fingerprint", DEBT-02 about "deduplication")
- Write observation with title "Schema validation fails on empty entries"
- Call writeObservationsToDebt with custom requirementsPath
- Assert: entry has formal_ref: "requirement:DEBT-01", formal_ref_source: "auto-detect"

Test 4 — Full lifecycle (write -> dedup -> link -> verify):
- Create temp with seed debt.json containing 1 existing entry (fingerprint X, occurrences: 3)
- Write 2 new observations: one matching fingerprint X (should merge), one new entry
- Call writeObservationsToDebt
- Assert: ledger has 2 entries (not 3), merged entry has occurrences: 4 (3+1), new entry has occurrences: 1
- Assert: formal_ref linking attempted on both entries
- Assert: return includes written: 1, updated: 1, merged: 0 (fingerprint match handled in write loop), linked: N

Test 5 — Threshold configuration:
- Create temp with empty debt.json
- Write 2 observations with moderately similar titles (similarity ~0.87)
- Call writeObservationsToDebt with threshold: 0.90
- Assert: NOT merged (below 0.90 threshold) — 2 entries remain
- Call writeObservationsToDebt with threshold: 0.85
- Assert: merged (above 0.85 threshold) — 1 entry

Use node:test framework, require assert/strict, use tmp directories for isolation.
  </action>
  <verify>node --test bin/debt-dedup-integration.test.cjs</verify>
  <done>
- Full pipeline exercises: observations -> fingerprint dedup -> Levenshtein dedup -> formal-ref linking -> ledger write
- Cross-source dedup verified (github + sentry observations merge correctly)
- Near-duplicate merge verified (similar titles from different sources merge)
- Formal reference auto-linking verified (title keyword matching works)
- Threshold configuration verified (different thresholds change merge behavior)
- All integration tests pass with proper cleanup
  </done>
</task>

</tasks>

<verification>
- `node --test bin/observe-debt-writer.test.cjs` — all tests pass (existing + new)
- `node --test bin/debt-dedup-integration.test.cjs` — all integration tests pass
- Full suite: `node --test bin/*debt*.test.cjs bin/*fingerprint*.test.cjs bin/formal-ref-linker.test.cjs bin/levenshtein.test.cjs` — all pass
</verification>

<success_criteria>
- The complete dedup pipeline is integrated into the observe-debt-writer workflow
- Cross-source observations are deduplicated by fingerprint and near-duplicate similarity
- Formal references are auto-linked with layered resolution
- End-to-end integration test proves the full lifecycle works
- All tests pass including existing v0.27-01 tests (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/v0.27-03-cross-source-deduplication/v0.27-03-03-SUMMARY.md`
</output>
