---
phase: v0.39-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/run-tlc.cjs
  - bin/run-alloy.cjs
  - bin/refinement-loop.cjs
  - bin/verification-mode.cjs
  - bin/verification-mode.test.cjs
  - bin/config-update.cjs
  - bin/config-update.test.cjs
  - .planning/config.json
autonomous: true
requirements:
  - FND-01
  - FND-03
formal_artifacts: none

must_haves:
  truths:
    - "Every model checker invocation carries a verification_mode tag ('diagnostic' or 'validation') that downstream consumers can read and act on"
    - "Running with --max-iterations N limits both Cycle 1 and Cycle 2 loops to N iterations, and the default of 3 is persisted in .planning/config.json"
  artifacts:
    - path: "bin/verification-mode.cjs"
      provides: "Mode tagging envelope and gate interpretation logic"
      exports: ["createVerificationEnvelope", "interpretGateResult", "MODES"]
    - path: "bin/verification-mode.test.cjs"
      provides: "Contract tests for mode tagging"
      contains: "diagnostic"
    - path: "bin/config-update.cjs"
      provides: "Iteration limit persistence and session directory creation"
      exports: ["updateMaxIterations", "getMaxIterations", "createSessionDirectory"]
    - path: "bin/config-update.test.cjs"
      provides: "Config persistence tests"
      contains: "max_iterations"
  key_links:
    - from: "bin/verification-mode.cjs"
      to: "bin/run-tlc.cjs"
      via: "Optional verification_mode parameter accepted by run-tlc"
      pattern: "verification_mode"
    - from: "bin/config-update.cjs"
      to: ".planning/config.json"
      via: "fs.readFileSync/writeFileSync atomic update"
      pattern: "max_iterations"
---

<objective>
Add verification mode tagging to model checker invocations and configurable iteration limits to .planning/config.json.

Purpose: Enable Cycle 1 (diagnostic, inverted semantics) and Cycle 2 (validation, normal semantics) to coexist without mode contradiction, and ensure iteration loops have a configurable, persisted bound.
Output: bin/verification-mode.cjs (mode envelope + gate interpretation), bin/config-update.cjs (iteration persistence), updated checker runners accepting verification_mode parameter.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.39-01-foundation-and-infrastructure/v0.39-01-RESEARCH.md
@bin/run-tlc.cjs
@bin/run-alloy.cjs
@bin/refinement-loop.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verification mode tagging module and update checker runners</name>
  <files>
    bin/verification-mode.cjs
    bin/verification-mode.test.cjs
    bin/run-tlc.cjs
    bin/run-alloy.cjs
    bin/refinement-loop.cjs
  </files>
  <action>
Create bin/verification-mode.cjs with these exports:

1. `MODES` — Object literal `{ DIAGNOSTIC: 'diagnostic', VALIDATION: 'validation' }`.

2. `createVerificationEnvelope(options)` — Accepts `{ verification_mode, modelPath, config, bugContext?, dumpTrace? }`. Returns the same object with `verification_mode` defaulting to `'validation'` if not provided. Validates mode is one of MODES values; throws if invalid non-null value supplied.

3. `interpretGateResult(hasViolation, verification_mode)` — Implements the four-cell truth table:
   - diagnostic + violation = `'REPRODUCED'` (model captures bug — success)
   - diagnostic + no violation = `'INCOMPLETE'` (model missing the bug — failure)
   - validation + violation = `'FAILED'` (invariant broken)
   - validation + no violation = `'PASSED'` (consequence model valid)
   Throws on unknown mode.

Update bin/run-tlc.cjs: In the function/CLI section that builds the TLC command args, accept an optional `--verification-mode=diagnostic|validation` CLI arg. Pass it through to the result object written by `writeCheckResult()` as `metadata.verification_mode`. Do NOT change TLC invocation logic itself — this is purely metadata on the result envelope. Default to `'validation'` when not specified, preserving backward compatibility.

Update bin/run-alloy.cjs: Same pattern — accept optional `--verification-mode` CLI arg, pass through to result metadata.

Update bin/refinement-loop.cjs: The existing `verifyBugReproduction` function already uses inverted semantics. Add `verification_mode: 'diagnostic'` to the metadata of its checker invocations (the `execFileSync` calls to run-tlc/run-alloy). This makes the implicit inversion explicit without changing any logic.

Create bin/verification-mode.test.cjs with 8+ tests:
- createVerificationEnvelope defaults to 'validation'
- createVerificationEnvelope passes through 'diagnostic'
- createVerificationEnvelope throws on invalid mode
- interpretGateResult diagnostic + violation = REPRODUCED
- interpretGateResult diagnostic + no violation = INCOMPLETE
- interpretGateResult validation + violation = FAILED
- interpretGateResult validation + no violation = PASSED
- interpretGateResult throws on unknown mode

IMPORTANT: Do not modify any existing checker logic (pass/fail conditions, TLC args, Alloy args). Only add the metadata envelope. Use existing nForma test patterns (require('node:test'), assert).
  </action>
  <verify>
Run `node --test bin/verification-mode.test.cjs` — all 8+ tests pass.
Run `npm test` — existing test suite still passes (backward compat).
Grep: `grep 'verification_mode' bin/run-tlc.cjs` returns matches.
Grep: `grep 'verification_mode' bin/run-alloy.cjs` returns matches.
Grep: `grep 'verification_mode' bin/refinement-loop.cjs` returns matches.
  </verify>
  <done>
verification-mode.cjs exports createVerificationEnvelope + interpretGateResult + MODES. All checker runners accept --verification-mode and pass it through to result metadata. Default is 'validation'. All existing tests still pass. 8+ new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create config-update module for iteration limits and session directories</name>
  <files>
    bin/config-update.cjs
    bin/config-update.test.cjs
    .planning/config.json
  </files>
  <action>
Create bin/config-update.cjs with these exports:

1. `getMaxIterations(projectRoot?)` — Reads `.planning/config.json`, returns `config.max_iterations` (number). Falls back to default 3 if key missing. Accepts optional projectRoot for testability.

2. `updateMaxIterations(value, projectRoot?)` — Reads `.planning/config.json`, sets `max_iterations` to the integer value, writes back atomically (readFileSync -> update -> writeFileSync with 2-space indent + trailing newline). Validates value is a positive integer; throws on invalid input. Accepts optional projectRoot for testability.

3. `createSessionDirectory(projectRoot?)` — Creates `.planning/formal/.tmp/session-<hex16>/` using `crypto.randomBytes(8).toString('hex')`. Returns absolute path. Creates `.tmp/` parent if needed via `{ recursive: true }`.

4. `parseMaxIterationsArg(argv?)` — Parses `--max-iterations=N` from process.argv (or provided array). Returns parsed integer or null if not found. Does NOT automatically persist — caller decides.

Update .planning/config.json: Add `"max_iterations": 3` key to the existing config object. Place it after the existing keys. Preserve all existing config values exactly.

Create bin/config-update.test.cjs with 6+ tests:
- getMaxIterations returns 3 from default config
- getMaxIterations returns fallback 3 when key missing
- updateMaxIterations persists value to config
- updateMaxIterations throws on non-positive integer
- createSessionDirectory creates directory with session prefix
- parseMaxIterationsArg parses --max-iterations=5 correctly
- parseMaxIterationsArg returns null when flag absent

Use a temp directory for tests that write to config.json (copy config to /tmp, operate there). Use existing nForma test patterns (require('node:test'), assert, fs).
  </action>
  <verify>
Run `node --test bin/config-update.test.cjs` — all 6+ tests pass.
Run `npm test` — existing test suite still passes.
Check config: `node -p "require('./.planning/config.json').max_iterations"` outputs `3`.
Verify .planning/formal/.tmp/ directory pattern: `node -e "const {createSessionDirectory} = require('./bin/config-update.cjs'); const d = createSessionDirectory(); console.log(d); require('fs').rmdirSync(d);"` creates and removes a session directory.
  </verify>
  <done>
config-update.cjs exports getMaxIterations, updateMaxIterations, createSessionDirectory, parseMaxIterationsArg. .planning/config.json contains max_iterations: 3. All 6+ new tests pass. Existing test suite unaffected.
  </done>
</task>

</tasks>

<verification>
- `node --test bin/verification-mode.test.cjs` passes (8+ tests)
- `node --test bin/config-update.test.cjs` passes (6+ tests)
- `npm test` passes (full suite, backward compat)
- `grep 'verification_mode' bin/run-tlc.cjs bin/run-alloy.cjs bin/refinement-loop.cjs` shows mode tagging in all three
- `node -p "require('./.planning/config.json').max_iterations"` outputs 3
</verification>

<success_criteria>
Every model checker invocation can carry a verification_mode tag. Gate interpretation logic correctly maps (mode, violation) to semantic outcome. --max-iterations default of 3 is persisted in config.json. Session directory creation works for downstream Cycle 2 artifact isolation.
</success_criteria>

<output>
After completion, create `.planning/phases/v0.39-01-foundation-and-infrastructure/v0.39-01-01-SUMMARY.md`
</output>
