# Plan v0.27-02-03 Summary: Observe Command, Debt Writer, and Renderer

**Completed:** 2026-03-04
**Duration:** ~5 minutes
**Tests:** 21 passing (8 debt writer + 13 renderer)

## Artifacts Created

| File | Lines | Purpose |
|------|-------|---------|
| `commands/qgsd/observe.md` | ~165 | Observe command specification (replaces triage) |
| `bin/observe-debt-writer.cjs` | ~95 | Debt ledger upsert after observe |
| `bin/observe-debt-writer.test.cjs` | ~155 | 8 integration tests for debt persistence |
| `bin/observe-render.cjs` | ~150 | Dual-table renderer (Issues + Drifts) |
| `bin/observe-render.test.cjs` | ~120 | 13 unit tests for rendering |

## Requirements Completed

- **OBS-01:** `/qgsd:observe` fetches from all configured sources in parallel, renders severity-sorted output
- **OBS-06:** Two output tables: Issues (discrete events) and Drifts (quantitative divergences)
- **DEBT-06:** `/qgsd:observe` writes new/updated debt entries after each run

## Key Decisions

- Debt writer uses v0.27-01 `fingerprintIssue` and `fingerprintDrift` functions (never inline computation)
- Upsert by fingerprint: same issues across runs update `occurrences` and `last_seen`, no duplicates
- Renderer returns string (doesn't console.log) for flexibility
- Observe command spec includes Step 4b (MCP bridge) for Sentry/sentry-feedback handlers
- `ack N` command in Step 7 lets users acknowledge debt entries directly

## Verification

```
node --test bin/observe-debt-writer.test.cjs bin/observe-render.test.cjs
# 21 tests, 0 failures

# Full phase suite
node --test bin/observe-config.test.cjs bin/observe-registry.test.cjs bin/observe-handlers.test.cjs bin/observe-debt-writer.test.cjs bin/observe-render.test.cjs
# 103 tests, 0 failures

# v0.27-01 regression check
node --test bin/validate-debt-entry.test.cjs bin/debt-state-machine.test.cjs bin/fingerprint-issue.test.cjs bin/fingerprint-drift.test.cjs bin/debt-ledger.test.cjs
# 127 tests, 0 failures
```
