---
phase: v0.12-09-verification-quick-fixes
verified: 2026-02-25T19:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---

# Phase v0.12-09: Verification Quick Fixes — Verification Report

**Phase Goal:** All 5 isolated verification infrastructure bugs are fixed: MCconvergence runs with -workers 1 (liveness-safe), JAR-not-found tests in run-tlc.test.cjs and run-alloy.test.cjs have conditional-skip guards, stale "NoSpuriousApproval" error message in run-alloy.cjs is corrected, rates.const variable names align with quorum.pm (or aggregation is documented), and the Petri net deadlock condition uses a runtime-parameterizable threshold.
**Verified:** 2026-02-25T19:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                              | Status     | Evidence                                                                                                       |
| --- | ---------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------- |
| 1   | MCconvergence branch uses `-workers 1` (not `'auto'`)                             | VERIFIED   | `bin/run-oscillation-tlc.cjs` line 102: `const workers = '1';` — unconditional, no ternary                    |
| 2   | run-tlc.test.cjs JAR-not-found test skips when tla2tools.jar is present           | VERIFIED   | Lines 33-34: `const jarPath = ...tla2tools.jar; if (fs.existsSync(jarPath)) { return; }`                      |
| 3   | run-alloy.test.cjs JAR-not-found test skips when Alloy JAR is present             | VERIFIED   | Lines 34-35: `const jarPath = ...org.alloytools.alloy.dist.jar; if (fs.existsSync(jarPath)) { return; }`      |
| 4   | run-alloy.cjs error message does NOT contain "NoSpuriousApproval"                 | VERIFIED   | Lines 113-117: names `ThresholdPasses / BelowThresholdFails / ZeroApprovalsFail` — no stale name anywhere     |
| 5   | VERIFICATION_TOOLS.md documents rates.const -> quorum.pm aggregation step         | VERIFIED   | Lines 94-108: "Note on variable names" section with per-slot vs aggregate explanation and node one-liner       |
| 6   | generate-petri-net.cjs accepts `--min-quorum=N` CLI flag                          | VERIFIED   | Lines 23-27: `minQuorumArg` parsed; `effectiveMinQuorum` overrides `MIN_QUORUM_SIZE`; deadlock check wired    |
| 7   | npm test passes with 0 failures                                                    | VERIFIED   | `npm test` output: 315 tests, 315 pass, 0 fail, 0 skipped                                                     |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact                        | Expected                                             | Status     | Details                                                                        |
| ------------------------------- | ---------------------------------------------------- | ---------- | ------------------------------------------------------------------------------ |
| `bin/run-oscillation-tlc.cjs`   | `const workers = '1'` unconditional, no ternary      | VERIFIED   | Line 102 confirmed; ternary removed; comment updated lines 99-101              |
| `bin/run-tlc.test.cjs`          | `fs` import + JAR-existence skip guard               | VERIFIED   | Line 12: `const fs = require('fs');`; line 34: `fs.existsSync` skip guard     |
| `bin/run-alloy.test.cjs`        | JAR-existence skip guard (was already had `fs`)      | VERIFIED   | Line 12: `const fs = require('fs');`; line 35: `fs.existsSync` skip guard     |
| `bin/run-alloy.cjs`             | Error message names correct assertions, not stale    | VERIFIED   | Lines 113-117: ThresholdPasses / BelowThresholdFails / ZeroApprovalsFail      |
| `VERIFICATION_TOOLS.md`         | Alloy section uses correct assertion names           | VERIFIED   | Line 60: `# checks ThresholdPasses / BelowThresholdFails / ZeroApprovalsFail` |
| `VERIFICATION_TOOLS.md`         | PRISM section: aggregation note with example         | VERIFIED   | Lines 94-108: full "Note on variable names" block with arithmetic + invocation |
| `bin/generate-petri-net.cjs`    | `--min-quorum=N` parsed; deadlock check wired to it  | VERIFIED   | Lines 23-27, 32, 35, 83: `effectiveMinQuorum` used throughout                 |

---

### Key Link Verification

| From                           | To                                      | Via                              | Status   | Details                                                                            |
| ------------------------------ | --------------------------------------- | -------------------------------- | -------- | ---------------------------------------------------------------------------------- |
| `--min-quorum=N` CLI arg       | deadlock `if` check                     | `effectiveMinQuorum > QUORUM_SLOTS.length` | WIRED | Line 32: condition uses `effectiveMinQuorum`; manually tested: `--min-quorum=6` fires warning |
| `--min-quorum=N` CLI arg       | `buildDot` call                         | `effectiveMinQuorum` parameter   | WIRED    | Line 83: `buildDot(QUORUM_SLOTS, effectiveMinQuorum)` — DOT label reflects override |
| JAR skip guard (tlc test)      | JAR existence check                     | `fs.existsSync(jarPath)`         | WIRED    | Line 34: returns early when JAR present; no assertion fires                        |
| JAR skip guard (alloy test)    | JAR existence check                     | `fs.existsSync(jarPath)`         | WIRED    | Line 35: returns early when JAR present; no assertion fires                        |

---

### Requirements Coverage

| Requirement | Source Plan       | Description                                                           | Status    | Evidence                                                              |
| ----------- | ----------------- | --------------------------------------------------------------------- | --------- | --------------------------------------------------------------------- |
| GAP-5       | v0.12-09-01-PLAN  | MCconvergence liveness-safe: -workers 1 enforcement                   | SATISFIED | `const workers = '1'` unconditional at line 102 of run-oscillation-tlc.cjs |
| TLA-04      | v0.12-09-02-PLAN  | JAR-not-found test in run-tlc.test.cjs: conditional-skip when JAR present | SATISFIED | `fs.existsSync(jarPath)` guard at line 34 of run-tlc.test.cjs        |
| ALY-02      | v0.12-09-03-PLAN  | Stale "NoSpuriousApproval" corrected; Alloy JAR skip guard added      | SATISFIED | run-alloy.cjs lines 113-117; VERIFICATION_TOOLS.md line 60; run-alloy.test.cjs line 35 |
| PRM-02      | v0.12-09-04-PLAN  | Manual aggregation step documented in VERIFICATION_TOOLS.md           | SATISFIED | VERIFICATION_TOOLS.md lines 94-108: full explanation + node one-liner + invocation example |
| PET-03      | v0.12-09-05-PLAN  | Petri net deadlock check uses runtime-parameterizable threshold       | SATISFIED | `--min-quorum=N` parsed at lines 23-27; deadlock check wired at line 32; buildDot call at line 83 |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |

No anti-patterns detected in modified files.

---

### Human Verification Required

None. All checks are fully automatable and were verified programmatically or by direct execution.

---

### Gaps Summary

No gaps. All 7 must-haves verified:

1. `bin/run-oscillation-tlc.cjs` — workers unconditionally `'1'`, comment accurate, no ternary
2. `bin/run-tlc.test.cjs` — `fs` imported, JAR-existence skip guard at line 34
3. `bin/run-alloy.test.cjs` — JAR-existence skip guard at line 35
4. `bin/run-alloy.cjs` — no stale "NoSpuriousApproval"; correct assertion names at lines 113-117
5. `VERIFICATION_TOOLS.md` (Alloy section) — correct assertion names at line 60
6. `VERIFICATION_TOOLS.md` (PRISM section) — aggregation documentation at lines 94-108
7. `bin/generate-petri-net.cjs` — `--min-quorum=N` fully wired; deadlock warning fires at runtime; `buildDot` receives `effectiveMinQuorum`; default behavior unchanged

`npm test`: 315 pass, 0 fail, 0 skipped — confirmed clean.

---

_Verified: 2026-02-25T19:00:00Z_
_Verifier: Claude (qgsd-verifier)_
