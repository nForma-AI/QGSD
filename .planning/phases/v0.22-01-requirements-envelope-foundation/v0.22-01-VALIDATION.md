---
phase: v0.22-01
slug: requirements-envelope-foundation
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-01
---

# Phase v0.22-01 -- Validation Strategy

> Template created by `/qgsd:plan-phase v0.22-01` (step 5.5) after research.
> Populated by `/qgsd:plan-phase v0.22-01` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.22-01`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (`node:test`) |
| **Config file** | none (built-in, no config file needed) |
| **Quick run command** | `node --test bin/aggregate-requirements.test.cjs bin/validate-requirements-haiku.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~15 seconds |
| **CI pipeline** | npm test script in package.json |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/aggregate-requirements.test.cjs bin/validate-requirements-haiku.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.22-01-01-01 | 01 | 1 | ENV-01 | unit | `node -e "const m = require('./bin/aggregate-requirements.cjs'); console.log(Object.keys(m))"` | TBD | pending |
| v0.22-01-01-02 | 01 | 1 | ENV-01 | unit | `node --test bin/aggregate-requirements.test.cjs` | TBD | pending |
| v0.22-01-02-01 | 02 | 1 | ENV-02 | unit | `node -e "const m = require('./bin/validate-requirements-haiku.cjs'); console.log(Object.keys(m))"` | TBD | pending |
| v0.22-01-02-02 | 02 | 1 | ENV-02 | unit | `node --test bin/validate-requirements-haiku.test.cjs` | TBD | pending |
| v0.22-01-03-01 | 03 | 2 | ENV-01, ENV-02 | integration | `node bin/aggregate-requirements.cjs && npm test` | TBD | pending |
| v0.22-01-03-02 | 03 | 2 | ENV-02 | manual | Haiku validation findings presented to user | N/A | pending |
| v0.22-01-03-03 | 03 | 2 | ENV-01, ENV-02 | integration | `node -e "const d=JSON.parse(require('fs').readFileSync('formal/requirements.json','utf8')); console.log('frozen_at:', d.frozen_at); if(!d.frozen_at) process.exit(1)"` | TBD | pending |

*Status values: pending / green / red / flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements -- no Wave 0 test tasks needed.

The project already has `node:test` built-in and `@anthropic-ai/sdk` installed. No new framework or dependency installation required. Test files (`bin/aggregate-requirements.test.cjs` and `bin/validate-requirements-haiku.test.cjs`) are created as part of Wave 1 TDD plans.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Haiku validation finding review and approval | ENV-02 | User must review semantic findings and decide accept/dismiss before freeze | Run `node bin/validate-requirements-haiku.cjs`, review findings, type "approved" |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-03-01

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.22-01`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 4 | `node --test bin/aggregate-requirements.test.cjs bin/validate-requirements-haiku.test.cjs` | — | — | pending |
| 2 | 3 | `npm test` | — | — | pending |

**Phase validation complete:** pending
