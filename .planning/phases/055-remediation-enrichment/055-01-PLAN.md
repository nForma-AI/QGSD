---
phase: 055-remediation-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflows/close-formal-gaps.md
  - commands/nf/close-formal-gaps.md
  - commands/nf/solve-remediate.md
autonomous: true
requirements:
  - CREM-01
formal_artifacts: none
must_haves:
  truths:
    - "When R->F remediation dispatches /nf:close-formal-gaps, the --seed-files argument includes files discovered via getImplementation and getCallers for the uncovered requirement IDs, so the generated formal spec can reference actual function signatures and call relationships from the codebase"
    - "When coderlm is unavailable during R->F dispatch, /nf:close-formal-gaps is dispatched without --seed-files and the solve loop continues with zero errors and no degradation"
    - "When seed-file discovery returns results, each close-formal-gaps dispatch is scoped to a single requirement so that seed files are not shared across unrelated requirements"
  artifacts:
    - path: "core/workflows/close-formal-gaps.md"
      provides: "--seed-files flag parsing and injection into Step 5 model generation prompt"
      contains: "seed-files"
    - path: "commands/nf/close-formal-gaps.md"
      provides: "Updated argument-hint and pass-through for --seed-files"
      contains: "seed-files"
    - path: "commands/nf/solve-remediate.md"
      provides: "Section 3a updated to query coderlm getImplementation/getCallers and pass --seed-files, dispatching one requirement at a time when seeds are present"
      contains: "seed-files"
  key_links:
    - from: "commands/nf/solve-remediate.md"
      to: "core/workflows/close-formal-gaps.md"
      via: "--seed-files argument passed from 3a dispatch to close-formal-gaps invocation"
      pattern: "seed-files"
    - from: "core/workflows/close-formal-gaps.md"
      to: "Step 5 model generation"
      via: "SEED_FILES content injected into spec generation prompt"
      pattern: "SEED_FILES"
---

<objective>
Add --seed-files support to the close-formal-gaps workflow and wire coderlm implementation/callers discovery into the R->F remediation dispatch in solve-remediate.

Purpose: When R->F gaps are remediated, formal specs are generated with knowledge of actual function signatures and call relationships rather than generic boilerplate. The seed files give the spec generator concrete code context, producing specs that reference real state variables and transitions.

Output: close-formal-gaps workflow supports --seed-files flag (Step 1 parses it, Step 5 injects content into generation prompt); solve-remediate section 3a queries coderlm for each uncovered requirement's implementation file and callers, then passes those files as --seed-files. When seed files are available, dispatch is one requirement at a time (not batched) to avoid cross-contaminating specs.
</objective>

<execution_context>
@./.claude/nf/workflows/execute-plan.md
@./.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core/workflows/close-formal-gaps.md
@commands/nf/close-formal-gaps.md
@commands/nf/solve-remediate.md
@bin/coderlm-adapter.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --seed-files flag to close-formal-gaps workflow and command</name>
  <files>core/workflows/close-formal-gaps.md</files>
  <files>commands/nf/close-formal-gaps.md</files>
  <action>
In core/workflows/close-formal-gaps.md:

1. In Step 1 (detect_gaps), add `--seed-files` flag parsing after the existing `--verbose` block:

```
If `--seed-files` is provided (comma-separated file paths):
- Parse the value: `SEED_FILES=$(echo "$RAW_SEED_FILES" | tr ',' '\n' | xargs -I{} echo "{}")`
- For each path that exists, read its content (up to 200 lines via `head -200 {file}`)
- Store as `$SEED_CONTEXT` — a multi-file block in the format:
  ```
  // Seed file: {relative_path}
  {file_content}
  ```
- Log: `Seed files loaded: {N} files ({list of paths})`
- If no files exist or flag is absent, set `$SEED_CONTEXT=""` (fail-open: missing seed files do not block generation)
```

2. In Step 5 (generate_model), after the existing Bug Context Injection block (or at the same injection point for all formalisms), add a Seed Files Injection block:

```
### Seed Files Injection (CREM-01)

If `$SEED_CONTEXT` is non-empty (i.e., `--seed-files` was provided in Step 1 with at least one existing file), append the following block to the spec generation prompt for ALL formalisms (TLA+, Alloy, PRISM, Petri), AFTER the requirement text and module context but BEFORE the formalism-specific instructions:

<seed_context>
The following source files are the implementation of the requirement(s) being modeled.
Use actual function signatures, state variable names, and call patterns from this code
as the basis for the formal model — do NOT invent generic names.

{$SEED_CONTEXT}
</seed_context>

Use the seed code to:
- Name TLA+ VARIABLES and CONSTANTS after the actual variables and parameters in the code
- Model only the state transitions that exist in the code (not hypothetical ones)
- Reference real callers in liveness properties where applicable
```

In commands/nf/close-formal-gaps.md, add `--seed-files` to the argument-hint and process section:
- argument-hint: add `[--seed-files=src/foo.cjs,src/bar.cjs]`
- process: add `  --seed-files=path1,path2    Seed spec generation with actual source file content (CREM-01)`
  </action>
  <verify>
grep -n "seed-files" /Users/jonathanborduas/code/QGSD-worktrees/feature-issue-58-integrate-coderlm-adapter/core/workflows/close-formal-gaps.md | wc -l
# Must return >= 3 (flag parsing in Step 1, injection block in Step 5, at least one usage)
grep -n "seed-files" /Users/jonathanborduas/code/QGSD-worktrees/feature-issue-58-integrate-coderlm-adapter/commands/nf/close-formal-gaps.md | wc -l
# Must return >= 2 (argument-hint and process)
grep "SEED_CONTEXT" /Users/jonathanborduas/code/QGSD-worktrees/feature-issue-58-integrate-coderlm-adapter/core/workflows/close-formal-gaps.md
# Must show the injection conditional
  </verify>
  <done>
close-formal-gaps workflow parses --seed-files in Step 1 (fail-open: missing files = empty SEED_CONTEXT), injects SEED_CONTEXT into Step 5 generation prompt for all formalisms when non-empty. commands/nf/close-formal-gaps.md argument-hint and process block include --seed-files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update solve-remediate section 3a to query coderlm and pass --seed-files</name>
  <files>commands/nf/solve-remediate.md</files>
  <action>
In section 3a (R->F Gaps) of commands/nf/solve-remediate.md, replace the existing batch dispatch with a per-requirement loop that queries coderlm for seed files and dispatches one requirement at a time when seeds are found.

**Why per-requirement dispatch instead of batching with seeds:**
Passing all seed files to a batch dispatch (`--batch --ids=REQ-01,REQ-02,...`) cross-contaminates specs — REQ-01's callers appear in REQ-02's generation prompt, producing incorrect models. When seed-file discovery returns any results, each requirement is dispatched individually. When coderlm is unavailable and SEED_FILES_ARG is empty, the existing batch dispatch is preserved exactly (fail-open, CADP-02).

Replace the existing section 3a dispatch block with:

```
**Coderlm seed-file discovery and dispatch (CREM-01):**

For each uncovered requirement ID (after applying cascade budget truncation), run the following node snippet to collect seed files via coderlm's synchronous API:

```bash
node -e "
const _nfBin = (n) => { const p = require('path').join(require('os').homedir(), '.claude/nf-bin', n); return require('fs').existsSync(p) ? p : './bin/' + n; };
const { createAdapter } = require(_nfBin('coderlm-adapter.cjs'));
const adapter = createAdapter({ enabled: true });

// Health check — skip entirely if server not responding
const health = adapter.healthSync();
if (health.error) {
  process.stdout.write(JSON.stringify({ seed_files: [], skipped: true, reason: health.error }));
  process.exit(0);
}

const reqId = process.argv[1];
const seedSet = new Set();

// Parse the requirement text to extract a function name heuristic.
// getImplementation expects a symbol name (function/class), NOT a requirement ID string.
// Strategy: use the first camelCase or snake_case token from the requirement description
// that looks like a function name (length >= 4, starts with lowercase letter).
// Fall back to the requirement ID stripped of dashes if no such token is found.
function extractSymbolHint(id) {
  // e.g. 'CREM-01' -> 'crem01' (last-resort fallback)
  return id.replace(/-/g, '').toLowerCase();
}

// Attempt with symbol hint derived from requirement text (caller must pass as argv[2] if known)
const symbolHint = process.argv[2] || extractSymbolHint(reqId);

const impl = adapter.getImplementationSync(symbolHint);
if (impl && impl.file && !impl.error) {
  seedSet.add(impl.file);
  // getCallersSync(symbolHint, impl.file) — first arg is the function name symbol, second arg scopes the search to the implementation file
  const callers = adapter.getCallersSync(symbolHint, impl.file);
  if (callers && callers.callers && !callers.error) {
    for (const c of callers.callers.slice(0, 3)) {
      if (c.file) seedSet.add(c.file);
    }
  }
}

process.stdout.write(JSON.stringify({ seed_files: [...seedSet], skipped: false }));
" -- \"\$REQ_ID\" \"\$SYMBOL_HINT\" 2>/dev/null || echo '{"seed_files":[],"skipped":true,"reason":"node error"}'
```

For each requirement ID:
1. Set `REQ_ID` to the requirement ID (e.g., "REQ-01")
2. Set `SYMBOL_HINT` to the function name derived from the requirement text (if available from the traceability matrix or roadmap requirement text), otherwise leave empty to use the fallback.
3. Run the node snippet above. Parse the JSON output.
4. If `seed_files` is non-empty: set `SEED_FILES_ARG="--seed-files=$(IFS=,; echo "${seed_files[*]}")"` and dispatch individually:
   ```
   /nf:close-formal-gaps --ids=$REQ_ID $SEED_FILES_ARG
   ```
   Log: `"R->F [$REQ_ID]: coderlm seed discovery: ${#seed_files[@]} seed files — dispatching individually"`
5. If `seed_files` is empty (coderlm unavailable OR no implementation found):
   - Accumulate the ID into `$BATCH_IDS`
   - Log: `"R->F [$REQ_ID]: no seed files (skipped=${skipped}) — adding to batch"`

After the per-requirement loop, if `$BATCH_IDS` is non-empty, run the existing batch dispatch:
```
/nf:close-formal-gaps --batch --ids=$BATCH_IDS
```

This preserves the existing batch behavior for all requirements when coderlm is unavailable, while scoping seed-enriched dispatches to individual requirements to prevent cross-contamination.
```

Note: `$SEED_FILES_ARG` is empty string when coderlm is unavailable — requirements accumulate in `$BATCH_IDS` and the existing batch dispatch fires, preserving pre-integration behavior exactly (fail-open, CADP-02).
  </action>
  <verify>
grep -n "seed-files\|SEED_FILES\|CREM-01\|seed.fil\|BATCH_IDS\|individually\|cross-contam\|symbolHint\|SYMBOL_HINT" /Users/jonathanborduas/code/QGSD-worktrees/feature-issue-58-integrate-coderlm-adapter/commands/nf/solve-remediate.md | head -15
# Must show SEED_FILES_ARG, BATCH_IDS, individual dispatch, and batch fallback
grep -c "SEED_FILES_ARG" /Users/jonathanborduas/code/QGSD-worktrees/feature-issue-58-integrate-coderlm-adapter/commands/nf/solve-remediate.md
# Must return >= 2 (definition + individual dispatch use)
grep -c "BATCH_IDS" /Users/jonathanborduas/code/QGSD-worktrees/feature-issue-58-integrate-coderlm-adapter/commands/nf/solve-remediate.md
# Must return >= 2 (accumulation and batch dispatch)
grep "symbolHint\|SYMBOL_HINT\|extractSymbolHint" /Users/jonathanborduas/code/QGSD-worktrees/feature-issue-58-integrate-coderlm-adapter/commands/nf/solve-remediate.md
# Must show symbol hint derivation (not raw requirement ID as query)
  </verify>
  <done>
Section 3a of solve-remediate.md contains a fail-open coderlm seed-discovery block that: (a) derives a function-name symbol hint from requirement text rather than using the raw ID as query, (b) dispatches requirements with seed files individually (preventing cross-contamination), (c) accumulates seed-less requirements for existing batch dispatch. Existing behavior unchanged when coderlm is unavailable.
  </done>
</task>

</tasks>

<verification>
1. grep "seed-files" core/workflows/close-formal-gaps.md — must show flag parsing in Step 1 and injection block in Step 5
2. grep "SEED_FILES_ARG" commands/nf/solve-remediate.md — must appear in the discovery block and individual dispatch form
3. grep "BATCH_IDS" commands/nf/solve-remediate.md — must appear: accumulation in loop and batch fallback dispatch
4. grep "symbolHint\|extractSymbolHint" commands/nf/solve-remediate.md — must show symbol-name derivation (not raw REQ-ID used as getImplementation query)
5. grep "seed-files" commands/nf/close-formal-gaps.md — must appear in argument-hint and process
6. grep "fail-open\|skipped.*true\|seed_files.*\[\]" commands/nf/solve-remediate.md — must show fallback paths for unavailable coderlm
7. Verify no existing dispatch behavior removed (grep "close-formal-gaps --batch" still present in section 3a for batch fallback)
</verification>

<success_criteria>
- --seed-files flag is supported in close-formal-gaps workflow (parsed in Step 1, injected in Step 5 for all formalisms)
- solve-remediate section 3a derives a function-name symbol hint from requirement text before calling getImplementationSync (raw requirement ID is NOT used directly as the symbol query)
- When seeds are discovered, dispatch is per-requirement (not batched) to prevent cross-contamination of seed context across specs
- When coderlm is unavailable (health check fails or node error), requirements fall back to existing batch dispatch — behavior identical to pre-integration
- No changes to test suite (workflow changes are markdown instructions, not executable code)
</success_criteria>

<output>
After completion, create `.planning/phases/055-remediation-enrichment/055-01-SUMMARY.md`
</output>
