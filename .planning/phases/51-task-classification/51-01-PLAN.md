---
phase: 51-task-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflows/quick.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-04
formal_artifacts: none

must_haves:
  truths:
    - "Running /nf:quick on a bug report triggers a Haiku subagent at Step 2.7 that classifies the task as bug_fix, feature, or refactor"
    - "The classification result is persisted in scope-contract.json and available to downstream consumers (executor, verifier)"
    - "Classification includes a confidence score (0.0-1.0) to support downstream routing decisions"
    - "If Haiku subagent errors or times out, classification defaults to feature with confidence 0.0 (fail-open)"
  artifacts:
    - path: "core/workflows/quick.md"
      provides: "Task classification subagent spawn at Step 2.7 and scope-contract.json persistence"
      contains: "bug_fix"
  key_links:
    - from: "core/workflows/quick.md"
      to: "scope-contract.json"
      via: "Classification object written alongside existing approach data in Step 2.7"
      pattern: "classification.*bug_fix|feature|refactor"
---

<objective>
Add a task classification sub-step to Step 2.7 of quick.md that uses a Haiku subagent to classify each quick task as bug_fix, feature, or refactor. Persist the classification result in scope-contract.json so downstream consumers (executor, verifier, routing logic in Plan 51-02) can read it.

Purpose: Enable automatic routing of bug fixes through the /nf:debug pipeline (Plan 51-02) by establishing a classification signal at task intake time.
Output: Updated core/workflows/quick.md with classification logic in Step 2.7 and scope-contract.json schema extension.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core/workflows/quick.md
@commands/nf/solve-classify.md
@.planning/phases/51-task-classification/51-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add classification subagent to Step 2.7 of quick.md</name>
  <files>core/workflows/quick.md</files>
  <action>
In core/workflows/quick.md, extend Step 2.7 to add a classification sub-step AFTER the existing approach derivation (sub-step 1) and BEFORE the scope contract write (sub-step 2). The new sub-step classifies the task description using a Haiku subagent.

Insert a new sub-step 1.5 (between the existing sub-step 1 "Derive approach via Haiku subagent" and sub-step 2 "Write scope contract") with the following:

**1.5. Classify task type via Haiku subagent (ROUTE-01):**

Spawn a Haiku subagent to classify the task:

```
Task(
  subagent_type="general-purpose",
  model="haiku",
  description="Classify quick task type",
  prompt="
You are classifying a development task into exactly one category.

## Task Description
${DESCRIPTION}

## Categories
- bug_fix: The task fixes a bug, error, regression, crash, or broken behavior. Signal words: fix, bug, error, broken, regression, crash, failing, wrong, incorrect, undefined, null, NaN, timeout, hang.
- feature: The task adds new functionality or capability. Signal words: add, implement, create, new, feature, enable, support, introduce.
- refactor: The task reorganizes, renames, cleans up, or simplifies existing code without changing behavior. Signal words: refactor, rename, reorganize, clean, simplify, extract, move, consolidate.

## Your Task
Respond with ONLY a JSON object:
{
  \"type\": \"bug_fix\" | \"feature\" | \"refactor\",
  \"confidence\": 0.0-1.0
}

Guidelines:
- Choose the SINGLE best category. When mixed signals exist, choose the primary intent.
- Confidence: 0.9+ for clear signals, 0.7-0.9 for moderate signals, below 0.7 for ambiguous.
- Respond with ONLY the JSON object, no markdown fencing or extra text.
"
)
```

Parse the Haiku response as JSON. Store as `$CLASSIFICATION`.

**Fail-open:** If Haiku is unavailable, response is empty, or JSON parsing fails, use fallback:
```json
{
  "type": "feature",
  "confidence": 0.0
}
```

Log: `"Step 2.7: Task classified as ${CLASSIFICATION.type} (confidence: ${CLASSIFICATION.confidence})"`
If fallback was used, log: `"Step 2.7: Classification fell back to feature (Haiku unavailable or parse error)"`

Store `$CLASSIFICATION` for use in scope contract write (sub-step 2) and routing (Step 5.8 in Plan 51-02).
  </action>
  <verify>
Read core/workflows/quick.md and confirm:
1. Step 2.7 contains a Haiku subagent spawn with prompt mentioning bug_fix, feature, refactor
2. The classification JSON schema includes "type" and "confidence" fields
3. Fail-open fallback defaults to feature with confidence 0.0
4. $CLASSIFICATION is stored for downstream use
  </verify>
  <done>Step 2.7 of quick.md spawns a Haiku subagent that classifies task descriptions into bug_fix, feature, or refactor with a confidence score, failing open to feature on errors.</done>
</task>

<task type="auto">
  <name>Task 2: Persist classification in scope-contract.json</name>
  <files>core/workflows/quick.md</files>
  <action>
In the same Step 2.7 of core/workflows/quick.md, modify sub-step 2 ("Write scope contract to task directory") to include the classification result in scope-contract.json.

Update the scope-contract.json schema to add a `classification` field alongside the existing fields. The updated JSON structure should be:

```json
{
  "${branch_name}": {
    "task_id": ${next_num},
    "task_description": "${DESCRIPTION}",
    "approach": "${APPROACH_BLOCK.approach}",
    "out_of_scope": [${APPROACH_BLOCK.out_of_scope items}],
    "classification": {
      "type": "${CLASSIFICATION.type}",
      "confidence": ${CLASSIFICATION.confidence},
      "routed_through_debug": false
    },
    "branches_affected": ["${branch_name}"],
    "created_at": "${timestamp}",
    "planner_model": "${planner_model}",
    "created_by": "quick-orchestrator"
  }
}
```

The `routed_through_debug` field defaults to `false` here and will be set to `true` by the routing logic in Plan 51-02 when a bug_fix task is actually dispatched through /nf:debug.

Also update the log line to include classification:
`"Step 2.7: Scope contract written to ${task_dir}/scope-contract.json (key: ${branch_name}, type: ${CLASSIFICATION.type})"`
  </action>
  <verify>
Read core/workflows/quick.md Step 2.7 sub-step 2 and confirm:
1. scope-contract.json template includes a `classification` object with `type`, `confidence`, and `routed_through_debug` fields
2. The log line includes the classification type
3. The classification data comes from the $CLASSIFICATION variable set in Task 1
  </verify>
  <done>scope-contract.json includes classification.type (bug_fix|feature|refactor), classification.confidence (0.0-1.0), and classification.routed_through_debug (boolean), making classification available to all downstream consumers.</done>
</task>

</tasks>

<verification>
- [ ] Step 2.7 has a Haiku subagent spawn for task classification
- [ ] Classification output is bug_fix | feature | refactor with confidence score
- [ ] Fail-open defaults to feature with confidence 0.0
- [ ] scope-contract.json schema includes classification object
- [ ] classification.routed_through_debug field exists (default false)
- [ ] Existing Step 2.7 approach derivation still works unchanged
</verification>

<success_criteria>
- Quick task descriptions are automatically classified into one of three categories
- Classification is persisted in scope-contract.json for downstream consumers
- No user interaction required for classification (automatic, fail-open)
- Existing quick.md behavior unchanged for feature and refactor tasks
</success_criteria>

<output>
After completion, create `.planning/phases/51-task-classification/51-01-SUMMARY.md`
</output>
