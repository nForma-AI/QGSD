---
phase: v0.37-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/formal-proximity.cjs
  - bin/nf-solve.cjs
  - test/annotation-back-linking.test.cjs
autonomous: true
formal_artifacts: none
requirements:
  - CLINK-02
  - CLINK-01

must_haves:
  truths:
    - "`@requirement` annotations in source files (bin/*.cjs) create direct edges in the proximity graph builder output"
    - "C->R scanner consulting proximity-index.json suppresses flagging for code-requirement pairs with score >= 0.6"
  artifacts:
    - path: "bin/formal-proximity.cjs"
      provides: "Step 12c: Parse @requirement annotations from bin/*.cjs and hooks/*.cjs source files, creating declares/declared_in edges"
      contains: "code_file::"
    - path: "bin/nf-solve.cjs"
      provides: "sweepCtoR suppression logic using proximity edge scores >= 0.6"
      contains: "SUPPRESS_THRESHOLD"
    - path: "test/annotation-back-linking.test.cjs"
      provides: "Tests for CLINK-01 and CLINK-02"
      min_lines: 40
  key_links:
    - from: "bin/formal-proximity.cjs"
      to: "proximity-index.json"
      via: "declares edges from code_file nodes to requirement nodes"
      pattern: "declares.*source-annotation"
    - from: "bin/nf-solve.cjs"
      to: "bin/formal-proximity.cjs"
      via: "sweepCtoR reads proximity-index.json edges built by proximity builder"
      pattern: "SUPPRESS_THRESHOLD|>= 0\\.6"
  consumers:
    - artifact: "bin/formal-proximity.cjs (new Step 12c)"
      consumed_by: "bin/nf-solve.cjs sweepCtoR"
      integration: "sweepCtoR reads proximity-index.json which now includes source annotation edges"
      verify_pattern: "SUPPRESS_THRESHOLD"
---

<objective>
Add @requirement annotation parsing from JavaScript source files to the proximity graph builder, and modify the C->R scanner to suppress orphan flags for code-requirement pairs with proximity score >= 0.6.

Purpose: Eliminate false-positive orphan flags in the C->R scanner by leveraging direct annotation declarations and proximity graph edges.
Output: Updated formal-proximity.cjs with source file annotation parsing, updated nf-solve.cjs sweepCtoR with suppression logic, tests.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v0.37-01-annotation-back-linking/v0.37-01-RESEARCH.md
@bin/formal-proximity.cjs
@bin/nf-solve.cjs
@bin/extract-annotations.cjs
@bin/formal-query.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source file @requirement annotation parsing to proximity graph builder (CLINK-02)</name>
  <files>bin/formal-proximity.cjs, test/annotation-back-linking.test.cjs</files>
  <action>
Add a new Step 12c in formal-proximity.cjs (after Step 12b which handles formal model annotations) that parses `@requirement` annotations from JavaScript source files in bin/ and hooks/.

Implementation details:
1. Define SOURCE_CODE_DIRS array: `[{ dir: path.join(ROOT, 'bin'), ext: '.cjs' }, { dir: path.join(ROOT, 'bin'), ext: '.mjs' }, { dir: path.join(ROOT, 'hooks'), ext: '.js' }]`
2. For each file (skip test files — those ending in `.test.cjs`), read first 30 lines
3. Parse TWO annotation patterns:
   - `// @requirement REQ-ID` (single annotation per line)
   - `// Requirements: REQ-01, REQ-02` (comma-separated header)
   Use regex: `/@requirement\s+([A-Z][A-Z0-9_-]+)/g` for the first pattern, `/\/\/\s*Requirements:\s*(.+)/` for the second
4. For each found requirement ID, create a `code_file::bin/filename.cjs` -> `requirement::REQ-ID` edge with `declares` relation and source `'source-annotation'`
5. The Step 13 REVERSE PASS (already existing) will automatically create the `declared_in` reverse edges — no manual reverse needed
6. Register the source in the `sources` object similar to Step 12b
7. Log count to stderr: `[proximity] Extracted N code-requirement annotations from M source files`

Do NOT parse beyond the first 30 lines — annotations belong in file headers only.
Do NOT modify the existing Step 12b (formal model annotations) — this is a new step.

Create test file `test/annotation-back-linking.test.cjs` with tests:
- Test that parseSourceAnnotations extracts IDs from `// @requirement FOO-01` pattern
- Test that parseSourceAnnotations extracts IDs from `// Requirements: FOO-01, FOO-02` pattern
- Test that the proximity builder creates `declares` edges for source file annotations
- Test that REVERSE_RELS produces `declared_in` reverse edges automatically

Export the parsing helper function from formal-proximity.cjs for testability (add to module.exports).
  </action>
  <verify>
Run `node bin/formal-proximity.cjs 2>&1 | grep "code-requirement"` and confirm annotation count > 0.
Run `node test/annotation-back-linking.test.cjs 2>&1 | grep -E "pass|fail"` and confirm all tests pass.
Verify proximity-index.json contains `code_file::` nodes with `declares` edges by: `node -e "const pi=JSON.parse(require('fs').readFileSync('.planning/formal/proximity-index.json','utf8')); const cf=Object.keys(pi.nodes).filter(k=>k.startsWith('code_file::')); const withDeclares=cf.filter(k=>pi.nodes[k].edges.some(e=>e.rel==='declares')); console.log('code_files with declares:', withDeclares.length)"`
  </verify>
  <done>
formal-proximity.cjs parses @requirement annotations from bin/*.cjs and hooks/*.js source files and creates declares/declared_in edges in the proximity graph. At least 1 code_file node has a declares edge after running the builder (extract-annotations.cjs already has `// Requirements: ANNOT-04` in its header).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add proximity-based suppression to C->R scanner (CLINK-01)</name>
  <files>bin/nf-solve.cjs, test/annotation-back-linking.test.cjs</files>
  <action>
Modify sweepCtoR() in bin/nf-solve.cjs (around line 1955 where proximity enrichment currently happens) to add suppression logic BEFORE the enrichment step.

Implementation details:
1. After the `untraced` array is populated (line 1950) and BEFORE the existing proximity enrichment block (line 1956), add a suppression pass:
2. Load proximity-index.json and SEMANTIC_WEIGHTS from formal-proximity.cjs
3. Define `const SUPPRESS_THRESHOLD = 0.6;`
4. For each item in `untraced`, look up `code_file::` + item.file in pi.nodes
5. Check if any edge from that node targets a `requirement::` node AND the edge's relation type has a SEMANTIC_WEIGHTS value >= 0.6
6. If yes: remove from untraced, increment traced count, add `item.suppressed_by` field with the requirement ID and score for diagnostics
7. After suppression, the existing proximity enrichment continues on remaining untraced items
8. Wrap in try/catch with fail-open semantics (if proximity-index.json missing or unreadable, skip suppression entirely)

Key: Use the SEMANTIC_WEIGHTS hash from formal-proximity.cjs for score lookup — do NOT hardcode edge weights. Import via: `const { SEMANTIC_WEIGHTS } = require('./formal-proximity.cjs');`

The `declares` edge type has weight 0.7 in SEMANTIC_WEIGHTS, which is >= 0.6, so source files with @requirement annotations will be suppressed.

Also handle the case where `@requirement` pattern is found in sweepCtoR's own header-comment fallback check (line 1937-1946). The existing regex `Requirements:` pattern already handles this, but add a second pattern check for `// @requirement REQ-ID` inline annotations:
```javascript
const annMatch = head.match(/@requirement\s+([A-Z][A-Z0-9_-]+)/);
if (annMatch && reqIdSet.has(annMatch[1])) { headerTraced = true; }
```

Add tests to test/annotation-back-linking.test.cjs:
- Test that a file with a `declares` edge (weight 0.7) to a requirement is suppressed from untraced
- Test that a file with only a `describes` edge (weight 0.2) is NOT suppressed
- Test fail-open: missing proximity-index.json does not crash sweepCtoR
  </action>
  <verify>
Run `node bin/formal-proximity.cjs && node -e "const s=require('./bin/nf-solve.cjs'); /* sweepCtoR is internal */"` — no errors.
Run `node test/annotation-back-linking.test.cjs 2>&1 | grep -E "pass|fail"` and confirm all tests pass.
Run full test suite: `npm test 2>&1 | tail -5` — no regressions.
  </verify>
  <done>
sweepCtoR suppresses orphan flags for code files whose proximity graph edges to requirements have SEMANTIC_WEIGHTS >= 0.6. Files with @requirement annotations (which create 0.7 declares edges via Task 1) are automatically suppressed. Files with only weak edges (< 0.6) remain flagged.
  </done>
</task>

</tasks>

<verification>
1. Run `node bin/formal-proximity.cjs` — proximity index rebuilt with source annotation edges
2. Run `node test/annotation-back-linking.test.cjs` — all CLINK tests pass
3. Run `npm test` — no regressions in existing test suite
4. Verify suppression by checking: a source file with `// Requirements: ANNOT-04` header (extract-annotations.cjs) should NOT appear in sweepCtoR untraced output after proximity rebuild
</verification>

<success_criteria>
- proximity-index.json contains code_file nodes with declares edges to requirement nodes (from source file @requirement annotations)
- sweepCtoR returns fewer untraced items when proximity-index.json has high-score edges
- All new tests pass, no regressions in existing suite
</success_criteria>

<output>
After completion, create `.planning/phases/v0.37-01-annotation-back-linking/v0.37-01-01-SUMMARY.md`
</output>
