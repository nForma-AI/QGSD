---
phase: v0.33-01-time-series-foundation
plan: 02
status: complete
started: 2026-03-10
completed: 2026-03-10
---

## Summary

Removed 164 duplicate entries from promotion-changelog.json and added a write-time dedup guard to prevent future duplicates.

## What Was Built

- **Write-time dedup guard**: Added `DEDUP_WINDOW_MS` constant and reverse-scan duplicate check in `appendChangelog()` in compute-per-model-gates.cjs
- **dedup-changelog.cjs**: One-time cleanup script with exported `dedup()` function for re-use and testing
- **dedup-changelog.test.cjs**: 10 unit tests covering within/outside window, different transitions, empty arrays, order preservation, boundary conditions, interleaved models

## Key Files

### Created
- `bin/dedup-changelog.cjs` — Standalone cleanup tool and dedup library
- `bin/dedup-changelog.test.cjs` — 10 unit tests

### Modified
- `bin/compute-per-model-gates.cjs` — Added DEDUP_WINDOW_MS constant and dedup guard in appendChangelog()
- `package.json` — Registered dedup-changelog.test.cjs in test:ci
- `.planning/formal/promotion-changelog.json` — Cleaned from 200 to 36 entries (164 duplicates removed)

## Decisions

- Used strict less-than for window comparison (entries exactly at 5-minute boundary are kept)
- Reverse scan in appendChangelog() for efficiency (most recent entries checked first, breaks early)
- dedup() exports for both CLI use and test imports

## Self-Check: PASSED

- `grep 'DEDUP_WINDOW_MS' bin/compute-per-model-gates.cjs` shows constant
- `node -e "const d = require('./bin/dedup-changelog.cjs'); console.log(typeof d.dedup)"` prints "function"
- Zero duplicates remain in promotion-changelog.json
- `node --test bin/dedup-changelog.test.cjs` — 10/10 pass
- `npm run test:ci` — 1221/1221 pass, 0 fail
