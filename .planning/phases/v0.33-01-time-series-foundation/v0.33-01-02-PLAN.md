---
phase: v0.33-01-time-series-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/compute-per-model-gates.cjs
  - bin/dedup-changelog.cjs
  - bin/dedup-changelog.test.cjs
  - package.json
autonomous: true
requirements:
  - STAB-03
formal_artifacts: none

must_haves:
  truths:
    - "The promotion-changelog.json contains no duplicate entries (same model, same transition within 5-minute window), and the write-time guard prevents future duplicates"
  artifacts:
    - path: "bin/compute-per-model-gates.cjs"
      provides: "Write-time dedup guard in appendChangelog()"
      contains: "DEDUP_WINDOW_MS"
    - path: "bin/dedup-changelog.cjs"
      provides: "One-time cleanup script for existing duplicates"
      exports: ["dedup"]
    - path: "bin/dedup-changelog.test.cjs"
      provides: "Unit tests for dedup logic and write-time guard"
      min_lines: 60
  key_links:
    - from: "bin/compute-per-model-gates.cjs"
      to: ".planning/formal/promotion-changelog.json"
      via: "appendChangelog() with dedup guard"
      pattern: "DEDUP_WINDOW_MS"
    - from: "bin/dedup-changelog.cjs"
      to: ".planning/formal/promotion-changelog.json"
      via: "reads, deduplicates, writes back"
      pattern: "promotion-changelog"
  consumers:
    - artifact: "bin/dedup-changelog.cjs"
      consumed_by: "User-invoked one-time cleanup"
      integration: "Standalone CLI tool: node bin/dedup-changelog.cjs"
      verify_pattern: "require.main === module"
    - artifact: "bin/dedup-changelog.test.cjs"
      consumed_by: "package.json test:ci script"
      integration: "Added to node --test list"
      verify_pattern: "dedup-changelog.test.cjs"
---

<objective>
Remove existing duplicate entries from promotion-changelog.json and add a write-time guard to prevent future duplicates.

Purpose: The promotion changelog has 200 entries with 1088 duplicate pairs (same model, same transition within 5-minute window). These duplicates will corrupt gate stabilization analysis in Phase v0.33-03. Cleaning the data now and preventing future duplicates ensures reliable flip-flop detection downstream.

Output: One-time dedup script (bin/dedup-changelog.cjs), write-time guard in appendChangelog(), unit tests, cleaned promotion-changelog.json.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.33-01-time-series-foundation/v0.33-01-RESEARCH.md
@bin/compute-per-model-gates.cjs
@.planning/formal/promotion-changelog.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add write-time dedup guard and create cleanup script</name>
  <files>bin/compute-per-model-gates.cjs, bin/dedup-changelog.cjs</files>
  <action>
**1. Modify `appendChangelog()` in bin/compute-per-model-gates.cjs** (~line 58):

Add a write-time dedup guard before `changelog.push(entry)`. The guard checks if any existing entry in the changelog matches ALL of:
- Same `model` value
- Same `from_level` value
- Same `to_level` value
- Timestamp within 5-minute window (`Math.abs(entryTime - existingTime) < 5 * 60 * 1000`)

If a duplicate is found, log a DEDUP message to stderr and return without writing. Define `DEDUP_WINDOW_MS = 5 * 60 * 1000` as a constant at the top of the file (near other constants like CHANGELOG_MAX_ENTRIES).

The check scans from the end of the array for efficiency (most recent entries are most likely to match). Use `for (let i = changelog.length - 1; i >= 0; i--)` and break once timestamps are older than the window.

Follow the implementation from RESEARCH.md Pattern 3 but use reverse scan for efficiency.

**2. Create `bin/dedup-changelog.cjs`** -- One-time cleanup script:

- Read `.planning/formal/promotion-changelog.json`
- Implement `dedup(changelog)` function that iterates entries in order, keeping each entry only if no previously-kept entry matches (same model, from_level, to_level within 5-minute window). Use the same `DEDUP_WINDOW_MS` constant.
- When run as main (`require.main === module`):
  - Read the changelog file
  - Run dedup
  - Print before/after/removed counts to stdout
  - Write cleaned array back to file
- Export `{ dedup }` for testing

**3. Run the cleanup script** to clean existing data:
The executor should run `node bin/dedup-changelog.cjs` after creating the script to clean the existing 1088 duplicates. This is a one-time operation.
  </action>
  <verify>
Run: `grep -n 'DEDUP_WINDOW_MS' bin/compute-per-model-gates.cjs` -- shows constant definition.
Run: `grep -n 'DEDUP' bin/compute-per-model-gates.cjs` -- shows dedup guard logic in appendChangelog.
Run: `node bin/dedup-changelog.cjs` -- prints "Before: N, After: M, Removed: R" with R > 0 (or R = 0 if already cleaned).
Run: `node -e "const d = require('./bin/dedup-changelog.cjs'); console.log(typeof d.dedup)"` -- prints "function".
  </verify>
  <done>
appendChangelog() in compute-per-model-gates.cjs rejects duplicate entries within 5-minute window. dedup-changelog.cjs exists as standalone cleanup tool. Existing promotion-changelog.json has been cleaned of duplicates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for dedup logic and write-time guard</name>
  <files>bin/dedup-changelog.test.cjs, package.json</files>
  <action>
Create `bin/dedup-changelog.test.cjs` using `node:test` and `node:assert/strict`.

Test cases for the `dedup()` function (import from `bin/dedup-changelog.cjs`):

1. **Removes duplicates within 5-min window** -- Two entries with same model/from/to, timestamps 1 minute apart. dedup keeps only the first.
2. **Keeps entries outside window** -- Two entries with same model/from/to but timestamps 10 minutes apart. Both kept.
3. **Keeps different transitions** -- Same model, same timestamp, but different from_level/to_level. Both kept.
4. **Handles empty array** -- `dedup([])` returns `[]`.
5. **Handles single entry** -- `dedup([entry])` returns `[entry]`.
6. **Preserves chronological order** -- After dedup, entries remain in original chronological order.
7. **Multiple models interleaved** -- Entries for model_A and model_B interleaved, only same-model duplicates removed.
8. **Boundary test: exactly 5 minutes** -- Entries exactly 5 * 60 * 1000 ms apart. Should be KEPT (guard uses strict less-than).

Test cases for the write-time guard in appendChangelog() (these test the actual function behavior):

9. **Guard blocks duplicate write** -- Create a temp changelog with one entry, attempt to appendChangelog with duplicate (same model/from/to within 5 min). File should still have original count.
10. **Guard allows non-duplicate write** -- appendChangelog with a different model succeeds.

For tests 9-10, use a temp directory and temporarily override the CHANGELOG_PATH constant, or test indirectly through the dedup function (simpler, since the guard uses the same logic). Choose the simpler approach that provides adequate coverage.

After creating the test file, add `bin/dedup-changelog.test.cjs` to the `test:ci` script in `package.json`. Append it to the existing list.

NOTE: Plan 01 also modifies package.json (adding solve-trend.test.cjs). Since both plans run in Wave 1 (parallel), coordinate by appending to the end of the test:ci list. If a merge conflict occurs, the executor should combine both additions.
  </action>
  <verify>
Run: `node --test bin/dedup-changelog.test.cjs` -- all tests pass.
Run: `grep 'dedup-changelog.test.cjs' package.json` -- confirms test is registered.
Run: `npm run test:ci` -- full suite passes with no regressions.
  </verify>
  <done>
dedup-changelog.test.cjs exists with 8-10 test cases covering dedup logic (within/outside window, different transitions, empty array, order preservation, boundary). Test file registered in package.json test:ci. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `node -e "const c = JSON.parse(require('fs').readFileSync('.planning/formal/promotion-changelog.json','utf8')); const dupes = c.filter((e,i) => c.some((o,j) => j < i && o.model === e.model && o.from_level === e.from_level && o.to_level === e.to_level && Math.abs(new Date(o.timestamp) - new Date(e.timestamp)) < 300000)); console.log('Dupes:', dupes.length)"` -- prints "Dupes: 0"
2. `grep 'DEDUP_WINDOW_MS' bin/compute-per-model-gates.cjs` -- shows constant
3. `node --test bin/dedup-changelog.test.cjs` -- all pass
4. `npm run test:ci` -- no regressions
</verification>

<success_criteria>
- promotion-changelog.json contains zero duplicate entries (same model, same transition within 5-minute window)
- Write-time dedup guard in appendChangelog() prevents future duplicates by rejecting matching entries within the 5-minute window
- One-time cleanup script (bin/dedup-changelog.cjs) is available for re-running if needed
- All unit tests pass and are registered in test:ci
</success_criteria>

<output>
After completion, create `.planning/phases/v0.33-01-time-series-foundation/v0.33-01-02-SUMMARY.md`
</output>
