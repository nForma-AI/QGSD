---
phase: v0.20-05
plan: "01"
status: complete
completed: 2026-02-28
---

# v0.20-05-01 SUMMARY: Wave 0 RED Test Scaffold

## Objective
Scaffold 8 failing (RED) tests for EVID-01 and EVID-02 in `bin/validate-traces.test.cjs`.

## What Was Built
- Added `runValidatorKeepTmp()` helper — runs validator without cleaning up tmpDir, returns `result.tmpDir`
- Added `getLastNDJSON(tmpDir)` helper — reads last NDJSON record from `formal/check-results.ndjson` in tmpDir
- Added `writeScoreboard(tmpDir, rounds)` helper — writes quorum-scoreboard.json fixture
- Added `VALID_EVENT` constant — valid conformance event line for test runs
- Added 8 failing tests covering observation_window fields and v2.1 check-result fields

## Test Results
- 24 previously passing tests: GREEN (no regressions)
- 8 new tests: RED (AssertionError: NDJSON record must be written — observation_window not yet implemented)

## Failing Tests (RED state confirmed)
1. `observation_window.window_start is ISO-8601 string`
2. `observation_window.window_end is ISO-8601 string`
3. `observation_window.n_traces is 0 without scoreboard`
4. `observation_window.n_events equals conformance log line count`
5. `observation_window.window_days is 0 without scoreboard`
6. `observation_window.window_days matches multi-day scoreboard span`
7. `check_id is ci:conformance-traces`
8. `surface is ci`

## Commit
`bd8863a5` feat(v0.20-05-01): add Wave 0 RED tests for EVID-01 and EVID-02

## key-files
created:
  - bin/validate-traces.test.cjs (122 lines added, Wave 0 section appended)
