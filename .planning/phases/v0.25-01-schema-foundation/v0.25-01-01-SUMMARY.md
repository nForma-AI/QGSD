---
phase: v0.25-01-schema-foundation
plan: "01"
subsystem: formal-traceability
tags: [formal-methods, tla, alloy, prism, requirements, json-schema, traceability]

# Dependency graph
requires: []
provides:
  - "model-registry.json: requirements field (string[]) on all 24 model entries"
  - "requirements.json: formal_models field (string[]) on 56 requirements with known coverage"
  - "aggregate-requirements.cjs: formal_models preservation across re-runs"
affects: [v0.25-02-annotation-schema, v0.25-03-matrix-generator, v0.25-04-bidirectional-validation, v0.25-05-decomp-integration]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bidirectional traceability: model-registry.json owns model->req mapping; requirements.json owns req->model mapping"
    - "Enrichment data preservation: aggregator captures formal_models before re-aggregation and merges back after"

key-files:
  created: []
  modified:
    - "formal/model-registry.json"
    - "formal/requirements.json"
    - "bin/aggregate-requirements.cjs"

key-decisions:
  - "26 requirement IDs in the seed map (QUORUM-04, SAFE-03, SAFE-04, CRED-07..12, etc.) do not exist in requirements.json -- formal_models skipped for those; model-registry still carries them as forward references for when requirements are added"
  - "Test model entry (../../../../tmp/promote-test/...) given empty requirements array rather than omitting the field, consistent with TUINavigation and QGSDQuorum_xstate"
  - "Aggregator uses pre-aggregation capture + merge-back pattern (not pass-through) because it builds fresh from milestone files"

patterns-established:
  - "formal_models field placement: after background field in requirement objects"
  - "requirements field placement: after description field in model-registry entries"

requirements-completed: [SCHEMA-01, SCHEMA-04]

# Metrics
duration: 4min
completed: 2026-03-03
---

# Phase v0.25-01 Plan 01: Schema Foundation Summary

**Bidirectional requirement-model traceability seeded: model-registry.json gets requirements arrays (21 of 24 non-empty), requirements.json gets formal_models arrays for 56 requirements, and the aggregator preserves formal_models across re-runs**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-03-03T08:54:28Z
- **Completed:** 2026-03-03T08:57:47Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added `requirements: string[]` to all 24 model entries in model-registry.json using seed data from TRACEABILITY_RESEARCH.md Section 5
- Added `formal_models: string[]` to 56 requirements in requirements.json via inverse mapping from the model-registry seed
- Updated aggregate-requirements.cjs to capture and preserve `formal_models` enrichment data across re-aggregation runs

## Task Commits

Each task was committed atomically:

1. **Task 1: Add requirements arrays to model-registry.json (SCHEMA-01)** - `1db54645` (feat)
2. **Task 2: Add formal_models to requirements.json and update aggregator (SCHEMA-04)** - `cd672224` (feat)

**Plan metadata:** (docs commit follows this summary)

## Files Created/Modified
- `formal/model-registry.json` - Added `requirements: string[]` field to all 24 model entries; 21 with non-empty data, 3 empty (TUINavigation, QGSDQuorum_xstate, test entry)
- `formal/requirements.json` - Added `formal_models: string[]` field to 56 requirements with known formal model coverage; field placed after `background`
- `bin/aggregate-requirements.cjs` - Added formal_models capture before re-aggregation and merge-back after; enrichment data now survives re-runs

## Decisions Made
- 26 requirement IDs in the seed map do not exist in requirements.json (QUORUM-04, SAFE-03, SAFE-04, CRED-07 through CRED-12, DETECT-06, PLAN-04/05/06, SCBD-04 through SCBD-07, STOP-10/11, SLOT-05, PRM-01, PRM-AM-01). `formal_models` was not added for these. The model-registry carries them as forward references for when those requirements are added to REQUIREMENTS.md.
- The test model entry received `"requirements": []` (consistent with TUINavigation and QGSDQuorum_xstate) rather than omitting the field entirely.
- Bidirectional check passes: all 56 requirements that have `formal_models` have correct model paths matching what model-registry claims. The 28 "SKIP" items are models claiming requirements that don't exist in requirements.json yet -- consistent with the 26-missing-IDs finding.

## Deviations from Plan

None - plan executed exactly as written.

Note: The plan verification step `node bin/aggregate-requirements.cjs` cannot run against the live file because requirements.json is frozen (frozen_at is set). The aggregator preservation was verified using a controlled temp-unfreeze test that confirmed formal_models survives re-aggregation correctly.

## Issues Encountered
- requirements.json is frozen (`frozen_at: 2026-03-03T07:56:45.450Z`), which prevents running the aggregator directly against it for verification. Used a controlled temp-unfreeze test instead to verify preservation logic. This is expected behavior per the envelope design (ENV-04).

## Next Phase Readiness
- v0.25-01-02 (check-result schema + writer): no dependency on this plan, can run in parallel
- v0.25-01-03 (runner wiring): no dependency on this plan
- v0.25-02 (annotation schema): no dependency on this plan
- v0.25-03 (matrix generator): will query both model-registry.json requirements arrays and requirements.json formal_models -- both are now populated and consistent

## Self-Check: PASSED

- FOUND: formal/model-registry.json
- FOUND: formal/requirements.json
- FOUND: bin/aggregate-requirements.cjs
- FOUND: .planning/phases/v0.25-01-schema-foundation/v0.25-01-01-SUMMARY.md
- FOUND commit: 1db54645 (Task 1 - model-registry.json)
- FOUND commit: cd672224 (Task 2 - requirements.json + aggregator)

---
*Phase: v0.25-01-schema-foundation*
*Completed: 2026-03-03*
