---
phase: v0.33-03-gate-stabilization
plan: 02
subsystem: gate-promotion
tags: [flip-flop, cooldown, stability, integration, nf-solve]

requires:
  - phase: v0.33-03-01
    provides: gate-stability.cjs module (detectFlipFlops, isCooldownSatisfied, updateCooldownState, createUnstableEntry)
provides:
  - Flip-flop detection integrated into promotion pipeline (runs before every promotion decision)
  - Cooldown enforcement blocks re-promotion of UNSTABLE models
  - Stability fields (stability_status, direction_changes, cooldown) in per-model-gates.json
  - --write-per-model default in sweepPerModelGates for automatic persistence
affects: [nf:solve, per-model-gates.json consumers, gate scoring TUI]

tech-stack:
  added: []
  patterns: [pre-promotion-gate-check, schema-version-bump]

key-files:
  created: []
  modified:
    - bin/compute-per-model-gates.cjs
    - bin/nf-solve.cjs
---

# Plan 02 Summary: Integration into Promotion Pipeline

## What Was Built

### compute-per-model-gates.cjs Changes
1. **Imported gate-stability.cjs** — detectFlipFlops, isCooldownSatisfied, updateCooldownState, createUnstableEntry
2. **Flip-flop detection runs BEFORE promotion loop** (Pitfall 2) — loads changelog, detects unstable models, loads previous stability state from per-model-gates.json
3. **Both promotion paths gated** — ADVISORY->SOFT_GATE and SOFT_GATE->HARD_GATE check isCooldownSatisfied before allowing promotion
4. **Blocked promotions log to stderr** — with [compute-per-model-gates] prefix (project convention)
5. **Stability fields added to per-model output** — stability_status (STABLE/UNSTABLE), direction_changes, cooldown object, flagged_at
6. **Schema bumped to v3** — reflects new stability fields
7. **unstable_models count** added to output header

### nf-solve.cjs Changes
1. **--write-per-model added to sweepPerModelGates args** — per-model gate reasons now persist every solve run
2. **Only in sweepPerModelGates** (not getAggregateGates) — avoids double-write (Pitfall 4)

## Deviations
None. Implementation follows research recommendations and plan specifications exactly.

## Self-Check: PASSED
- [x] All tasks executed
- [x] Each task committed individually (2 commits)
- [x] SUMMARY.md created
- [x] grep verifications pass (gate-stability, stability_status, isCooldownSatisfied, schema_version 3, write-per-model)
- [x] Full test suite passes (1284/1284, 0 fail)
