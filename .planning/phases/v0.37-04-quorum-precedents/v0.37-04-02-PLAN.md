---
phase: v0.37-04-quorum-precedents
plan: 02
type: execute
wave: 2
depends_on: ["v0.37-04-01"]
files_modified:
  - bin/quorum-slot-dispatch.cjs
  - bin/quorum-slot-dispatch.test.cjs
autonomous: true
requirements:
  - QPREC-02
  - QPREC-03
formal_artifacts: none

must_haves:
  truths:
    - "Quorum dispatch prompts include up to 3 keyword-matched precedents relevant to the current question"
    - "Precedents older than 90 days are automatically pruned and do not appear in dispatch prompts"
  artifacts:
    - path: "bin/quorum-slot-dispatch.cjs"
      provides: "Precedent loading, keyword matching, TTL pruning, prompt injection into buildModeAPrompt and buildModeBPrompt"
      exports: ["loadPrecedents", "matchPrecedentsByKeywords", "formatPrecedentsSection"]
    - path: "bin/quorum-slot-dispatch.test.cjs"
      provides: "Tests for precedent matching, formatting, TTL filtering, prompt injection placement"
      contains: "precedent"
  key_links:
    - from: "bin/quorum-slot-dispatch.cjs"
      to: ".planning/quorum/precedents.json"
      via: "loadPrecedents reads JSON file fail-open"
      pattern: "precedents\\.json"
    - from: "bin/quorum-slot-dispatch.cjs (matchPrecedentsByKeywords)"
      to: "bin/quorum-slot-dispatch.cjs (extractKeywords)"
      via: "Reuses existing extractKeywords function for keyword overlap scoring"
      pattern: "extractKeywords"
    - from: "bin/quorum-slot-dispatch.cjs (main)"
      to: "bin/quorum-slot-dispatch.cjs (buildModeAPrompt/buildModeBPrompt)"
      via: "Passes matched precedents as parameter; prompt builders inject section after requirements"
      pattern: "precedents"
---

<objective>
Extend quorum-slot-dispatch.cjs to load precedents, match by keyword relevance, prune stale entries, and inject up to 3 relevant precedents into dispatch prompts.

Purpose: The extraction tool (Plan 01) creates the precedent database. This plan wires it into the live quorum dispatch path so models receive relevant past decisions as context during voting, improving consistency and reducing re-litigation of settled questions.
Output: Extended quorum-slot-dispatch.cjs with precedent integration, extended test suite verifying matching/formatting/injection/TTL behaviors.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/v0.37-04-quorum-precedents/v0.37-04-RESEARCH.md
@.planning/phases/v0.37-04-quorum-precedents/v0.37-04-01-SUMMARY.md
@bin/quorum-slot-dispatch.cjs
@bin/quorum-slot-dispatch.test.cjs
@bin/extract-precedents.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add precedent loading, matching, formatting, and injection to quorum-slot-dispatch.cjs</name>
  <files>bin/quorum-slot-dispatch.cjs</files>
  <action>
Extend `bin/quorum-slot-dispatch.cjs` with three new functions and wire them into the dispatch pipeline. Follow the exact pattern used for requirements loading/matching/injection (lines 98-369).

**New functions:**

1. `loadPrecedents(projectRoot)` — Load precedents.json fail-open:
   - Read `path.join(projectRoot, '.planning/quorum/precedents.json')`
   - Parse JSON, extract `precedents` array
   - Return array on success, empty array on any error (fail-open with stderr warning)
   - Mirror the `loadRequirements()` pattern (lines 98-152)

2. `matchPrecedentsByKeywords(precedents, question, maxMatches = 3)` — Score and select:
   - Reuse existing `extractKeywords()` function (line 237) for tokenization
   - For each precedent: score = (keyword overlap on precedent.question * 1) + (keyword overlap on precedent.outcome * 2)
   - Filter by `isPrecedentFresh()` — import from extract-precedents.cjs or inline the TTL check (prefer inline to avoid circular dependency; use same 90-day default)
   - Filter score > 0, sort descending, take top `maxMatches` (default 3)
   - Return array of matched precedent objects

3. `formatPrecedentsSection(precedents)` — Format for prompt injection:
   - Return null if empty/null input
   - Format as: header `=== PAST QUORUM PRECEDENTS ===`, description line, then for each precedent: `- **CONSENSUS** (date): truncated question (120 chars)` and `  Outcome: truncated outcome (150 chars)`
   - Footer: `================================`
   - Follow the `formatRequirementsSection()` pattern (lines 295-334)
   - Truncate question to 120 chars and outcome to 150 chars with `...` suffix (per research pitfall 4: prevent prompt size bloat)

**Wiring into dispatch pipeline (main function, around line 975):**
After the existing requirements loading block (lines 974-976), add:
```
const allPrecedents = loadPrecedents(repoDir);
const matchedPrecedents = matchPrecedentsByKeywords(allPrecedents, question);
```

**Wiring into prompt builders:**
- In `buildModeAPrompt` (line 336): Add `precedents` to destructured params. After the requirements section injection (lines 362-369), add precedent section injection using same pattern:
  ```
  if (precedents && precedents.length > 0) {
    const precSection = formatPrecedentsSection(precedents);
    if (precSection) { lines.push(''); lines.push(precSection); }
  }
  ```
- In `buildModeBPrompt` (line 485): Same pattern — add `precedents` param, inject after requirements section.
- Update both `buildModeAPrompt` and `buildModeBPrompt` call sites in main() to pass `precedents: matchedPrecedents`.

**Module exports update (line 1140):**
Add `loadPrecedents`, `matchPrecedentsByKeywords`, `formatPrecedentsSection` to the exports object.

**TTL implementation (QPREC-03):**
Inline the freshness check in `matchPrecedentsByKeywords` rather than importing from extract-precedents.cjs (avoids cross-module dependency for a 5-line function). Use same logic: `(Date.now() - new Date(prec.date).getTime()) < 90 * 24 * 60 * 60 * 1000`. Stale precedents are filtered out before scoring, ensuring they never appear in dispatch prompts.

**Invariant compliance:** The EventualConsensus liveness property from the quorum formal spec is unaffected — precedent injection adds prompt context but does not change the vote/decide/deliberate action structure.
  </action>
  <verify>
1. `grep 'loadPrecedents' bin/quorum-slot-dispatch.cjs` — function exists
2. `grep 'matchPrecedentsByKeywords' bin/quorum-slot-dispatch.cjs` — function exists
3. `grep 'formatPrecedentsSection' bin/quorum-slot-dispatch.cjs` — function exists
4. `grep 'PAST QUORUM PRECEDENTS' bin/quorum-slot-dispatch.cjs` — section header in formatter
5. `grep 'precedents' bin/quorum-slot-dispatch.cjs | grep -c 'module.exports'` — exported
6. `node -e "const m = require('./bin/quorum-slot-dispatch.cjs'); console.log(typeof m.loadPrecedents, typeof m.matchPrecedentsByKeywords, typeof m.formatPrecedentsSection)"` — prints "function function function"
  </verify>
  <done>quorum-slot-dispatch.cjs loads precedents, matches by keyword relevance (top 3), prunes stale entries (>90 days), and injects formatted section into both Mode A and Mode B prompts after the requirements section</done>
</task>

<task type="auto">
  <name>Task 2: Extend quorum-slot-dispatch test suite for precedent behaviors</name>
  <files>bin/quorum-slot-dispatch.test.cjs</files>
  <action>
Extend the existing `bin/quorum-slot-dispatch.test.cjs` with new test sections for precedent functionality. Add tests after the existing test blocks.

**Structural tests (new describe block: "Precedent exports"):**
- `loadPrecedents` is exported and is a function
- `matchPrecedentsByKeywords` is exported and is a function
- `formatPrecedentsSection` is exported and is a function

**matchPrecedentsByKeywords tests (new describe block: "matchPrecedentsByKeywords"):**
- Returns empty array when precedents is empty
- Returns empty array when question has no meaningful keywords (all stopwords)
- Matches precedent whose question shares keywords with input question
- Scores outcome keyword matches higher than question keyword matches (2x weight)
- Returns at most 3 precedents even when more match
- Sorts by score descending (highest relevance first)
- Excludes precedents with date > 90 days old (TTL pruning in matching)
- Includes precedent dated 89 days ago (within TTL)
- Handles precedent with invalid date gracefully (skips, does not throw)

**formatPrecedentsSection tests (new describe block: "formatPrecedentsSection"):**
- Returns null for empty array
- Returns null for null input
- Includes `=== PAST QUORUM PRECEDENTS ===` header
- Includes `================================` footer
- Truncates question longer than 120 chars with `...`
- Truncates outcome longer than 150 chars with `...`
- Includes consensus and date for each entry
- Formats multiple precedents correctly (one entry per precedent)

**Prompt injection tests (new describe block: "Precedent prompt injection"):**
- `buildModeAPrompt` with precedents param produces output containing `PAST QUORUM PRECEDENTS` section
- `buildModeAPrompt` with empty precedents does NOT include precedent section
- `buildModeBPrompt` with precedents param produces output containing `PAST QUORUM PRECEDENTS` section
- Precedent section appears after requirements section (if both present) — verify by checking string index ordering

**Test data:** Create mock precedent objects inline:
```javascript
const mockPrecedents = [
  { question: "Should we use keyword matching for precedent lookup?", date: new Date().toISOString().slice(0, 10), consensus: "APPROVE", outcome: "Keyword matching is sufficient for MVP", source_file: "test.md", computed_at: new Date().toISOString() },
  { question: "Should we add semantic embeddings?", date: new Date().toISOString().slice(0, 10), consensus: "BLOCK", outcome: "Deferred to future phase when corpus is larger", source_file: "test2.md", computed_at: new Date().toISOString() }
];
```

Follow existing test file patterns (node:test describe/it, node:assert/strict).
  </action>
  <verify>
Run: `npm test -- bin/quorum-slot-dispatch.test.cjs`
- All existing tests still pass (no regressions)
- All new precedent-related tests pass
- Test count increases by the number of new tests added
  </verify>
  <done>quorum-slot-dispatch.test.cjs covers precedent loading, keyword matching with scoring and TTL pruning, prompt formatting with truncation, and injection placement in both Mode A and Mode B prompts, with all tests passing and no regressions</done>
</task>

</tasks>

<verification>
1. `npm test -- bin/quorum-slot-dispatch.test.cjs` — all tests pass including new precedent tests
2. `node -e "const m = require('./bin/quorum-slot-dispatch.cjs'); const p = m.loadPrecedents('.'); console.log(Array.isArray(p))"` — prints true
3. `node -e "const m = require('./bin/quorum-slot-dispatch.cjs'); console.log(m.formatPrecedentsSection([{question:'test',date:'2026-03-01',consensus:'APPROVE',outcome:'yes'}]))"` — prints formatted section with header/footer
4. Precedent section appears in buildModeAPrompt output when precedents are provided
5. Stale precedents (>90 days) are filtered out before injection
</verification>

<success_criteria>
- Quorum dispatch prompts include up to 3 keyword-matched precedents relevant to the current question (QPREC-02)
- Precedents older than 90 days are automatically pruned and do not appear in dispatch prompts (QPREC-03)
- All existing quorum-slot-dispatch tests pass (no regressions)
- New precedent tests cover matching, formatting, TTL, and injection
</success_criteria>

<output>
After completion, create `.planning/phases/v0.37-04-quorum-precedents/v0.37-04-02-SUMMARY.md`
</output>
