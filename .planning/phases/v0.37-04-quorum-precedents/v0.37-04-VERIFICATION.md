---
phase: v0.37-04
phase_name: Quorum Precedents
status: passed
verified_at: 2026-03-16T23:10:00Z
verifier: nf-verifier
formal_check:
  passed: 3
  failed: 0
  skipped: 0
  counterexamples: []
must_haves:
  - id: SC1
    description: "Running extract-precedents.cjs produces precedents.json with BLOCK/APPROVE decisions"
    status: passed
    evidence: "15 precedents extracted from 69 debate files, all APPROVE/BLOCK, no INCONCLUSIVE"
  - id: SC2
    description: "Quorum dispatch prompts include up to 3 keyword-matched precedents"
    status: passed
    evidence: "buildModeAPrompt and buildModeBPrompt inject PAST QUORUM PRECEDENTS section; matchPrecedentsByKeywords returns max 3 sorted by score"
  - id: SC3
    description: "Precedents older than 90 days are automatically pruned"
    status: passed
    evidence: "91-day-old precedent excluded from matching; 89-day-old included; test TC-PREC-MATCH-7 and TC-PREC-MATCH-8 confirm"
requirements_checked:
  - QPREC-01
  - QPREC-02
  - QPREC-03
---

## Verification Report — Phase v0.37-04: Quorum Precedents

### Goal
Past quorum decisions become queryable knowledge that enriches future quorum dispatch with relevant context.

### Formal Check
Quorum module formal check: 3 passed, 0 failed, 0 skipped. No counterexamples. The EventualConsensus liveness property is unaffected by precedent injection (adds prompt context only, does not change vote/decide/deliberate action structure).

### Success Criteria Verification

**SC1: Extract-precedents produces precedents.json** -- PASSED
- `node bin/extract-precedents.cjs` exits 0
- `.planning/quorum/precedents.json` contains 15 entries
- All entries have required fields: question, date, consensus, outcome, source_file, computed_at
- No INCONCLUSIVE entries in output
- 54 slot-response files correctly skipped (lack debate template format)

**SC2: Dispatch prompts include up to 3 keyword-matched precedents** -- PASSED
- `loadPrecedents()` reads precedents.json fail-open with cache
- `matchPrecedentsByKeywords()` reuses `extractKeywords()`, weights outcome 2x, caps at 3
- `formatPrecedentsSection()` produces `=== PAST QUORUM PRECEDENTS ===` block
- Both `buildModeAPrompt` and `buildModeBPrompt` inject section after requirements
- TC-PREC-INJECT-1 through TC-PREC-INJECT-4 confirm placement

**SC3: Stale precedents (>90 days) are pruned** -- PASSED
- TTL check inlined in `matchPrecedentsByKeywords` (90-day threshold)
- 91-day-old precedent excluded (TC-PREC-MATCH-7)
- 89-day-old precedent included (TC-PREC-MATCH-8)
- Invalid date handled gracefully (TC-PREC-MATCH-9)

### Test Results
- `bin/extract-precedents.test.cjs`: 22/22 pass
- `bin/quorum-slot-dispatch.test.cjs`: 83/83 pass (57 existing + 26 new)
- Zero regressions

### Requirements Traceability
| Requirement | Covered By | Status |
|-------------|-----------|--------|
| QPREC-01 | Plan 01: extract-precedents.cjs + test suite | Satisfied |
| QPREC-02 | Plan 02: loadPrecedents + matchPrecedentsByKeywords + prompt injection | Satisfied |
| QPREC-03 | Plan 02: 90-day TTL in matchPrecedentsByKeywords + tests | Satisfied |
