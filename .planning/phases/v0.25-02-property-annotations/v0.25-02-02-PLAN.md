---
phase: v0.25-02-property-annotations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .formal/alloy/quorum-votes.als
  - .formal/alloy/quorum-composition.als
  - .formal/alloy/scoreboard-recompute.als
  - .formal/alloy/availability-parsing.als
  - .formal/alloy/transcript-scan.als
  - .formal/alloy/install-scope.als
  - .formal/alloy/taxonomy-safety.als
  - .formal/alloy/account-pool-structure.als
  - .formal/prism/quorum.props
  - .formal/prism/oauth-rotation.props
  - .formal/prism/mcp-availability.props
autonomous: true
requirements:
  - ANNOT-02
  - ANNOT-03
formal_artifacts: none

must_haves:
  truths:
    - "Every Alloy assertion and check in all 8 Alloy model files has a `-- @requirement REQ-ID` comment immediately preceding its definition -- verified by grep count matching expected assertion counts"
    - "Every PRISM property in all 3 .props files has a `// @requirement REQ-ID` comment immediately preceding its property formula -- verified by grep count matching expected property counts"
    - "Annotations use the correct comment syntax per formalism: `--` for Alloy, `//` for PRISM"
    - "Generated files (quorum-votes.als, quorum.props) have annotations added with a preservation note"
    - "Running `grep -c '@requirement' .formal/alloy/install-scope.als` returns at least 5 (one per assertion)"
  artifacts:
    - path: ".formal/alloy/install-scope.als"
      provides: "@requirement annotations for 5 assertions"
      contains: "@requirement INST"
    - path: ".formal/prism/quorum.props"
      provides: "@requirement annotations for 4 properties"
      contains: "@requirement PRM"
    - path: ".formal/alloy/scoreboard-recompute.als"
      provides: "@requirement annotations for 4 assertions"
      contains: "@requirement SCBD"
  key_links:
    - from: ".formal/alloy/*.als"
      to: ".planning/research/TRACEABILITY_RESEARCH.md"
      via: "Property-to-requirement mapping from Section 5.2"
      pattern: "@requirement \\w+-\\d+"
    - from: ".formal/prism/*.props"
      to: ".planning/research/TRACEABILITY_RESEARCH.md"
      via: "Property-to-requirement mapping from Section 5.3"
      pattern: "@requirement \\w+-\\d+"
---

<objective>
Add structured `-- @requirement REQ-ID` annotations to all 8 Alloy assertion files and `// @requirement REQ-ID` annotations to all 3 PRISM .props files.

Purpose: ANNOT-02 and ANNOT-03 require machine-parseable requirement annotations on all Alloy assertions and PRISM properties. The property-to-requirement mapping from TRACEABILITY_RESEARCH.md Sections 5.2 and 5.3 provides the authoritative source.
Output: All 8 Alloy files annotated with ~28 `@requirement` comments; all 3 PRISM .props files annotated with ~10 `@requirement` comments.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.25-02-property-annotations/v0.25-02-RESEARCH.md
@.planning/research/TRACEABILITY_RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate all 8 Alloy model files (ANNOT-02)</name>
  <files>
.formal/alloy/quorum-votes.als
.formal/alloy/quorum-composition.als
.formal/alloy/scoreboard-recompute.als
.formal/alloy/availability-parsing.als
.formal/alloy/transcript-scan.als
.formal/alloy/install-scope.als
.formal/alloy/taxonomy-safety.als
.formal/alloy/account-pool-structure.als
  </files>
  <action>
For each of the 8 Alloy model files, add `-- @requirement REQ-ID` annotation comments immediately before each `assert` definition. Place one `-- @requirement` line per requirement ID. The annotation must be directly above the `assert AssertName {` line with no blank lines between.

Use this exact mapping from TRACEABILITY_RESEARCH.md Section 5.2:

**quorum-votes.als (GENERATED):**
Add a preservation note at the top: `-- NOTE: @requirement annotations added by v0.25-02 (not generated). Preserve when regenerating.`
- ThresholdPasses: two lines — `-- @requirement QUORUM-02` and `-- @requirement SAFE-01`
- BelowThresholdFails: two lines — `-- @requirement QUORUM-02` and `-- @requirement SAFE-01`
- ZeroApprovalsFail: `-- @requirement SAFE-04`

**quorum-composition.als:**
- AllRulesHold: two lines — `-- @requirement SPEC-03` and `-- @requirement COMP-01`

**scoreboard-recompute.als:**
- RecomputeIdempotent: `-- @requirement SCBD-01`
- NoVoteLoss: `-- @requirement SCBD-02`
- NoDoubleCounting: `-- @requirement SCBD-03`
- TNplusScoreIsCorrect: `-- @requirement SCBD-04`

**availability-parsing.als:**
- ParseCorrect: `-- @requirement CALIB-01`
- YearRolloverHandled: `-- @requirement CALIB-02`
- FallbackIsNull: `-- @requirement CALIB-03`

**transcript-scan.als:**
- BoundaryCorrectCheck: `-- @requirement STOP-08`
- PairingUniqueCheck: `-- @requirement STOP-09`
- NoDuplicateCountingCheck: `-- @requirement STOP-10`
- SuccessCountNeverExceedsMinSizeCheck: `-- @requirement STOP-11`

**install-scope.als:**
- NoConflict: `-- @requirement INST-01`
- AllEquivalence: `-- @requirement INST-02`
- InstallIdempotent: `-- @requirement INST-03`
- RollbackSoundCheck: `-- @requirement INST-04`
- ConfigSyncCompleteCheck: `-- @requirement INST-05`

**taxonomy-safety.als:**
- NoInjection: `-- @requirement SCBD-05`
- TaxonomyClosed: `-- @requirement SCBD-06`
- NewCategoryConsistent: `-- @requirement SCBD-07`

**account-pool-structure.als:**
- AddPreservesValidity: `-- @requirement CRED-07`
- SwitchPreservesValidity: `-- @requirement CRED-08`
- RemovePreservesValidity: `-- @requirement CRED-09`
- SwitchPreservesPool: `-- @requirement CRED-10`
- RemoveShrinksPool: `-- @requirement CRED-11`

**Placement example for install-scope.als NoConflict:**
```alloy
-- @requirement INST-01
assert NoConflict {
    all s: InstallState | NoConflictingScope[s]
}
```

If an existing comment block precedes the assertion, place the `-- @requirement` line(s) AFTER the existing comment but BEFORE the `assert` line. Do NOT modify assertion formulas.
  </action>
  <verify>
```bash
for f in .formal/alloy/*.als; do echo "$f: $(grep -c '@requirement' "$f") annotations"; done
```
Expected counts: quorum-votes=5 (3 assertions, 2 have 2 req IDs), quorum-composition=2, scoreboard-recompute=4, availability-parsing=3, transcript-scan=4, install-scope=5, taxonomy-safety=3, account-pool-structure=5. Total: 31.

Verify generated file has preservation note:
```bash
grep -c "Preserve when regenerating" .formal/alloy/quorum-votes.als
```
Expected: 1
  </verify>
  <done>
All 8 Alloy model files have `-- @requirement REQ-ID` annotations on every assertion. Generated file quorum-votes.als has a preservation note. ANNOT-02 is fully satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Annotate all 3 PRISM .props files (ANNOT-03)</name>
  <files>
.formal/prism/quorum.props
.formal/prism/oauth-rotation.props
.formal/prism/mcp-availability.props
  </files>
  <action>
For each of the 3 PRISM .props files, add `// @requirement REQ-ID` annotation comments immediately before each property formula line. Place one `// @requirement` line per requirement ID. The annotation must be directly above the property formula (e.g., `P=? [ F s=1 ]`) with no blank lines between.

Use this exact mapping from TRACEABILITY_RESEARCH.md Section 5.3:

**quorum.props (GENERATED):**
Add a preservation note after the existing header: `// NOTE: @requirement annotations added by v0.25-02 (not generated). Preserve when regenerating.`

- P1 (`P=? [ F s=1 ]`): two lines — `// @requirement PRM-01` and `// @requirement QUORUM-04`
- P2 (`R{"rounds"}=? [ F s=1 ]`): `// @requirement PRM-01`
- P3 (`P=? [ F<=9 s=1 ]`): two lines — `// @requirement PRM-01` and `// @requirement LOOP-01`
- P4 (`P=? [ F<=10 s=1 ]`): `// @requirement PRM-01`

**oauth-rotation.props:**
- P_succeed (`P=? [ F s=1 ]`): two lines — `// @requirement PRM-AM-01` and `// @requirement CRED-12`
- P_exhausted (`P=? [ F s=0 ]`): `// @requirement PRM-AM-01`
- R_rotations (`R{"rotations"}=? [ F s<=1 ]`): `// @requirement PRM-AM-01`

**mcp-availability.props:**
- P1 (`S=? [ "min_quorum_available" ]`): `// @requirement MCPENV-04`
- P2 (`S=? [ "total_outage" ]`): two lines — `// @requirement MCPENV-04` and `// @requirement FAIL-01`
- P3 (`S=? [ "majority_available" ]`): `// @requirement MCPENV-04`

**Placement example for quorum.props P1:**
```prism
// @requirement PRM-01
// @requirement QUORUM-04
// P1: Eventual convergence — must be exactly 1.0 (certain termination)
P=? [ F s=1 ]
```

Note: For PRISM .props files, the `@requirement` lines should go BEFORE any descriptive comment for that property (e.g., `// P1: Eventual convergence...`) so the parser can associate annotations with the property formula that follows. If there is already a descriptive comment, place `@requirement` lines above the descriptive comment.

IMPORTANT: Preserve all existing comments and header text. Do NOT modify property formulas.
  </action>
  <verify>
```bash
for f in .formal/prism/*.props; do echo "$f: $(grep -c '@requirement' "$f") annotations"; done
```
Expected counts: quorum.props=6 (4 properties, 2 have 2 req IDs), oauth-rotation.props=4 (3 properties, 1 has 2 req IDs), mcp-availability.props=4 (3 properties, 1 has 2 req IDs). Total: 14.

Verify generated file has preservation note:
```bash
grep -c "Preserve when regenerating" .formal/prism/quorum.props
```
Expected: 1
  </verify>
  <done>
All 3 PRISM .props files have `// @requirement REQ-ID` annotations on every property. Generated file quorum.props has a preservation note. ANNOT-03 is fully satisfied.
  </done>
</task>

</tasks>

<verification>
1. `grep -rl '@requirement' .formal/alloy/ | sort` lists all 8 Alloy files
2. `grep -rl '@requirement' .formal/prism/*.props | sort` lists all 3 PRISM .props files
3. Total Alloy annotations: `grep -rc '@requirement' .formal/alloy/*.als | awk -F: '{sum+=$2}END{print sum}'` >= 31
4. Total PRISM annotations: `grep -rc '@requirement' .formal/prism/*.props | awk -F: '{sum+=$2}END{print sum}'` >= 14
5. No .pm files contain @requirement annotations (only .props files): `grep -rl '@requirement' .formal/prism/*.pm 2>/dev/null` returns empty
</verification>

<success_criteria>
- All 8 Alloy model files have @requirement annotations on every assertion
- All 3 PRISM .props files have @requirement annotations on every property
- Annotation format is `-- @requirement REQ-ID` for Alloy and `// @requirement REQ-ID` for PRISM
- No blank lines between annotation and definition
- No assertion/property formulas were modified
- Generated files have preservation notes
</success_criteria>

<output>
After completion, create `.planning/phases/v0.25-02-property-annotations/v0.25-02-02-SUMMARY.md`
</output>
