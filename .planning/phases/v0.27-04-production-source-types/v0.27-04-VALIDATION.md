---
phase: v0.27-04
slug: production-source-types
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-04
---

# Phase v0.27-04 — Validation Strategy

> Template created by `/qgsd:plan-phase v0.27-04` (step 5.5) after research.
> Populated by `/qgsd:plan-phase v0.27-04` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.27-04`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (`node:test`) |
| **Config file** | none — uses `node --test` directly |
| **Quick run command** | `node --test bin/observe-handler-prometheus.test.cjs bin/observe-handler-grafana.test.cjs bin/observe-handler-logstash.test.cjs` |
| **Full suite command** | `npm run test:ci` |
| **Estimated runtime** | ~15 seconds |
| **CI pipeline** | npm run test:ci (existing) |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/observe-handler-prometheus.test.cjs bin/observe-handler-grafana.test.cjs bin/observe-handler-logstash.test.cjs`
- **After every plan wave:** Run `npm run test:ci`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.27-04-01-01 | 01 | 1 | OBS-03 | unit | `node --test bin/observe-handler-prometheus.test.cjs` | TBD | ⬜ pending |
| v0.27-04-02-01 | 02 | 1 | OBS-04 | unit | `node --test bin/observe-handler-grafana.test.cjs` | TBD | ⬜ pending |
| v0.27-04-03-01 | 03 | 1 | OBS-05 | unit | `node --test bin/observe-handler-logstash.test.cjs` | TBD | ⬜ pending |
| v0.27-04-03-02 | 03 | 1 | OBS-05 | integration | `node -e "const h = require('./bin/observe-handlers.cjs'); console.log(typeof h.handlePrometheus, typeof h.handleGrafana, typeof h.handleLogstash)"` | TBD | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed. Each TDD plan creates its own test file as part of the RED phase (test-first), so test scaffolding is built into the implementation tasks.

---

## Manual-Only Verifications

All phase behaviors have automated verification coverage.

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-03-04

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.27-04`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 4 | `node --test bin/observe-handler-*.test.cjs` | {N} | {N} | ⬜ pending |

**Phase validation complete:** pending
