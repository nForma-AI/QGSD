---
phase: v0.20-06
status: passed
verified: 2026-02-28
verifier: orchestrator (auto-verification)
---

# Verification: Phase v0.20-06 — Triage Bundle

## Phase Goal

After every formal verification run, `generate-triage-bundle.cjs` produces two human-readable
files — a per-check delta report and a suspects list — giving the developer an immediate view
of what changed and what needs attention.

## Must-Haves Verification

### SC-1: `bin/generate-triage-bundle.cjs` exists and produces output files

**Verified**: PASS

```
ls -la bin/generate-triage-bundle.cjs
→ -rw-r--r-- ... 10631 Feb 28 23:46 bin/generate-triage-bundle.cjs

node bin/generate-triage-bundle.cjs
→ [generate-triage-bundle] Wrote: formal/diff-report.md
→ [generate-triage-bundle] Wrote: formal/suspects.md

ls formal/diff-report.md formal/suspects.md
→ both files exist
```

Suspects filtering confirmed: fail checks included, clean passes excluded (test suite GREEN).
Delta detection confirmed: transitioned/new checks detected, first-run handled.

### SC-2: `run-formal-verify.cjs` calls `generate-triage-bundle.cjs` as final step

**Verified**: PASS

```
grep -n "ci:triage-bundle" bin/run-formal-verify.cjs
→ line 204: tool: 'ci', id: 'ci:triage-bundle',
→ line 205: label: 'Generate triage bundle — diff-report.md + suspects.md (generate-triage-bundle)',
→ line 206: type: 'node', script: 'generate-triage-bundle.cjs', args: [],
```

Entry is LAST in STEPS (after ci:conformance-traces). Marked nonCritical: true so triage
failure is visible but does not block exit code.

### SC-3: "first run" note when no previous snapshot

**Verified**: PASS

```
rm formal/diff-report.md && node bin/generate-triage-bundle.cjs
grep -i "first run" formal/diff-report.md
→ **Previous Run:** first run — no previous snapshot
→ **Status:** First run — no previous snapshot to compare against.
```

## Requirements Traceability

| Requirement | Status | Evidence |
|-------------|--------|----------|
| TRIAGE-01 | PASS | bin/generate-triage-bundle.cjs creates diff-report.md + suspects.md; tests 1-6 GREEN |
| TRIAGE-02 | PASS | ci:triage-bundle entry in run-formal-verify.cjs STEPS at line 204; test 7 GREEN |

## Test Results

```
node --test bin/generate-triage-bundle.test.cjs
→ pass 7, fail 0
```

All 7 Wave 0 tests GREEN — full RED→GREEN cycle complete.

```
node --test bin/run-formal-verify.test.cjs
→ pass 12, fail 0
```

12/12 tests pass including new TRIAGE-02 assertion.

## Commits

- `b508dfae` — test(v0.20-06-01): add 7 RED tests for generate-triage-bundle TRIAGE-01/02
- `3791b47d` — feat(v0.20-06-02): implement generate-triage-bundle.cjs — TRIAGE-01
- `6342b28e` — feat(v0.20-06-03): wire ci:triage-bundle into run-formal-verify.cjs — TRIAGE-02

## Overall: PASSED

All 3 success criteria verified. TRIAGE-01 and TRIAGE-02 satisfied. No regressions.
