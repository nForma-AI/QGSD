---
phase: v0.20-02
status: passed
verified: 2026-02-28
verifier: qgsd-orchestrator
---

# Phase v0.20-02: Liveness Fairness Lint — Verification

**Goal:** The CI pipeline automatically detects liveness properties that lack a fairness declaration and marks them `inconclusive` instead of `pass`, preventing false-positive proofs.

## Must-Haves Verification

### SC1: Liveness cfg without fairness declaration → result=inconclusive

**Verified:** `node bin/check-liveness-fairness.cjs` outputs `Result: inconclusive` for specs lacking fairness declarations.

```
[check-liveness-fairness] Result: inconclusive
[check-liveness-fairness] Missing fairness declarations:
  MCTUINavigation: EscapeProgress
  MCsafety: AllTransitionsValid, DeliberationMonotone
```

NDJSON entry written with `result: "inconclusive"`, `tool: "check-liveness-fairness"`, `check_id: "ci:liveness-fairness-lint"`. PASS.

### SC2: Spec with fairness declaration → result=pass (no regression)

**Verified:** `node --test bin/check-liveness-fairness.test.cjs` — 6/6 tests GREEN, including:
- `result is 'pass' when all configs have fairness declarations` — PASS
- `result is 'inconclusive' when any config lacks fairness declaration` — PASS

No regression. PASS.

### SC3: ci:liveness-fairness-lint entry visible in run summary

**Verified:**
- `grep "ci:liveness-fairness-lint" bin/run-formal-verify.cjs` — entry present with `tool: 'ci'`, `script: 'check-liveness-fairness.cjs'`
- `node bin/run-formal-verify.cjs --only=ci` — 3/3 CI steps pass, `ci:liveness-fairness-lint` appears in SUMMARY table
- STEPS count = 26 (was 25). PASS.

## Requirements Traceability

| Requirement | Status | Evidence |
|-------------|--------|----------|
| LIVE-01 | SATISFIED | check-liveness-fairness.cjs exits 0, emits result=inconclusive for missing fairness |
| LIVE-02 | SATISFIED | ci:liveness-fairness-lint in STEPS[25] of run-formal-verify.cjs, visible via --only=ci |

## Test Suites

| Test file | Tests | Pass | Fail |
|-----------|-------|------|------|
| bin/check-liveness-fairness.test.cjs | 6 | 6 | 0 |
| bin/run-formal-verify.test.cjs | 10 | 10 | 0 |

All tests GREEN.

## Plans Completed

| Plan | Status | What was built |
|------|--------|----------------|
| v0.20-02-01 | Complete | Wave 0 RED test scaffold — 7 tests specifying LIVE-01/LIVE-02 contract |
| v0.20-02-02 | Complete | bin/check-liveness-fairness.cjs — dynamic MC*.cfg discovery, v2.1 NDJSON output |
| v0.20-02-03 | Complete | ci:liveness-fairness-lint STEPS entry in run-formal-verify.cjs (26 total) |

## Verdict: PASSED

All 3 success criteria satisfied. LIVE-01 and LIVE-02 requirements complete. Phase v0.20-02 goal achieved.
