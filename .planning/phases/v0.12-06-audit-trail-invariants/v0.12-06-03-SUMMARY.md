---
phase: v0.12-06
plan: "03"
status: complete
commit: 381eb81
date: 2026-02-25
requirements:
  - GAP-3
  - GAP-9
---

# v0.12-06 Plan 03 — GREEN: bin/run-audit-alloy.cjs Implementation

## What Was Built

Implemented `bin/run-audit-alloy.cjs` — the Node.js Alloy JAR runner for GAP-3 and GAP-9 audit trail specs. All 4 Wave 0 RED stubs from Plan 01 are now GREEN.

## Artifacts

| Artifact | Status |
|---|---|
| `bin/run-audit-alloy.cjs` | Created — 145 lines, zero external deps |

## Key Implementation Details

1. **`--spec` argument selector** — `VALID_SPECS = ['scoreboard-recompute', 'availability-parsing']`; defaults to `scoreboard-recompute` when omitted; exits 1 with descriptive message listing valid specs for unknown values
2. **Check order** (mandatory, matches tests): `--spec` validation → JAVA_HOME → Java version → JAR → .als → invoke Alloy JAR → scan for CEX
3. **Dynamic specName in CEX warning** — `'Counterexample found in ' + specName + '.als assertion'` — does NOT hardcode assertion name (ALY-02 fix carried forward)
4. **Zero external dependencies** — only `child_process`, `fs`, `path`
5. **`NoSpuriousApproval` does not appear** in run-audit-alloy.cjs (stale name from run-alloy.cjs not repeated)

## Test Results (Wave 3 GREEN)

All 4 tests in `bin/run-audit-alloy.test.cjs` pass GREEN:
- Test 1 (JAVA_HOME invalid): GREEN — exit 1, stderr contains "JAVA_HOME"
- Test 2 (JAR missing): PASS/SKIP — JAR present in repo, skip guard fires correctly
- Test 3 (unknown --spec bogus): GREEN — exit 1, stderr contains "Unknown spec"
- Test 4 (invalid --spec listing valid names): GREEN — exit 1, stderr contains spec names

## npm test Final Results

- 302 pass, 2 fail (pre-existing JAR-related failures — not introduced by v0.12-06)
- Zero regressions

## Deviations

None. Implementation mirrors run-alloy.cjs exactly with 3 targeted differences: --spec dispatch, dynamic .als path, dynamic specName in CEX warning.
