---
phase: v0.12-04
plan: "01"
subsystem: testing
tags: [tdd, tla+, circuit-breaker, run-oscillation-tlc, wave-0, red-stubs]

requires:
  - phase: v0.12-03
    provides: run-alloy.test.cjs pattern; package.json test script baseline

provides:
  - bin/run-oscillation-tlc.test.cjs — 5 Wave 0 RED stubs for run-oscillation-tlc.cjs error paths
  - package.json test script updated — bin/run-breaker-tlc.test.cjs (side-fix) and bin/run-oscillation-tlc.test.cjs appended

affects: v0.12-04-02 (TLA+ spec authoring), v0.12-04-03 (runner implementation turns these RED to GREEN)

tech-stack:
  added: []
  patterns:
    - Wave 0 RED stub pattern for runner scripts — spawnSync the target binary; all tests fail ENOENT or assert.fail() before implementation

key-files:
  created:
    - bin/run-oscillation-tlc.test.cjs
    - .planning/phases/v0.12-04-circuit-breaker-algorithm-verification/v0.12-04-01-SUMMARY.md
  modified:
    - package.json

key-decisions:
  - "Test 2 skips (not fails) when no Java or JAR present — mirrors run-breaker-tlc.test.cjs pattern exactly"
  - "Test 5 uses assert.fail() when run-oscillation-tlc.cjs does not exist — gives clear Wave 0 RED signal"
  - "run-breaker-tlc.test.cjs side-fix: was accidentally missing from npm test; added alongside new file"
  - "Workers conditional in Test 5 regex: MCoscillation → '1', MCconvergence → 'auto' (from RESEARCH.md Pitfall 5)"

patterns-established:
  - "Wave 0 RED stub for runner test: reference binary by path; spawnSync fails ENOENT → tests fail assertion not crash"
  - "Test 5 workers assertion: checks source file content when file exists, assert.fail() when it doesn't — deterministic RED"
---

# v0.12-04 Plan 01 SUMMARY

**Date:** 2026-02-25
**Phase:** v0.12-04 — Circuit Breaker Algorithm Verification
**Plan:** 01 — Wave 0 RED stubs
**Status:** COMPLETE

## What Was Built

### Task 1: bin/run-oscillation-tlc.test.cjs

Created with 5 Wave 0 RED stubs:

1. `exits non-zero and prints clear error when JAVA_HOME points to nonexistent path` — FAIL (ENOENT on spawn)
2. `exits non-zero and prints download URL when tla2tools.jar is not found` — SKIP (no Java on this machine)
3. `exits non-zero with descriptive message for unknown --config value` — FAIL (ENOENT on spawn)
4. `exits non-zero and lists valid configs (MCoscillation, MCconvergence) in error output for invalid config` — FAIL (ENOENT on spawn)
5. `MCoscillation config uses -workers 1 and MCconvergence does not specify -workers 1` — FAIL (assert.fail() — file not yet implemented)

Pattern: mirrors `bin/run-breaker-tlc.test.cjs` exactly — same spawnSync header, same Java detection logic in Test 2, same skip-not-fail guard.

### Task 2: package.json test script update

Appended two entries to `scripts.test`:
- `bin/run-breaker-tlc.test.cjs` — side-fix; was present on disk but missing from test script
- `bin/run-oscillation-tlc.test.cjs` — new Wave 0 stub file

### Verification

```
node --test bin/run-oscillation-tlc.test.cjs
# ✖ 4 fail (ENOENT + assert.fail), ✔ 1 pass (Test 2 skipped — no Java)

npm test
# 287 pass, 6 fail
# New failures: 4 run-oscillation-tlc stubs (expected RED)
# Pre-existing failures: run-tlc/run-alloy JAR tests (Java installed locally)
# All 287 pre-existing tests continue to pass
```

Package.json includes both:
```
node -e "const p = require('./package.json'); console.log(p.scripts.test)" | grep -E "run-breaker-tlc|run-oscillation-tlc"
# → true
```

## Commits

1. `d7179cc` — `test(v0.12-04): add Wave 0 RED stubs for run-oscillation-tlc error paths`
2. `bfcddc0` — `chore(v0.12-04): add run-breaker-tlc and run-oscillation-tlc tests to npm test script`

## Success Criteria Check

| Criterion | Status |
|-----------|--------|
| bin/run-oscillation-tlc.test.cjs exists with 5 stubs | PASS |
| All 5 stubs fail in Wave 0 RED state (4 fail, 1 skips on no-Java) | PASS |
| package.json includes bin/run-breaker-tlc.test.cjs | PASS |
| package.json includes bin/run-oscillation-tlc.test.cjs | PASS |
| All pre-existing npm test cases continue to pass | PASS |
| Wave 0 commit exists | PASS |

## Next: Plan 02

`v0.12-04-02-PLAN.md` — Author TLA+ specs:
- `formal/tla/QGSDOscillation.tla` — oscillation detection algorithm spec (GAP-1)
- `formal/tla/MCoscillation.cfg` — TLC config for oscillation spec
- `formal/tla/QGSDConvergence.tla` — state persistence invariants spec (GAP-5)
- `formal/tla/MCconvergence.cfg` — TLC config for convergence spec
