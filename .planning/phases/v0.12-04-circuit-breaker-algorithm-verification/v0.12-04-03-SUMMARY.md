---
phase: v0.12-04
plan: "03"
subsystem: testing
tags: [tla-plus, formal-verification, tlc, node-test, circuit-breaker, oscillation]

# Dependency graph
requires:
  - phase: v0.12-04-01
    provides: Wave 0 RED stubs — bin/run-oscillation-tlc.test.cjs with 5 failing tests + package.json test script entry
  - phase: v0.12-04-02
    provides: TLA+ specs — QGSDOscillation.tla (GAP-1) + QGSDConvergence.tla (GAP-5) + MCoscillation.cfg + MCconvergence.cfg
provides:
  - bin/run-oscillation-tlc.cjs — Node.js TLC wrapper for QGSDOscillation and QGSDConvergence specs with Java detection, JAR check, config dispatch, and workers selection
  - All 5 run-oscillation-tlc.test.cjs stubs GREEN — error-path coverage for JAVA_HOME, JAR, unknown config, invalid config, workers conditional
affects:
  - v0.12-04 phase completion
  - any future formal verification runner tooling in bin/

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TLC runner forked from run-breaker-tlc.cjs template with 5 targeted diffs: VALID_CONFIGS, default config, spec path dispatch, workers selection, log prefix"
    - "workers='1' for liveness specs (MCoscillation has PROPERTY), workers='auto' for safety-only specs (MCconvergence)"
    - "Wave 0 RED stubs turned GREEN by implementing the corresponding runner — TDD GREEN phase"

key-files:
  created:
    - bin/run-oscillation-tlc.cjs
  modified: []

key-decisions:
  - "MCoscillation uses -workers 1 (liveness PROPERTY present — avoids TLC multi-worker liveness bug in TLC v1.8.0)"
  - "MCconvergence uses -workers auto (safety-only INVARIANT — multi-worker is safe and faster)"
  - "Zero external npm dependencies in runner — child_process, fs, path built-ins only"
  - "specPath dispatched from configName: MCoscillation → QGSDOscillation.tla, MCconvergence → QGSDConvergence.tla"

patterns-established:
  - "Pattern: TLC runner cloning — copy run-breaker-tlc.cjs, apply 5 targeted changes (VALID_CONFIGS, default, spec dispatch, workers, log prefix)"
  - "Pattern: workers conditional — liveness specs always use workers='1'; safety-only specs use workers='auto'"

requirements-completed: [GAP-1, GAP-5]

# Metrics
duration: 2min
completed: 2026-02-25
---

# Phase v0.12-04 Plan 03: run-oscillation-tlc.cjs Implementation Summary

**Node.js TLC runner for QGSDOscillation and QGSDConvergence specs — Java/JAR gating, config dispatch, -workers 1 for liveness (MCoscillation) and -workers auto for safety-only (MCconvergence)**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-25T11:22:49Z
- **Completed:** 2026-02-25T11:24:32Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Implemented bin/run-oscillation-tlc.cjs — 120 lines, zero external npm dependencies
- All 5 Wave 0 RED stubs in bin/run-oscillation-tlc.test.cjs turned GREEN: JAVA_HOME error, JAR missing, unknown config, invalid config, workers conditional
- npm test passes: 293/295 tests pass; 2 pre-existing failures in run-alloy and run-tlc JAR-present tests (pre-existing, unrelated)

## Task Commits

Each task was committed atomically:

1. **Task 1: GREEN — implement bin/run-oscillation-tlc.cjs** - `fb20d90` (feat)
2. **Task 2: Full npm test green-check and final verification** - verification only, no new commit

**Plan metadata:** (final docs commit — see below)

## Files Created/Modified

- `bin/run-oscillation-tlc.cjs` — TLC runner for QGSDOscillation (GAP-1) and QGSDConvergence (GAP-5); Java detection, JAR presence check, config dispatch, workers selection based on spec type

## Decisions Made

- workers='1' for MCoscillation: liveness PROPERTY requires single-worker mode to avoid TLC v1.8.0 multi-worker liveness bug (matches MCliveness pattern in run-tlc.cjs)
- workers='auto' for MCconvergence: safety-only (INVARIANT only), multi-worker is safe and faster
- specPath computed from configName: MCoscillation → QGSDOscillation.tla, MCconvergence → QGSDConvergence.tla
- All log prefixes use [run-oscillation-tlc] to distinguish from [run-breaker-tlc] in stdout/stderr

## Deviations from Plan

None - plan executed exactly as written. The exact implementation from the plan's code block was used verbatim.

## Issues Encountered

None. The 2 npm test failures (run-alloy JAR test and run-tlc JAR test) are pre-existing failures caused by the JARs being present on this system — the tests skip when JARs are absent and fail when they are present. These failures existed before this plan's changes.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase v0.12-04 is COMPLETE: all 3 plans done
  - Plan 01: Wave 0 RED stubs + package.json test entry
  - Plan 02: QGSDOscillation.tla (GAP-1) + QGSDConvergence.tla (GAP-5) + cfg files
  - Plan 03: bin/run-oscillation-tlc.cjs runner (this plan)
- GAP-1 (oscillation detection algorithm correctness) and GAP-5 (circuit breaker state persistence) TLA+ specs are complete and runnable via `node bin/run-oscillation-tlc.cjs MCoscillation|MCconvergence`
- Requires Java >=17 and formal/tla/tla2tools.jar for actual TLC model checking

---
*Phase: v0.12-04*
*Completed: 2026-02-25*
