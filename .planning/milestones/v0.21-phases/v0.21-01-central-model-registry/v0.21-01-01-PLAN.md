---
phase: v0.21-01
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - bin/initialize-model-registry.test.cjs
  - bin/promote-model.test.cjs
  - bin/accept-debug-invariant.test.cjs
  - bin/model-registry.test.cjs
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03]

must_haves:
  truths:
    - "All four test files exist and fail (RED) when run against absent implementation files"
    - "Each test file covers the behavior cases documented in VALIDATION.md Wave 0 requirements"
    - "Running node --test on any test file exits non-zero (tests fail, not error)"
  artifacts:
    - path: "bin/initialize-model-registry.test.cjs"
      provides: "RED tests for idempotent init, relative paths, .formal/ scan"
      min_lines: 30
    - path: "bin/promote-model.test.cjs"
      provides: "RED tests for merge, duplicate rejection, atomic rename, version increment"
      min_lines: 40
    - path: "bin/accept-debug-invariant.test.cjs"
      provides: "RED tests for spec write, conflict rejection, session_id, update_source"
      min_lines: 35
    - path: "bin/model-registry.test.cjs"
      provides: "RED tests for registry schema: paths, required fields, version, enum"
      min_lines: 30
  key_links:
    - from: "bin/initialize-model-registry.test.cjs"
      to: "bin/initialize-model-registry.cjs"
      via: "require('./initialize-model-registry.cjs')"
      pattern: "require.*initialize-model-registry"
    - from: "bin/promote-model.test.cjs"
      to: "bin/promote-model.cjs"
      via: "require('./promote-model.cjs')"
      pattern: "require.*promote-model"
    - from: "bin/accept-debug-invariant.test.cjs"
      to: "bin/accept-debug-invariant.cjs"
      via: "require('./accept-debug-invariant.cjs')"
      pattern: "require.*accept-debug-invariant"
---

<objective>
Create Wave 0 RED test scaffolding for all four formal model registry scripts before any implementation begins.

Purpose: Nyquist compliance requires test stubs committed before implementation so every implementation task has an immediately-runnable feedback loop. These tests must fail (RED) because the implementation files don't exist yet.
Output: Four test files covering ARCH-01/02/03 behaviors; all exit non-zero when run.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.21-01-central-model-registry/v0.21-01-RESEARCH.md
@.planning/phases/v0.21-01-central-model-registry/v0.21-01-VALIDATION.md
@bin/generate-triage-bundle.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RED test stubs for initialize-model-registry and model-registry schema</name>
  <files>bin/initialize-model-registry.test.cjs</files>
  <files>bin/model-registry.test.cjs</files>
  <action>
Create two test files using Node.js built-in `node:test` framework (matching existing pattern in bin/generate-triage-bundle.test.cjs — shebang `#!/usr/bin/env node`, `'use strict'`, `const { test } = require('node:test')`, `const assert = require('node:assert')`).

**bin/initialize-model-registry.test.cjs** — 3 tests covering ARCH-01 init behaviors:
1. "idempotent: exits silently if registry exists" — use os.tmpdir() scratch dir; write a dummy `.formal/model-registry.json`; run initialize-model-registry.cjs; assert registry content unchanged (require the script via spawnSync since it may not export functions).
2. "empty .formal/ produces registry with correct update_source" — create temp dir with `.formal/tla/Test.tla` and `.formal/tla/QGSDQuorum_xstate.tla`; run tool; parse output JSON; assert `models["tla/Test.tla"].update_source === "manual"` and `models["tla/QGSDQuorum_xstate.tla"].update_source === "generate"`.
3. "all registry keys are relative paths (no ./ prefix, no absolute prefix)" — run on temp .formal/; parse registry; for every key assert `!key.startsWith('/') && !key.startsWith('./')`.

Use `spawnSync(process.execPath, [TOOL_PATH], { cwd: tmpDir, encoding: 'utf8', timeout: 10000 })` pattern. At this point the tool doesn't exist — tests will fail with non-zero exit due to MODULE_NOT_FOUND. That is the expected RED state.

**bin/model-registry.test.cjs** — 4 schema validation tests covering ARCH-01:
1. "registry schema has required top-level fields (version, last_sync, models)" — parse a sample registry JSON, assert all three keys present.
2. "each model entry has required fields" — assert keys: version, last_updated, update_source, source_id, session_id, description.
3. "update_source enum is valid" — assert each entry's update_source is one of ["generate", "debug", "plan-promote", "manual"].
4. "version increments on update" — create two registry snapshots where version goes 1 → 2; assert second > first.

These tests operate on in-memory JSON fixtures (no external tool call). They will PASS immediately (schema-only tests on fixtures). This is acceptable — the model-registry.test.cjs tests the data contract, not a script.
  </action>
  <verify>
```
node --test /Users/jonathanborduas/code/QGSD/bin/initialize-model-registry.test.cjs 2>&1 | tail -5
node --test /Users/jonathanborduas/code/QGSD/bin/model-registry.test.cjs 2>&1 | tail -5
```
initialize-model-registry.test.cjs exits non-zero (RED — tool doesn't exist yet).
model-registry.test.cjs exits zero (GREEN — fixture-based schema tests pass).
  </verify>
  <done>Both files exist. initialize-model-registry.test.cjs has 3 test cases that fail. model-registry.test.cjs has 4 test cases that pass (schema fixture tests).</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test stubs for promote-model and accept-debug-invariant</name>
  <files>bin/promote-model.test.cjs</files>
  <files>bin/accept-debug-invariant.test.cjs</files>
  <action>
Create two test files using the same `node:test` + `spawnSync` pattern.

**bin/promote-model.test.cjs** — 6 tests covering ARCH-02 behaviors:
1. "merges single PROPERTY into target spec" — create temp dir with a minimal .tla target spec (containing `PROPERTY Existing == TRUE`) and a proposed-changes.tla (containing `PROPERTY NewInvariant == TRUE`); run promote-model.cjs; assert target spec now contains both PROPERTY lines.
2. "merges multiple PROPERTY definitions" — same pattern, proposed file has 3 PROPERTY blocks.
3. "rejects duplicate PROPERTY name" — proposed-changes.tla contains `PROPERTY Existing` which also exists in target; assert exit code non-zero and stderr contains "duplicate" or "conflict".
4. "target spec is atomically renamed (tmp file removed)" — after run, assert no `*.tmp.*` files remain in .formal/tla/.
5. "registry version increments after promotion" — read registry before and after; assert model entry version increased by 1.
6. "source_id is recorded in registry" — run with `--source-id v0.21-01-test`; assert registry entry has `source_id: "v0.21-01-test"`.

spawnSync call: `spawnSync(process.execPath, [TOOL_PATH, proposedPath, targetPath, '--source-id', 'v0.21-01-test'], { cwd: tmpDir, encoding: 'utf8', timeout: 10000 })`. Tool doesn't exist — all tests RED (MODULE_NOT_FOUND exit).

**bin/accept-debug-invariant.test.cjs** — 5 tests covering ARCH-03 behaviors:
1. "writes PROPERTY to spec that previously had none" — create empty .tla; run tool with property name "DebugInv1" and body "x > 0"; assert spec now contains `PROPERTY DebugInv1 == x > 0`.
2. "rejects write if property name already exists in spec" — spec has `PROPERTY DebugInv1`; run same; assert exit non-zero.
3. "tmp file removed after atomic rename" — after successful run, no `*.tmp.*` in .formal/tla/.
4. "session_id recorded in registry" — run with `--session-id debug-sess-1234567890-abc12345`; read registry; assert `session_id === "debug-sess-1234567890-abc12345"`.
5. "update_source is debug in registry" — same run; assert registry entry `update_source === "debug"`.

All tests RED at this point (tool absent).
  </action>
  <verify>
```
node --test /Users/jonathanborduas/code/QGSD/bin/promote-model.test.cjs 2>&1 | tail -5
node --test /Users/jonathanborduas/code/QGSD/bin/accept-debug-invariant.test.cjs 2>&1 | tail -5
```
Both exit non-zero (RED). Test output shows test count matching expected (6 and 5 respectively).
  </verify>
  <done>Both files exist with the correct number of test cases. All tests fail (RED) because tool files are absent. Running the full VALIDATION.md suite exits non-zero.</done>
</task>

</tasks>

<verification>
Run full Wave 0 suite:
```
node --test bin/initialize-model-registry.test.cjs bin/promote-model.test.cjs bin/accept-debug-invariant.test.cjs bin/model-registry.test.cjs 2>&1 | tail -10
```
Expected: 3 test files exit non-zero (RED). model-registry.test.cjs exits zero (fixture tests pass). Overall suite exits non-zero.
</verification>

<success_criteria>
Four test files exist in bin/. Running the Wave 0 suite produces RED failures for initialize-model-registry, promote-model, and accept-debug-invariant tests (implementation files absent). model-registry schema tests pass. VALIDATION.md Wave 0 checklist satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/v0.21-01-central-model-registry/v0.21-01-01-SUMMARY.md`
</output>
