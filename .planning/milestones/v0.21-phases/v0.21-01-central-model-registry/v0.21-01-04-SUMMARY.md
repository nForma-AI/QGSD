---
plan: v0.21-01-04
status: complete
completed: 2026-03-01
---

# v0.21-01-04 Summary: ARCH-03 accept-debug-invariant.cjs

## Objective Achieved
Implemented `accept-debug-invariant.cjs` — the debug write path for ARCH-03. Debug discoveries are now captured directly in canonical formal specs with full provenance tracking.

## What Was Built

### Files Created
- `bin/accept-debug-invariant.cjs` — 145-line CLI tool for debug invariant writes

## Key Behaviors
- CLI: `node bin/accept-debug-invariant.cjs <spec.tla> --property-name <name> --property-body <body> --session-id <id>`
- Validates `--session-id` at parse time (before file I/O): must match `/^debug-sess-\d+-[0-9a-f]{8}$/`
- Missing `--session-id`: exit 1 with descriptive error (FV traceability contract)
- Malformed `--session-id`: exit 1 with format example
- Spec not found: exit 1
- Duplicate PROPERTY name in target: exit 1
- Appends PROPERTY with debug session comment before `====` end marker (or at end)
- Writes spec atomically: tmp file + `fs.renameSync`
- Updates registry: update_source=debug, session_id recorded, version++, atomic tmp+rename
- Fail-open: missing registry produces warning, not failure

## Test Results
- `node --test bin/accept-debug-invariant.test.cjs`: 5/5 GREEN
- Full phase suite: 18/18 GREEN (initialize-model-registry + promote-model + accept-debug-invariant + model-registry)

## Integration Verified
- PROPERTY written to canonical spec with debug comment `(* DEBUG SESSION: <id> *)`
- Registry records update_source=debug and session_id
- Duplicate name rejected at spec-read time (exit 1)
- Malformed session-id rejected at parse time (exit 1, no file I/O)
- No tmp files remain after operation

## Self-Check: PASSED
- bin/accept-debug-invariant.cjs exists (145 lines)
- 5/5 accept-debug-invariant tests GREEN
- 18/18 full phase test suite GREEN
- All ARCH-03 requirements satisfied
