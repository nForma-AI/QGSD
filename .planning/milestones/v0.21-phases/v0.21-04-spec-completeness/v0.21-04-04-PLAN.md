---
phase: v0.21-04
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/generate-phase-spec.cjs
  - bin/generate-phase-spec.test.cjs
  - formal/tla/scratch/.gitkeep
autonomous: true
requirements: [SPEC-04]

must_haves:
  truths:
    - "bin/generate-phase-spec.cjs reads must_haves: truths: from YAML frontmatter of *-PLAN.md files (NOT from task-envelope.json, which has empty truths at planning time) and translates them into a TLA+ MODULE with INVARIANT/PROPERTY stubs"
    - "The CLI accepts a phase plan file path or phase directory as input, iterates over *-PLAN.md files, extracts must_haves.truths arrays from YAML frontmatter, and emits one scratch TLA+ spec per phase"
    - "The generated scratch spec is written to formal/tla/scratch/<phase>-<timestamp>.tla"
    - "Each truth becomes either INVARIANT (for safety keywords: threshold, depth, count, max, never, always) or PROPERTY (for liveness keywords: eventually, deadline, async, progress, future)"
    - "node --test bin/generate-phase-spec.test.cjs exits 0: YAML frontmatter parsing, TLA+ generation, file-write, and truth-classification tests pass"
    - "Running generate-phase-spec.cjs on the current phase directory produces a parseable TLA+ file with correct MODULE header from the plan frontmatter truths"
    - "formal/tla/scratch/ directory exists (created by script if missing)"
  artifacts:
    - path: "bin/generate-phase-spec.cjs"
      provides: "CLI tool: reads must_haves.truths from *-PLAN.md YAML frontmatter (NOT task-envelope.json), outputs scratch TLA+ spec with truths as INVARIANT/PROPERTY stubs"
      min_lines: 80
      exports: ["generatePhaseSpec"]
    - path: "bin/generate-phase-spec.test.cjs"
      provides: "Unit tests for schema parsing and TLA+ generation"
      min_lines: 60
    - path: "formal/tla/scratch/.gitkeep"
      provides: "Ensures formal/tla/scratch/ directory is tracked in git"
  key_links:
    - from: "bin/generate-phase-spec.cjs"
      to: "formal/tla/scratch/<phase>-<timestamp>.tla"
      via: "fs.writeFileSync after generating specContent string"
      pattern: "formal/tla/scratch"
    - from: ".planning/phases/<phase>/*-PLAN.md"
      to: "bin/generate-phase-spec.cjs"
      via: "CLI argument: node bin/generate-phase-spec.cjs .planning/phases/v0.21-04-spec-completeness/ — iterates *-PLAN.md files, extracts must_haves.truths from YAML frontmatter"
      pattern: "PLAN.md"
---

<objective>
Write bin/generate-phase-spec.cjs that reads must_haves: truths: from YAML frontmatter in *-PLAN.md files and translates them into a per-phase TLA+ scratch spec with INVARIANT/PROPERTY stubs (SPEC-04). The CLI accepts a phase directory (or single plan file), iterates over all *-PLAN.md files, parses their YAML frontmatter, and combines all truths into one scratch spec. Create formal/tla/scratch/ directory. Write unit tests that validate YAML frontmatter parsing, truth classification, and TLA+ generation.

Purpose: SPEC-04 makes plan requirements machine-checkable — every planned truth becomes a TLA+ property that TLC can verify. The correct data source is the *-PLAN.md frontmatter (task-envelope.json truths are empty at planning time). This closes the loop between planning (what must be true) and formal verification (TLC proves it). The scratch spec becomes the mathematical contract for each phase.
Output: bin/generate-phase-spec.cjs (exportable), bin/generate-phase-spec.test.cjs, formal/tla/scratch/.gitkeep.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.21-04-spec-completeness/v0.21-04-RESEARCH.md
@.planning/phases/v0.21-04-spec-completeness/v0.21-04-01-PLAN.md
@formal/tla/QGSDOscillation.tla
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/generate-phase-spec.cjs and formal/tla/scratch/ directory</name>
  <files>bin/generate-phase-spec.cjs, formal/tla/scratch/.gitkeep</files>
  <action>
Create `formal/tla/scratch/` directory with a `.gitkeep` file so the directory is tracked in git:
```bash
mkdir -p formal/tla/scratch && touch formal/tla/scratch/.gitkeep
```

Create `bin/generate-phase-spec.cjs` as a CLI tool (and importable module). Use Node.js built-ins only (fs, path, child_process).

**IMPORTANT: Data source is *-PLAN.md YAML frontmatter, NOT task-envelope.json.**
The `task-envelope.json` truths field is empty at planning time (`"key_decisions": []` only). The correct source is `must_haves.truths` from the YAML frontmatter in each `*-PLAN.md` file.

**CLI usage:**
```
node bin/generate-phase-spec.cjs <phase-dir-or-plan-file> [--dry-run]
```
Examples:
```
node bin/generate-phase-spec.cjs .planning/phases/v0.21-04-spec-completeness/
node bin/generate-phase-spec.cjs .planning/phases/v0.21-04-spec-completeness/v0.21-04-01-PLAN.md
```

**Module exports:**
```js
module.exports = { generatePhaseSpec, classifyTruth, parsePlanFrontmatter };
```

**Implementation:**

```js
#!/usr/bin/env node
'use strict';
// bin/generate-phase-spec.cjs
// SPEC-04: Reads must_haves: truths: from *-PLAN.md YAML frontmatter and translates
// them into per-phase TLA+ scratch spec with INVARIANT/PROPERTY stubs.
//
// Data source: YAML frontmatter in *-PLAN.md files (NOT task-envelope.json — truths
// are empty in task-envelope.json at planning time; plan frontmatter is the correct source).
//
// Usage:
//   node bin/generate-phase-spec.cjs .planning/phases/v0.21-04-spec-completeness/
//   node bin/generate-phase-spec.cjs .planning/phases/v0.21-04-spec-completeness/v0.21-04-01-PLAN.md
//   node bin/generate-phase-spec.cjs <phase-dir-or-plan-file> --dry-run
//
// Output: formal/tla/scratch/<phase>-<timestamp>.tla

const fs = require('fs');
const path = require('path');

const ROOT = path.join(__dirname, '..');
const SCRATCH_DIR = path.join(ROOT, 'formal', 'tla', 'scratch');

// Parse YAML frontmatter from a PLAN.md file.
// Returns the frontmatter object (parsed manually — no yaml dependency).
// Frontmatter is between the first two '---' lines.
function parsePlanFrontmatter(content) {
  const lines = content.split('\n');
  if (lines[0].trim() !== '---') return {};
  const endIdx = lines.slice(1).findIndex(l => l.trim() === '---');
  if (endIdx === -1) return {};
  const fmLines = lines.slice(1, endIdx + 1);
  // Minimal YAML parser: extracts must_haves.truths array only.
  // Looks for 'must_haves:' block, then 'truths:' key, then collects '- "..."' entries.
  const result = {};
  let inMustHaves = false;
  let inTruths = false;
  const truths = [];
  for (const line of fmLines) {
    if (/^must_haves:/.test(line)) { inMustHaves = true; inTruths = false; continue; }
    if (inMustHaves && /^\s{2}truths:/.test(line)) { inTruths = true; continue; }
    if (inMustHaves && inTruths && /^\s{4}-\s+"?(.+)"?\s*$/.test(line)) {
      const m = line.match(/^\s{4}-\s+"?(.+?)"?\s*$/);
      if (m) truths.push(m[1].replace(/\\"/g, '"'));
      continue;
    }
    // Stop collecting truths when indentation drops
    if (inTruths && !/^\s{4}/.test(line) && line.trim() !== '') { inTruths = false; }
    if (inMustHaves && !/^\s{2,}/.test(line) && line.trim() !== '') { inMustHaves = false; }
    // Extract phase from frontmatter
    const phaseMatch = line.match(/^phase:\s*(.+)/);
    if (phaseMatch) result.phase = phaseMatch[1].replace(/['"]/g, '').trim();
  }
  result.truths = truths;
  return result;
}

// Collect all *-PLAN.md files from a directory or single file path.
function collectPlanFiles(inputPath) {
  const resolved = path.resolve(inputPath);
  const stat = fs.statSync(resolved);
  if (stat.isFile()) return [resolved];
  // Directory: glob for *-PLAN.md files
  return fs.readdirSync(resolved)
    .filter(f => f.endsWith('-PLAN.md'))
    .sort()
    .map(f => path.join(resolved, f));
}

// Classify a truth string as safety (INVARIANT) or liveness (PROPERTY)
// Safety keywords: threshold, depth, count, max, min, never, always, exactly, must, blocked, only
// Liveness keywords: eventually, deadline, async, progress, future, when, after, until, leads-to
function classifyTruth(truth) {
  const lower = truth.toLowerCase();
  const livenessKeywords = ['eventually', 'deadline', 'async', 'progress', 'future', 'when.*then', 'after', 'until', 'leads-to'];
  const isLiveness = livenessKeywords.some(kw => {
    if (kw.includes('.*')) return new RegExp(kw).test(lower);
    return lower.includes(kw);
  });
  return isLiveness ? 'PROPERTY' : 'INVARIANT';
}

// Generate TLA+ module content from collected truths
function generatePhaseSpec({ phase, truths }) {
  const allTruths = truths || [];

  // TLA+ module name: sanitize phase string
  const moduleName = 'Phase' + (phase || 'unknown').replace(/[^a-zA-Z0-9]/g, '_') + 'Spec';
  const timestamp = new Date().toISOString();

  let spec = `---- MODULE ${moduleName} ----
(* Source: must_haves: truths: from *-PLAN.md YAML frontmatter *)
(* Generated by bin/generate-phase-spec.cjs — SPEC-04 *)
(* Phase: ${phase || 'unknown'} *)
(* Generated: ${timestamp} *)
(* Purpose: Verify proposed state machine changes before quorum approval *)
(* PLACEHOLDER properties — developer fills in formal logic before TLC run *)

EXTENDS Naturals, FiniteSets, TLC

VARIABLES state, counter

TypeOK == state \\in {"INIT", "RUNNING", "DONE"}

Init == state = "INIT" /\\ counter = 0

Next ==
    \\/ /\\ state = "INIT" /\\ state' = "RUNNING" /\\ counter' = counter + 1
    \\/ /\\ state = "RUNNING" /\\ state' = "DONE" /\\ counter' = counter
    \\/ /\\ state = "DONE" /\\ UNCHANGED <<state, counter>>

Spec == Init /\\ [][Next]_<<state, counter>>

\\* ── Requirements as Properties ──────────────────────────────────

`;

  if (allTruths.length === 0) {
    spec += `\\* No must_haves: truths: found in *-PLAN.md frontmatter
\\* Add truths to see them translated to INVARIANT/PROPERTY stubs

`;
  }

  allTruths.forEach((truth, idx) => {
    const kind = classifyTruth(truth);
    const propName = `Req${String(idx + 1).padStart(2, '0')}`;
    const label = truth.substring(0, 100).replace(/\\/g, '\\\\').replace(/"/g, '\\"');
    spec += `${kind} ${propName} ==
  \\* "${label}${truth.length > 100 ? '...' : ''}"
  TRUE  \\* PLACEHOLDER: replace TRUE with formal TLA+ expression

`;
  });

  spec += `====`;
  return { moduleName, spec, truthCount: allTruths.length };
}

// CLI entrypoint
if (require.main === module) {
  const args = process.argv.slice(2).filter(a => !a.startsWith('--'));
  const flags = process.argv.slice(2).filter(a => a.startsWith('--'));
  const isDryRun = flags.includes('--dry-run');

  if (args.length === 0) {
    process.stderr.write('[generate-phase-spec] Usage: node bin/generate-phase-spec.cjs <phase-dir-or-PLAN.md> [--dry-run]\n');
    process.stderr.write('[generate-phase-spec] Reads must_haves: truths: from *-PLAN.md YAML frontmatter (NOT task-envelope.json)\n');
    process.exit(1);
  }

  const inputPath = path.resolve(args[0]);
  if (!fs.existsSync(inputPath)) {
    process.stderr.write('[generate-phase-spec] Error: path not found: ' + inputPath + '\n');
    process.exit(1);
  }

  const planFiles = collectPlanFiles(inputPath);
  if (planFiles.length === 0) {
    process.stderr.write('[generate-phase-spec] Error: no *-PLAN.md files found in ' + inputPath + '\n');
    process.exit(1);
  }

  // Collect truths from all plan files
  let phase = 'unknown';
  const allTruths = [];
  for (const planFile of planFiles) {
    const content = fs.readFileSync(planFile, 'utf8');
    const fm = parsePlanFrontmatter(content);
    if (fm.phase) phase = fm.phase;
    if (fm.truths && fm.truths.length > 0) allTruths.push(...fm.truths);
  }

  const { moduleName, spec, truthCount } = generatePhaseSpec({ phase, truths: allTruths });
  const ts = Date.now();
  const outName = phase.replace(/[^a-zA-Z0-9-]/g, '_') + '-' + ts + '.tla';
  const outPath = path.join(SCRATCH_DIR, outName);

  if (isDryRun) {
    process.stdout.write('[generate-phase-spec] DRY-RUN: would write ' + outPath + '\n');
    process.stdout.write('[generate-phase-spec] Plan files: ' + planFiles.join(', ') + '\n');
    process.stdout.write('[generate-phase-spec] Truths collected: ' + truthCount + '\n');
    process.stdout.write(spec + '\n');
  } else {
    fs.mkdirSync(SCRATCH_DIR, { recursive: true });
    fs.writeFileSync(outPath, spec, 'utf8');
    process.stdout.write('[generate-phase-spec] Written: ' + outPath + ' (' + truthCount + ' truths from ' + planFiles.length + ' plan files → INVARIANT/PROPERTY stubs)\n');
  }
}

module.exports = { generatePhaseSpec, classifyTruth, parsePlanFrontmatter };
```

After writing the file, verify the module parses cleanly and the classifyTruth function correctly classifies:
- "eventually passes" → PROPERTY
- "threshold is respected" → INVARIANT
- "count must be > 0" → INVARIANT
- "deadline met" → PROPERTY
  </action>
  <verify>
```bash
cd /Users/jonathanborduas/code/QGSD
node -e "const {classifyTruth} = require('./bin/generate-phase-spec.cjs'); console.log(classifyTruth('eventually passes'), classifyTruth('threshold must be respected'))"
# Expected: PROPERTY INVARIANT
node bin/generate-phase-spec.cjs .planning/phases/v0.21-04-spec-completeness/ --dry-run
ls formal/tla/scratch/.gitkeep
```
classifyTruth returns PROPERTY for "eventually" and INVARIANT for "threshold". Dry-run prints TLA+ spec with MODULE header (truths extracted from *-PLAN.md frontmatter). .gitkeep exists.
  </verify>
  <done>bin/generate-phase-spec.cjs exists and parses without error. classifyTruth returns correct PROPERTY/INVARIANT for sample truths. --dry-run on phase directory reads *-PLAN.md frontmatter and prints a valid TLA+ spec with MODULE declaration and non-empty INVARIANT/PROPERTY stubs. formal/tla/scratch/.gitkeep exists.</done>
</task>

<task type="auto">
  <name>Task 2: Write bin/generate-phase-spec.test.cjs</name>
  <files>bin/generate-phase-spec.test.cjs</files>
  <action>
Create `bin/generate-phase-spec.test.cjs` using node:test framework (match the import/require pattern from existing tests like bin/run-alloy.test.cjs — read that file to confirm).

**Tests to write:**

1. **"classifyTruth — liveness keywords produce PROPERTY"**
   - Input: "eventually decision is PASS" → expect "PROPERTY"
   - Input: "progress guaranteed after quorum" → expect "PROPERTY"
   - Input: "system terminates after deadline" → expect "PROPERTY"

2. **"classifyTruth — safety keywords produce INVARIANT"**
   - Input: "threshold must be 3 or greater" → expect "INVARIANT"
   - Input: "count never exceeds maxSize" → expect "INVARIANT"
   - Input: "depth is always positive" → expect "INVARIANT"
   - Input: "blocked when no evidence" → expect "INVARIANT"

3. **"parsePlanFrontmatter — extracts must_haves.truths from PLAN.md content"**
   - Construct a synthetic PLAN.md string with YAML frontmatter including must_haves.truths array
   - Call parsePlanFrontmatter(content)
   - Assert returned object has truths array matching the expected values
   - Assert phase field is correctly extracted

4. **"generatePhaseSpec — produces valid TLA+ MODULE header"**
   - Input: `{ phase: 'test-42', truths: ['threshold > 0', 'eventually done'] }`
   - Assert spec contains `---- MODULE Phasetest_42Spec ----`
   - Assert spec contains `EXTENDS Naturals`
   - Assert spec contains `====` at end

5. **"generatePhaseSpec — each truth becomes INVARIANT or PROPERTY stub"**
   - Input: `{ phase: 'x', truths: ['threshold > 0', 'eventually done'] }`
   - Assert spec contains `INVARIANT Req01`
   - Assert spec contains `PROPERTY Req02`
   - Assert both contain `TRUE  \\* PLACEHOLDER`

6. **"generatePhaseSpec — empty truths produces spec with no INVARIANT/PROPERTY"**
   - Input: `{ phase: 'empty', truths: [] }`
   - Assert spec contains `No must_haves: truths: found` comment
   - Assert spec does NOT contain `INVARIANT Req`

7. **"generatePhaseSpec — current phase *-PLAN.md frontmatter produces parseable output"**
   - Load `.planning/phases/v0.21-04-spec-completeness/v0.21-04-01-PLAN.md` with fs.readFileSync
   - Call parsePlanFrontmatter(content), then generatePhaseSpec with extracted { phase, truths }
   - Assert spec contains `---- MODULE`
   - Assert spec ends with `====`
   - Assert truthCount > 0 (plan frontmatter has truths, unlike task-envelope.json)

8. **"formal/tla/scratch/ directory exists after running CLI on phase directory"**
   - spawnSync `node bin/generate-phase-spec.cjs .planning/phases/v0.21-04-spec-completeness/`
   - Assert status === 0
   - Assert fs.existsSync('formal/tla/scratch') is true

Use `assert` from node:assert for all assertions. Use `describe`/`test` or just `test` blocks per existing project pattern.
  </action>
  <verify>
```bash
cd /Users/jonathanborduas/code/QGSD
node --test bin/generate-phase-spec.test.cjs 2>&1 | tail -15
```
Test exits 0 with all 8 tests passing (or 7+ if scratch/ dir test has minor path issue). Output shows test names and "pass" markers.
  </verify>
  <done>node --test bin/generate-phase-spec.test.cjs exits 0. All 8 tests pass: classifyTruth classification correct, parsePlanFrontmatter correctly extracts truths from YAML frontmatter, generatePhaseSpec module output correct, current phase *-PLAN.md frontmatter produces valid spec with non-zero truthCount, CLI creates formal/tla/scratch/ directory.</done>
</task>

</tasks>

<verification>
Full plan validation:
```bash
cd /Users/jonathanborduas/code/QGSD
# Module parses and classifies correctly
node -e "
  const {classifyTruth, generatePhaseSpec, parsePlanFrontmatter} = require('./bin/generate-phase-spec.cjs');
  console.assert(classifyTruth('eventually done') === 'PROPERTY');
  console.assert(classifyTruth('threshold > 0') === 'INVARIANT');
  const {spec} = generatePhaseSpec({phase:'v0.21-04', truths:['threshold > 0','eventually done']});
  console.assert(spec.includes('INVARIANT Req01'));
  console.assert(spec.includes('PROPERTY Req02'));
  // Verify parsePlanFrontmatter reads real plan file
  const fs = require('fs');
  const content = fs.readFileSync('.planning/phases/v0.21-04-spec-completeness/v0.21-04-01-PLAN.md','utf8');
  const fm = parsePlanFrontmatter(content);
  console.assert(fm.truths.length > 0, 'plan frontmatter must have truths (unlike task-envelope.json)');
  console.log('All assertions passed');
"
# Tests pass
node --test bin/generate-phase-spec.test.cjs 2>&1 | tail -8
# Scratch dir created (from CLI on phase directory)
node bin/generate-phase-spec.cjs .planning/phases/v0.21-04-spec-completeness/ --dry-run
ls formal/tla/scratch/
```
</verification>

<success_criteria>
1. bin/generate-phase-spec.cjs exists, exports generatePhaseSpec + classifyTruth + parsePlanFrontmatter, exits 0 on valid input
2. classifyTruth returns PROPERTY for liveness keywords, INVARIANT for safety keywords
3. parsePlanFrontmatter correctly extracts must_haves.truths from *-PLAN.md YAML frontmatter (NOT task-envelope.json)
4. generatePhaseSpec produces TLA+ with correct MODULE header, INVARIANT/PROPERTY stubs for each truth, PLACEHOLDER comment
5. CLI accepts a phase directory, iterates *-PLAN.md files, and combines all truths
6. formal/tla/scratch/ directory exists with .gitkeep
7. node --test bin/generate-phase-spec.test.cjs exits 0 (8/8 tests pass)
8. Running generate-phase-spec.cjs on the phase directory reads *-PLAN.md frontmatter and produces a .tla file in formal/tla/scratch/ with non-zero truth count
</success_criteria>

<output>
After completion, create `.planning/phases/v0.21-04-spec-completeness/v0.21-04-04-SUMMARY.md`
</output>
