# v0.21-05-01 Summary: TLA+ Delta Synthesizer

## Status: COMPLETE

## What was built
Created `bin/generate-proposed-changes.cjs` -- reads a PLAN.md file's `must_haves: truths:` block and generates a `proposed-changes.tla` scratch spec with INVARIANT/PROPERTY stubs plus `MCProposedChanges.cfg` for TLC invocation. Output is scoped to `{phaseDir}/formal/` (phase-scoped, NOT `formal/tla/scratch/`).

## Key decisions
- Reuses `parsePlanFrontmatter` and `classifyTruth` from `generate-phase-spec.cjs` via `require()` -- no duplication
- Zero-padded `ReqNN` naming scheme (Req01, Req02, ...) for bidirectional traceability with PLAN-02's `formatTlcFeedback`
- ReqNN-to-Truth index mapping comment block in generated spec for manual audit
- `fs.mkdirSync(formalDir, { recursive: true })` prevents ENOENT during parallel executions

## Test results
8/8 tests GREEN (130ms)

## Key files

### Created
- `bin/generate-proposed-changes.cjs` -- TLA+ delta synthesis from PLAN.md frontmatter truths
- `bin/generate-proposed-changes.test.cjs` -- 8 unit tests

### Modified
None

## Self-Check: PASSED
- [x] All 8 tests pass
- [x] Exports: generateProposedChanges, generateTlaCfg
- [x] Reuses parsePlanFrontmatter/classifyTruth from generate-phase-spec.cjs
- [x] Output scoped to phaseDir/formal/ (not formal/tla/scratch/)
- [x] ReqNN-to-Truth mapping block present in generated spec
- [x] formal/ auto-created via mkdir -p
