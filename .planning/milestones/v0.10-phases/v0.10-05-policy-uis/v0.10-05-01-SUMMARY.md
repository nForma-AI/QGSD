---
phase: v0.10-05-policy-uis
plan: "01"
subsystem: manage-agents
tags: [tdd, test-stubs, red-phase, pure-functions, policy-ui]
dependency_graph:
  requires: []
  provides: [v0.10-05-02-PLAN.md]
  affects: [bin/manage-agents.test.cjs]
tech_stack:
  added: []
  patterns: [TDD RED-GREEN-REFACTOR, destructured _pure import]
key_files:
  created: []
  modified:
    - bin/manage-agents.test.cjs
decisions:
  - Plan stated 22 failing stubs; actual stubs in the action block total 24 (4+4+4+5+7). The plan body is authoritative — 24 stubs were added, all fail with TypeError. Documentation discrepancy noted.
metrics:
  duration: 2 min
  completed: 2026-02-24
  tasks_completed: 1
  files_modified: 1
---

# Phase v0.10-05 Plan 01: TDD RED stubs for 5 new pure functions Summary

**One-liner:** Added 24 failing RED stubs covering buildTimeoutChoices, applyTimeoutUpdate, buildPolicyChoices, buildUpdateLogEntry, and parseUpdateLogErrors — all failing with TypeError since functions are not yet exported from _pure.

## What Was Built

Extended `bin/manage-agents.test.cjs` with:

1. **Updated destructured import** (line 9) — added 5 new function names: `buildTimeoutChoices`, `applyTimeoutUpdate`, `buildPolicyChoices`, `buildUpdateLogEntry`, `parseUpdateLogErrors`

2. **24 new failing test stubs** organized by function:
   - `buildTimeoutChoices`: 4 stubs covering quorum_timeout_ms lookup, timeout_ms fallback, missing provider (null), and PROVIDER_SLOT env derivation
   - `applyTimeoutUpdate`: 4 stubs covering successful update, immutability contract, non-target preservation, and no-op for missing slot
   - `buildPolicyChoices`: 4 stubs covering 3-choice set, current-marker annotation, non-current no-marker, and null policy no-marker
   - `buildUpdateLogEntry`: 5 stubs covering string+newline return, JSON field correctness, ISO 8601 ts, null detail, status verbatim
   - `parseUpdateLogErrors`: 7 stubs covering null/empty/non-ERROR/age-filter/window-inclusion/malformed-JSON/default-24h-window

## Test Results

```
node --test bin/manage-agents.test.cjs 2>&1 | grep -E "^ℹ (pass|fail)"
ℹ pass 94
ℹ fail 24
```

RED state confirmed: all 24 new stubs fail with `TypeError: <fn> is not a function`. Existing 94 tests unaffected.

## Decisions Made

- **Plan count discrepancy**: Plan frontmatter stated 22 stubs; the actual `<action>` block contained 24 stubs. Applied all 24 stubs as written — the implementation block is authoritative over the count stated in frontmatter.
- **doesNotThrow failure mode**: `parseUpdateLogErrors: skips malformed JSON lines without throwing` fails with `AssertionError` (not plain TypeError) because `assert.doesNotThrow` wraps the TypeError into an assertion failure. This is correct RED behavior — the test fails, which is the intent.

## Deviations from Plan

### Plan Count Discrepancy (Documentation Only)

**Found during:** Task 1

**Issue:** PLAN.md frontmatter and objective state "22 failing test stubs" but the `<action>` block contains 24 stubs (4+4+4+5+7=24).

**Fix:** Applied all 24 stubs from the action block. The code specification (the stubs themselves) is authoritative; the count in prose documentation is advisory.

**Files modified:** None beyond the planned modification to `bin/manage-agents.test.cjs`

**Commit:** c8d560d

## Self-Check: PASSED

- `bin/manage-agents.test.cjs` modified: FOUND
- Commit c8d560d: FOUND
- pass 94, fail 24: CONFIRMED
