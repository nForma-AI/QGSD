---
phase: v0.10-07-retroactive-verification-closure
plan: "01"
status: complete
completed: 2026-02-25T00:00:00Z
requirements-affected:
  - DASH-01
  - DASH-02
  - DASH-03
commits:
  - b4e1e3d
---

# Plan 01 Summary — Wave 0: RED stubs for probeAllSlots + liveDashboard

## What Was Done

Added 4 failing test stubs to `bin/manage-agents.test.cjs` that document the RED state before `probeAllSlots` and `liveDashboard` are exported via `_pure`. This creates a traceable audit trail that the gap existed and was deliberately closed.

## Test State After This Plan

- Prior tests: 118 pass (unchanged)
- New stubs: 4 fail with `TypeError: probeAllSlots is not a function` / `TypeError: liveDashboard is not a function`
- Total: 122 tests, 118 pass, 4 fail

## Stubs Added

1. `probeAllSlots: subprocess slots return healthy:null without HTTP probe`
2. `probeAllSlots: empty slots returns empty object`
3. `probeAllSlots: returns one entry per slot`
4. `liveDashboard smoke: non-TTY path resolves without throwing`

Each stub destructures `probeAllSlots` or `liveDashboard` from `_pure` inside the test body, scoping the failure to that test without affecting the other 118.

## Commit

- `b4e1e3d` — test(v0.10-07-01): add 4 RED stubs for probeAllSlots + liveDashboard _pure exports
