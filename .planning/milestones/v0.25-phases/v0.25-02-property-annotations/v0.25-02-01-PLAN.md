---
phase: v0.25-02-property-annotations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .formal/tla/QGSDQuorum.tla
  - .formal/tla/QGSDCircuitBreaker.tla
  - .formal/tla/QGSDOscillation.tla
  - .formal/tla/QGSDConvergence.tla
  - .formal/tla/QGSDDeliberation.tla
  - .formal/tla/QGSDPreFilter.tla
  - .formal/tla/QGSDAccountManager.tla
  - .formal/tla/QGSDMCPEnv.tla
  - .formal/tla/QGSDStopHook.tla
  - .formal/tla/QGSDRecruiting.tla
  - .formal/tla/TUINavigation.tla
autonomous: true
requirements:
  - ANNOT-01
formal_artifacts: none

must_haves:
  truths:
    - "Every TLA+ invariant and temporal property in all 11 TLA+ model files has a `\\* @requirement REQ-ID` comment immediately preceding its definition"
    - "Each `@requirement` annotation uses the exact format `\\* @requirement REQ-ID` with one annotation per line"
    - "Annotations are placed in the comment block directly above the property definition with no blank lines separating them"
    - "Generated files (QGSDQuorum.tla, QGSDQuorum_xstate.tla) have annotations added with a note that they are not generated and should be preserved"
    - "TTrace files are NOT annotated (only the 11 main model files)"
    - "Running `grep -c '@requirement' .formal/tla/QGSDStopHook.tla` returns at least 8 (one per property, some properties have multiple requirement IDs)"
  artifacts:
    - path: ".formal/tla/QGSDQuorum.tla"
      provides: "@requirement annotations for 7 properties"
      contains: "@requirement QUORUM"
    - path: ".formal/tla/QGSDStopHook.tla"
      provides: "@requirement annotations for 7 properties"
      contains: "@requirement STOP"
    - path: ".formal/tla/QGSDCircuitBreaker.tla"
      provides: "@requirement annotations for 3 properties"
      contains: "@requirement DETECT"
  key_links:
    - from: ".formal/tla/*.tla"
      to: ".planning/research/TRACEABILITY_RESEARCH.md"
      via: "Property-to-requirement mapping from Section 5.1"
      pattern: "@requirement \\w+-\\d+"
---

<objective>
Add structured `\* @requirement REQ-ID` annotations to every property/invariant in all 11 TLA+ model files.

Purpose: ANNOT-01 requires machine-parseable requirement annotations on all TLA+ properties. The property-to-requirement mapping from TRACEABILITY_RESEARCH.md Section 5.1 provides the authoritative source for which requirement ID(s) each property covers.
Output: All 11 TLA+ model files annotated with ~52 `@requirement` comments across ~52 properties.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.25-02-property-annotations/v0.25-02-RESEARCH.md
@.planning/research/TRACEABILITY_RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate handwritten TLA+ models (8 files)</name>
  <files>
.formal/tla/QGSDCircuitBreaker.tla
.formal/tla/QGSDOscillation.tla
.formal/tla/QGSDConvergence.tla
.formal/tla/QGSDDeliberation.tla
.formal/tla/QGSDPreFilter.tla
.formal/tla/QGSDAccountManager.tla
.formal/tla/QGSDStopHook.tla
.formal/tla/QGSDRecruiting.tla
  </files>
  <action>
For each of the 8 handwritten TLA+ model files, add `\* @requirement REQ-ID` annotation comments immediately before each property/invariant definition. Place one `\* @requirement` line per requirement ID. The annotation must be in the comment block directly above the property definition (the `PropertyName ==` line) with no blank lines between annotation and definition.

Use this exact mapping from TRACEABILITY_RESEARCH.md Section 5.1:

**QGSDCircuitBreaker.tla:**
- TypeOK: `\* @requirement DETECT-01`
- DisabledExcludesActive: `\* @requirement DETECT-02`
- MonitoringReachable: `\* @requirement DETECT-03`

**QGSDOscillation.tla:**
- TypeOK: `\* @requirement DETECT-04`
- OscillationFlaggedCorrectly: `\* @requirement DETECT-05`
- AlgorithmTerminates: `\* @requirement DETECT-06`

**QGSDConvergence.tla:**
- TypeOK: `\* @requirement ORES-01`
- LogBeforeDelete: `\* @requirement ORES-02`
- ResolvedAtWriteOnce: `\* @requirement ORES-03`
- HaikuUnavailableNoCorruption: `\* @requirement ORES-04`
- ConvergenceEventuallyResolves: `\* @requirement ORES-05`

**QGSDDeliberation.tla:**
- TypeOK: `\* @requirement PLAN-01`
- TotalRoundsBounded: `\* @requirement LOOP-02`
- DeliberationMonotone: `\* @requirement SAFE-03`
- ImprovementMonotone: `\* @requirement IMPR-01`
- ProtocolTerminates: `\* @requirement PLAN-02`

**QGSDPreFilter.tla:**
- TypeOK: `\* @requirement PLAN-03`
- AutoResolutionSound: `\* @requirement PLAN-04`
- FilterRoundsBounded: `\* @requirement LOOP-03`
- AutoResolvedPhaseConsistent: `\* @requirement PLAN-05`
- PreFilterTerminates: `\* @requirement PLAN-06`

**QGSDAccountManager.tla:**
- TypeOK: `\* @requirement CRED-01`
- ActiveIsPoolMember: `\* @requirement CRED-02`
- NoActiveWhenEmpty: `\* @requirement CRED-03`
- IdleNoPending: `\* @requirement CRED-04`
- OpMatchesState: `\* @requirement CRED-05`
- IdleReachable: `\* @requirement CRED-06`

**QGSDStopHook.tla:**
- TypeOK: `\* @requirement STOP-01`
- SafetyInvariant1: two lines — `\* @requirement STOP-02` and `\* @requirement SPEC-01`
- SafetyInvariant2: `\* @requirement STOP-03`
- SafetyInvariant3: `\* @requirement STOP-04`
- LivenessProperty1: `\* @requirement STOP-05`
- LivenessProperty2: `\* @requirement STOP-06`
- LivenessProperty3: `\* @requirement STOP-07`

**QGSDRecruiting.tla:**
- TypeOK: `\* @requirement SLOT-02`
- PolledCeiling: `\* @requirement SLOT-03`
- SubslotsFirst: `\* @requirement SLOT-04`
- FullRecruitment: `\* @requirement SLOT-05`

**Placement example for QGSDStopHook.tla SafetyInvariant1:**
```tla
(*
 * SafetyInvariant1: BLOCK can only be decided if a planning command was detected.
 * Prevents false blocks on non-planning turns.
 *)
\* @requirement STOP-02
\* @requirement SPEC-01
SafetyInvariant1 ==
  decision = "BLOCK" => hasCommand
```

IMPORTANT: Preserve all existing comments. The `@requirement` line(s) go AFTER any existing `(* ... *)` comment block but BEFORE the property definition line. Do NOT modify the property formulas themselves.
  </action>
  <verify>
For each file, count annotations:
```bash
for f in .formal/tla/QGSDCircuitBreaker.tla .formal/tla/QGSDOscillation.tla .formal/tla/QGSDConvergence.tla .formal/tla/QGSDDeliberation.tla .formal/tla/QGSDPreFilter.tla .formal/tla/QGSDAccountManager.tla .formal/tla/QGSDStopHook.tla .formal/tla/QGSDRecruiting.tla; do echo "$f: $(grep -c '@requirement' "$f") annotations"; done
```
Expected counts: CircuitBreaker=3, Oscillation=3, Convergence=5, Deliberation=5, PreFilter=5, AccountManager=6, StopHook=8 (7 properties, 1 has 2 req IDs), Recruiting=4. Total: 39.
  </verify>
  <done>
All 8 handwritten TLA+ model files have `\* @requirement REQ-ID` annotations on every property/invariant. Annotations use correct TLA+ comment syntax and are placed directly above each property definition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Annotate remaining TLA+ models (3 files: generated + no-marker)</name>
  <files>
.formal/tla/QGSDQuorum.tla
.formal/tla/QGSDMCPEnv.tla
.formal/tla/TUINavigation.tla
  </files>
  <action>
Add `\* @requirement REQ-ID` annotations to the remaining 3 TLA+ model files. These include one generated file (QGSDQuorum.tla) and two without generation markers (QGSDMCPEnv.tla, TUINavigation.tla).

**QGSDQuorum.tla (GENERATED):**
This file is generated by `bin/generate-formal-specs.cjs`. Add annotations manually and add a comment at the top of the file noting annotations were added post-generation:
```tla
(* NOTE: @requirement annotations added by v0.25-02 (not generated).
 * Preserve annotations when regenerating. *)
```
Properties to annotate:
- TypeOK: `\* @requirement QUORUM-01`
- UnanimityMet: two lines — `\* @requirement QUORUM-02` and `\* @requirement SAFE-01`
- QuorumCeilingMet: two lines — `\* @requirement QUORUM-03` and `\* @requirement SLOT-01`
- DeliberationBounded: `\* @requirement LOOP-01`
- AllTransitionsValid: `\* @requirement SAFE-02`
- DeliberationMonotone: `\* @requirement SAFE-03`
- EventualConsensus: two lines — `\* @requirement QUORUM-04` and `\* @requirement RECV-01`

**QGSDMCPEnv.tla:**
- TypeInvariantHolds: `\* @requirement MCPENV-01`
- NoSpuriousConsensus: `\* @requirement MCPENV-02`
- EventualDecision: `\* @requirement MCPENV-03`

**TUINavigation.tla:**
TUINavigation properties have no clear requirement IDs in TRACEABILITY_RESEARCH.md — they are listed with descriptive text only. The model-registry has `"requirements": []` for TUINavigation. Since no matching requirement IDs exist in requirements.json, annotate using the closest descriptive identifiers based on the model's domain. Check requirements.json for any VIS-* or WIZ-* requirement IDs. If none match, use descriptive annotations:
- TypeOK: `\* @requirement TUI-01` (type safety for navigation state)
- NoDeadlock: `\* @requirement TUI-02` (ESC always available)
- DepthBounded: `\* @requirement TUI-03` (depth within MaxDepth)
- EscapeProgress: `\* @requirement TUI-04` (ESC reduces depth)

NOTE: Do NOT annotate QGSDQuorum_xstate.tla in this task — it is covered separately. It is a generated scaffold with unbounded state space; however, it should still be annotated for completeness. Read the file, identify its properties, and annotate them. The properties in QGSDQuorum_xstate.tla mirror QGSDQuorum.tla but may have different names. Use the same requirement IDs as QGSDQuorum.tla for matching properties.

Actually, include QGSDQuorum_xstate.tla: read it, identify its property definitions, and annotate them with the same requirement IDs used for QGSDQuorum.tla's corresponding properties.

IMPORTANT: Preserve all existing comments and file headers. Do NOT modify property formulas.
  </action>
  <verify>
```bash
for f in .formal/tla/QGSDQuorum.tla .formal/tla/QGSDMCPEnv.tla .formal/tla/TUINavigation.tla .formal/tla/QGSDQuorum_xstate.tla; do echo "$f: $(grep -c '@requirement' "$f") annotations"; done
```
Expected: QGSDQuorum >= 10 (7 properties, 3 have 2 req IDs), QGSDMCPEnv = 3, TUINavigation = 4, QGSDQuorum_xstate >= 7.

Verify generated file has preservation note:
```bash
grep -c "Preserve annotations when regenerating" .formal/tla/QGSDQuorum.tla
```
Expected: 1
  </verify>
  <done>
All 11 TLA+ model files (including QGSDQuorum_xstate.tla) have `\* @requirement REQ-ID` annotations on every property/invariant. Generated file QGSDQuorum.tla has a preservation note for the annotation comments. ANNOT-01 is fully satisfied.
  </done>
</task>

</tasks>

<verification>
1. `grep -rl '@requirement' .formal/tla/ | grep -v TTrace | sort` lists all 11 main TLA+ model files (plus QGSDQuorum_xstate.tla = 12 total, excluding TTrace files)
2. Total annotation count: `grep -rc '@requirement' .formal/tla/*.tla | grep -v TTrace | awk -F: '{sum+=$2}END{print sum}'` should be >= 52
3. No TTrace files contain annotations: `grep -rl '@requirement' .formal/tla/*TTrace* 2>/dev/null` returns empty
</verification>

<success_criteria>
- All 11 TLA+ model files have @requirement annotations on every property/invariant
- Annotation format is consistently `\* @requirement REQ-ID` (one per line)
- No blank lines between annotation and property definition
- No property formulas were modified
- Generated files have preservation notes
- TTrace files are not annotated
</success_criteria>

<output>
After completion, create `.planning/phases/v0.25-02-property-annotations/v0.25-02-01-SUMMARY.md`
</output>
