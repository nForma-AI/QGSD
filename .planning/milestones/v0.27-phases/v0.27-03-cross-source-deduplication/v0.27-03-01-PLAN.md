---
phase: v0.27-03
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/levenshtein.cjs
  - bin/levenshtein.test.cjs
  - .formal/debt.schema.json
  - bin/validate-debt-entry.cjs
  - bin/validate-debt-entry.test.cjs
autonomous: true
formal_artifacts: none
requirements:
  - FP-03
  - DEBT-02

must_haves:
  truths:
    - "Levenshtein similarity function returns correct distance and similarity score for all edge cases (empty strings, identical strings, single character, long strings)"
    - "Similarity score is 1 - (distance / max(len_a, len_b)) and returns values between 0.0 and 1.0"
    - "Debt schema accepts formal_ref_source field with values 'manual', 'auto-detect', 'spec-inferred', or null"
    - "Existing debt entries without formal_ref_source pass validation (backward compatible)"
  artifacts:
    - path: "bin/levenshtein.cjs"
      provides: "Levenshtein distance and similarity functions"
      exports: ["levenshteinDistance", "levenshteinSimilarity"]
    - path: "bin/levenshtein.test.cjs"
      provides: "Comprehensive unit tests for Levenshtein functions"
      min_lines: 60
    - path: ".formal/debt.schema.json"
      provides: "Updated schema with formal_ref_source field"
      contains: "formal_ref_source"
    - path: "bin/validate-debt-entry.cjs"
      provides: "Updated validation with formal_ref_source support"
      contains: "formal_ref_source"
  key_links:
    - from: "bin/levenshtein.cjs"
      to: "bin/debt-dedup.cjs"
      via: "similarity scoring for near-duplicate detection"
      pattern: "levenshteinSimilarity"
    - from: "bin/validate-debt-entry.cjs"
      to: ".formal/debt.schema.json"
      via: "validation mirrors schema constraints including new formal_ref_source field"
      pattern: "formal_ref_source"
---

<objective>
Implement the Levenshtein similarity algorithm and extend the debt schema to support formal_ref_source tracking.

Purpose: Provides the similarity comparison foundation needed by the dedup engine (Plan 02) and extends the schema so formal reference linking (Plan 02) can record its provenance. These are independent building blocks with no cross-dependency.

Output: Levenshtein module (bin/levenshtein.cjs) with comprehensive tests, updated debt schema and validation supporting formal_ref_source field.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v0.27-03-cross-source-deduplication/v0.27-03-RESEARCH.md
@.planning/phases/v0.27-03-cross-source-deduplication/v0.27-03-CONTEXT.md
@.formal/debt.schema.json
@bin/validate-debt-entry.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Levenshtein distance and similarity (TDD)</name>
  <files>
    bin/levenshtein.cjs
    bin/levenshtein.test.cjs
  </files>
  <action>
RED: Create bin/levenshtein.test.cjs with tests FIRST:
- levenshteinDistance("kitten", "sitting") === 3
- levenshteinDistance("", "") === 0
- levenshteinDistance("abc", "") === 3
- levenshteinDistance("", "abc") === 3
- levenshteinDistance("same", "same") === 0
- levenshteinDistance("a", "b") === 1
- levenshteinDistance("abc", "axc") === 1
- levenshteinSimilarity("same", "same") === 1.0
- levenshteinSimilarity("", "") === 1.0 (both empty = identical)
- levenshteinSimilarity("abc", "xyz") — low similarity
- levenshteinSimilarity("abc", "abd") — high similarity (0.667)
- Case sensitivity test: levenshteinSimilarity("ABC", "abc") confirms case-sensitive by default
- Threshold check: similarity("TypeError in auth", "TypeError in authentication") >= 0.85 (near-duplicate example)
- Threshold check: similarity("TypeError in auth", "SyntaxError in parser") < 0.85 (different issue)
- Long string test (256 chars) completes without error

Run tests — they MUST fail (no implementation yet).

GREEN: Create bin/levenshtein.cjs implementing Wagner-Fischer algorithm:
- levenshteinDistance(a, b) — returns integer distance
  - Use two-row space optimization: O(min(m,n)) space
  - Handle empty string edge cases (return length of other)
- levenshteinSimilarity(a, b) — returns float 0.0 to 1.0
  - Formula: 1 - (distance / max(a.length, b.length))
  - Both empty strings: return 1.0 (identical)
- CommonJS module.exports: { levenshteinDistance, levenshteinSimilarity }

Run tests — they MUST pass.

REFACTOR: Ensure the two-row optimization is clean and well-commented.
  </action>
  <verify>node --test bin/levenshtein.test.cjs</verify>
  <done>
- levenshteinDistance computes correct edit distance for all test cases
- levenshteinSimilarity returns normalized score between 0.0 and 1.0
- Both functions handle empty strings, identical strings, and long strings
- All unit tests pass (15+ test cases)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend debt schema with formal_ref_source field (TDD)</name>
  <files>
    .formal/debt.schema.json
    bin/validate-debt-entry.cjs
    bin/validate-debt-entry.test.cjs
  </files>
  <action>
RED: Add tests to bin/validate-debt-entry.test.cjs:
- Valid entry with formal_ref_source: "manual" passes
- Valid entry with formal_ref_source: "auto-detect" passes
- Valid entry with formal_ref_source: "spec-inferred" passes
- Valid entry with formal_ref_source: null passes
- Entry without formal_ref_source passes (backward compatible — field is optional)
- Invalid formal_ref_source value (e.g., "invalid") is rejected
- Entry with formal_ref_source: "manual" and formal_ref: null passes (source tracking without ref is allowed)

Run tests — new tests MUST fail.

GREEN: Update .formal/debt.schema.json:
- Add "formal_ref_source" property:
  - type: ["string", "null"]
  - enum: ["manual", "auto-detect", "spec-inferred", null]
  - description: "Source of formal_ref assignment: manual, auto-detect, spec-inferred, or null if unlinked"

Update bin/validate-debt-entry.cjs:
- Add "formal_ref_source" to the allowedProps Set
- Add validation: if entry has formal_ref_source, check it is string or null, and if string must be one of "manual", "auto-detect", "spec-inferred"
- Pattern: mirror the existing formal_ref validation style

Run tests — all tests MUST pass (both new and existing).
  </action>
  <verify>node --test bin/validate-debt-entry.test.cjs</verify>
  <done>
- debt.schema.json includes formal_ref_source with correct type and enum
- validate-debt-entry.cjs accepts valid formal_ref_source values
- validate-debt-entry.cjs rejects invalid formal_ref_source values
- Existing entries without formal_ref_source still pass validation (backward compatible)
- All tests pass including both new and previously existing tests
  </done>
</task>

</tasks>

<verification>
- `node --test bin/levenshtein.test.cjs` — all tests pass
- `node --test bin/validate-debt-entry.test.cjs` — all tests pass
- `.formal/debt.schema.json` includes formal_ref_source property
- bin/validate-debt-entry.cjs allowedProps includes "formal_ref_source"
</verification>

<success_criteria>
- Levenshtein similarity function correctly handles all edge cases and produces scores usable by the dedup engine
- Debt schema is extended with formal_ref_source while maintaining full backward compatibility
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/v0.27-03-cross-source-deduplication/v0.27-03-01-SUMMARY.md`
</output>
