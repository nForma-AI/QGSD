---
phase: v0.27-02
phase_name: Observe Skill Core
verified: 2026-03-04T18:30:00Z
status: passed
score: 12/12 must-haves verified
re_verification: false
---

# Phase v0.27-02: Observe Skill Core Verification Report

**Phase Goal:** Users can run `/qgsd:observe` to fetch issues from all configured sources in parallel, see severity-sorted output split into Issues and Drifts tables, and have results written to the debt ledger.

**Verified:** 2026-03-04T18:30:00Z
**Status:** PASSED
**Score:** 12/12 must-haves verified
**Tests:** 103 passing (all phase modules) + 127 passing (v0.27-01 regression)

---

## Goal Achievement Summary

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | New source types require only handler function + config entry, no orchestrator changes | ✓ VERIFIED | `registerHandler()` pattern in registry, uniform dispatch via `dispatchAll()`, no type-specific conditionals in observe command |
| 2 | Config loads from `.planning/observe-sources.md` with backward compatibility to `triage-sources.md` | ✓ VERIFIED | Config loader tries observe-sources.md first, falls back to triage-sources.md; tests verify both paths work |
| 3 | One failing source does not block others — fail-open behavior enforced | ✓ VERIFIED | `Promise.allSettled()` in `dispatchAll()`, per-source timeout wrapping with `Promise.race()`, test "includes both fulfilled and failed results" passes |
| 4 | All handlers return uniform schema: `{ source_label, source_type, status, issues[], error?, _mcp_instruction? }` | ✓ VERIFIED | GitHub, Sentry, Sentry-feedback, bash handlers all return same schema; 39 handler tests pass including "standard schema compliance" suite (4 tests) |
| 5 | `/qgsd:observe` command fetches from all configured sources in parallel | ✓ VERIFIED | Command spec Step 4 calls `dispatchAll()`, which uses `Promise.allSettled()` for parallel dispatch; test "runs multiple sources in parallel and returns all results" confirms |
| 6 | Output renders two separate tables: Issues (discrete events) and Drifts (quantitative divergences) | ✓ VERIFIED | `renderObserveOutput()` separates by `issue_type`, renders two distinct tables; test "separates issues and drifts into two tables" passes |
| 7 | Issues are sorted by severity (error > bug > warning > info), then by age | ✓ VERIFIED | `classifySeverity()` defines sort order; test "sorts issues by severity then age" passes |
| 8 | Observe writes new/updated debt entries to `.formal/debt.json` after each run | ✓ VERIFIED | Command spec Step 6a calls `writeObservationsToDebt()`; test "creates new debt entry with correct fingerprint and status open" passes |
| 9 | Debt entries use deterministic fingerprints from v0.27-01 (`fingerprintIssue`, `fingerprintDrift`) — never inline computation | ✓ VERIFIED | `observe-debt-writer.cjs` imports and calls v0.27-01 functions directly; test "uses fingerprintDrift for drift observations" and "uses fingerprintIssue for issue observations" pass |
| 10 | Sentry/Sentry-feedback handlers return `status: "pending_mcp"` with `_mcp_instruction` for MCP execution | ✓ VERIFIED | Handlers return `{ status: "pending_mcp", _mcp_instruction: { type, tool, params, mapper } }`; tests verify structure |
| 11 | Observe command bridges MCP results via Step 4b — executes MCP tools, maps results to standard schema using exported mapper functions | ✓ VERIFIED | Command spec Step 4b details MCP bridge pattern; mappers (`mapSentryIssuesToSchema`, `mapSentryFeedbackToSchema`) exported from handlers |
| 12 | No Task() subagents spawned for Sentry MCP calls — observe command executes MCP tools directly in Step 4b | ✓ VERIFIED | Step 4b documentation shows direct MCP tool calls, not Task() spawn; no "subagent" or "Task" references in MCP bridge section |

**Score:** 12/12 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bin/observe-config.cjs` | Config loader with YAML parsing, backward compat, validation | ✓ EXISTS | 230 lines; exports `loadObserveConfig`; parses frontmatter, infers issue_type, applies defaults |
| `bin/observe-config.test.cjs` | Unit tests for config loading | ✓ EXISTS | 280 lines; 27 tests passing (config suite + validation suite) |
| `bin/observe-registry.cjs` | Source handler registry with pluggable dispatch | ✓ EXISTS | 115 lines; exports `registerHandler`, `getHandler`, `listHandlers`, `dispatchSource`, `dispatchAll` |
| `bin/observe-registry.test.cjs` | Registry unit tests | ✓ EXISTS | 155 lines; 16 tests passing (register/get/list/dispatch suites + timeout suite) |
| `bin/observe-handlers.cjs` | GitHub, Sentry, sentry-feedback, bash handlers | ✓ EXISTS | 300 lines; exports 4 handlers + 2 mappers (`mapSentryIssuesToSchema`, `mapSentryFeedbackToSchema`) |
| `bin/observe-handlers.test.cjs` | Handler unit tests with mocked externals | ✓ EXISTS | 280 lines; 39 tests passing (4 handler suites + schema compliance suite) |
| `bin/observe-debt-writer.cjs` | Debt ledger upsert after observe | ✓ EXISTS | 95 lines; exports `writeObservationsToDebt`; imports v0.27-01 fingerprinting/persistence functions |
| `bin/observe-debt-writer.test.cjs` | Debt writer integration tests | ✓ EXISTS | 155 lines; 8 tests passing (upsert suite + fingerprint suite) |
| `bin/observe-render.cjs` | Dual-table rendering (Issues + Drifts) | ✓ EXISTS | 150 lines; exports `renderObserveOutput`, `classifySeverity`, `formatAge` |
| `bin/observe-render.test.cjs` | Render unit tests | ✓ EXISTS | 120 lines; 13 tests passing (separation suite + rendering suite) |
| `commands/qgsd/observe.md` | Observe command specification | ✓ EXISTS | 165 lines; YAML frontmatter with name: "qgsd:observe"; 7 process steps + Step 4b |
| `.planning/observe-sources.md` | Default observe sources configuration | ✓ EXISTS | 85 lines; YAML frontmatter with `observe_config` and `sources` examples; backward-compat documentation |

---

## Requirements Coverage

| Requirement | Phase | Description | Status | Evidence |
|-------------|-------|-------------|--------|----------|
| **OBS-01** | v0.27-02 | `/qgsd:observe` fetches from all configured sources in parallel, renders severity-sorted triage table — same behavior as current `/qgsd:triage` | ✓ SATISFIED | Command spec Steps 1-5; `dispatchAll()` for parallel fetch; `renderObserveOutput()` sorts by severity |
| **OBS-02** | v0.27-02 | Source types are pluggable — adding a new source type requires only a new fetch handler function and config entry, not changes to the observe orchestrator | ✓ SATISFIED | `registerHandler()` pattern in registry; handlers map; no type-specific conditionals in command; test "Handler registry supports register/get/list/dispatch" confirms |
| **OBS-06** | v0.27-02 | `/qgsd:observe` presents two output tables: **Issues** (discrete events) and **Drifts** (quantitative divergences) | ✓ SATISFIED | `renderObserveOutput()` separates by `issue_type`, renders two tables; test "separates issues and drifts into two tables" passes |
| **OBS-07** | v0.27-02 | Observe config lives in `.planning/observe-sources.md` YAML frontmatter, backward-compatible with existing triage-sources.md format | ✓ SATISFIED | Config loader implements backward compat (tries observe-sources.md first, falls back to triage-sources.md); test "Falling back to triage-sources.md when observe-sources.md is missing" passes |
| **OBS-08** | v0.27-02 | Each source fetch handler has a configurable timeout and fail-open behavior — one failing source does not block the others | ✓ SATISFIED | Per-source timeout via `Promise.race()` in `dispatchSource()`; `Promise.allSettled()` in `dispatchAll()`; test "includes both fulfilled and failed results" confirms |
| **DEBT-06** | v0.27-02 | `/qgsd:observe` writes new or updated debt entries after each run; the human triage step sets status to `acknowledged` for entries the user selects to work on | ✓ SATISFIED | Command spec Step 6a calls `writeObservationsToDebt()`; test "creates new debt entry with correct fingerprint and status open" passes; Step 7 includes "ack N" action |

---

## Test Results

### v0.27-02 Module Tests

| Module | Tests | Passing | Status |
|--------|-------|---------|--------|
| `bin/observe-config.test.cjs` | 27 | 27 | ✓ PASS |
| `bin/observe-registry.test.cjs` | 16 | 16 | ✓ PASS |
| `bin/observe-handlers.test.cjs` | 39 | 39 | ✓ PASS |
| `bin/observe-debt-writer.test.cjs` | 8 | 8 | ✓ PASS |
| `bin/observe-render.test.cjs` | 13 | 13 | ✓ PASS |
| **Total Phase Tests** | **103** | **103** | ✓ **PASS** |

### v0.27-01 Regression Tests

| Module | Tests | Passing | Status |
|--------|-------|---------|--------|
| `bin/validate-debt-entry.test.cjs` | 10 | 10 | ✓ PASS |
| `bin/debt-state-machine.test.cjs` | 18 | 18 | ✓ PASS |
| `bin/fingerprint-issue.test.cjs` | 21 | 21 | ✓ PASS |
| `bin/fingerprint-drift.test.cjs` | 28 | 28 | ✓ PASS |
| `bin/debt-ledger.test.cjs` | 50 | 50 | ✓ PASS |
| **Total v0.27-01 Tests** | **127** | **127** | ✓ **PASS** |

**Zero failures in all test suites. Zero regressions.**

---

## Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|----|--------|----------|
| `commands/qgsd/observe.md` (Step 2) | `bin/observe-config.cjs` | `loadObserveConfig()` | ✓ WIRED | Command spec calls `loadObserveConfig()` in Step 2; config file exists and exports function |
| `commands/qgsd/observe.md` (Step 3) | `bin/observe-registry.cjs` | `registerHandler()` | ✓ WIRED | Command spec calls `registerHandler()` in Step 3; registry exports function |
| `commands/qgsd/observe.md` (Step 4) | `bin/observe-registry.cjs` | `dispatchAll()` | ✓ WIRED | Command spec calls `dispatchAll()` in Step 4; registry exports function |
| `commands/qgsd/observe.md` (Step 3+4b) | `bin/observe-handlers.cjs` | handler registration + mapper functions | ✓ WIRED | Command spec registers handlers in Step 3, calls mappers in Step 4b; handlers exports all 4 handlers + 2 mappers |
| `commands/qgsd/observe.md` (Step 4b) | `bin/observe-handlers.cjs` | `mapSentryIssuesToSchema`, `mapSentryFeedbackToSchema` | ✓ WIRED | Step 4b imports mappers; handlers exports both functions |
| `commands/qgsd/observe.md` (Step 5) | `bin/observe-render.cjs` | `renderObserveOutput()` | ✓ WIRED | Command spec calls `renderObserveOutput()` in Step 5; render module exports function |
| `commands/qgsd/observe.md` (Step 6a) | `bin/observe-debt-writer.cjs` | `writeObservationsToDebt()` | ✓ WIRED | Command spec calls `writeObservationsToDebt()` in Step 6a; debt writer exports function |
| `bin/observe-debt-writer.cjs` | `bin/debt-ledger.cjs` | `readDebtLedger()`, `writeDebtLedger()` | ✓ WIRED | Debt writer imports and calls v0.27-01 functions; v0.27-01 modules exported and available |
| `bin/observe-debt-writer.cjs` | `bin/fingerprint-issue.cjs` | `fingerprintIssue()` | ✓ WIRED | Debt writer imports v0.27-01 fingerprinting function; v0.27-01 module available |
| `bin/observe-debt-writer.cjs` | `bin/fingerprint-drift.cjs` | `fingerprintDrift()` | ✓ WIRED | Debt writer imports v0.27-01 fingerprinting function; v0.27-01 module available |
| `.planning/observe-sources.md` | `bin/observe-config.cjs` | YAML frontmatter parsing | ✓ WIRED | Config loader parses observe-sources.md YAML; config file exists with valid frontmatter |

---

## Anti-Pattern Scan

| File | Pattern | Count | Severity | Status |
|------|---------|-------|----------|--------|
| All `.cjs` files | TODO/FIXME/XXX/HACK markers | 0 | — | ✓ CLEAN |
| All `.cjs` files | Placeholder/coming soon comments | 0 | — | ✓ CLEAN |
| `observe-handlers.cjs` | Unhandled exceptions (no try-catch) | 0 | — | ✓ CLEAN |
| `observe-config.cjs` | Silent skips without error reporting | 0 | — | ✓ CLEAN |
| `observe-render.cjs` | console.log in production code | 0 | — | ✓ CLEAN |
| `observe-debt-writer.cjs` | Inline fingerprint computation | 0 | — | ✓ CLEAN (uses v0.27-01 functions) |
| `commands/qgsd/observe.md` | Task() subagent spawn for Sentry MCP | 0 | — | ✓ CLEAN (uses direct MCP calls in Step 4b) |

**No blockers. No warnings. All patterns follow project conventions.**

---

## Implementation Quality

### Code Standards
- **Style consistency:** All modules use CommonJS patterns, node:test framework (project standard)
- **Error handling:** All handlers wrapped in try-catch (fail-open per OBS-08); debt writer validates before write; config loader returns errors inline
- **No external dependencies:** Uses only Node.js built-ins (fs, path, crypto, child_process, node:test, node:assert)
- **Test coverage:** 103 tests across 5 modules; all critical paths covered (success, failure, timeout, edge cases)
- **Backward compatibility:** Config loader handles both observe-sources.md and triage-sources.md; issue_type inferred for known source types

### Pluggability Verification
New source type (e.g., `prometheus`) would require:
1. Create handler function: `function handlePrometheus(sourceConfig, options) { ... return { source_label, source_type: "prometheus", status, issues, error? } }`
2. Add config example to `observe-sources.md` with `issue_type: drift`
3. Register in observe.md Step 3: `registerHandler('prometheus', handlePrometheus)`
**No changes to orchestrator, registry, or renderer.** ✓ Pluggability confirmed.

### Debt Persistence Verification
Fingerprinting chain:
- observe-debt-writer.cjs imports `fingerprintIssue` from `bin/fingerprint-issue.cjs` (v0.27-01)
- observe-debt-writer.cjs imports `fingerprintDrift` from `bin/fingerprint-drift.cjs` (v0.27-01)
- No inline fingerprinting; all dedup uses v0.27-01 deterministic functions
- Tests verify: "increments occurrences and updates last_seen on repeat observation" ✓
**Fingerprint stability confirmed.**

---

## Summary

Phase v0.27-02 achieves all phase goals:

✓ **Users can run `/qgsd:observe`** — Command specification created with 7-step process + Step 4b MCP bridge
✓ **Fetch from all configured sources in parallel** — `dispatchAll()` uses Promise.allSettled for uniform parallel dispatch
✓ **See severity-sorted output** — `renderObserveOutput()` sorts Issues and Drifts by severity then age
✓ **Split into Issues and Drifts tables** — Separate rendering by `issue_type` field; two distinct tables
✓ **Have results written to debt ledger** — `writeObservationsToDebt()` writes after each observe run, using v0.27-01 fingerprinting for dedup

All 6 requirements (OBS-01, OBS-02, OBS-06, OBS-07, OBS-08, DEBT-06) satisfied.
All 12 must-haves verified.
All 103 phase tests passing.
All 127 v0.27-01 dependency tests passing (zero regressions).
All artifacts substantive (not stubs), properly wired, and tested.

---

**Status: PASSED**
**Score: 12/12 must-haves verified**
**Ready for next phase**

_Verified: 2026-03-04T18:30:00Z_
_Verifier: Claude (qgsd-verifier)_
