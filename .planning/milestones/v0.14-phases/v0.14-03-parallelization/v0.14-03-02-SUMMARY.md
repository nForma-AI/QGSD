---
phase: v0.14-03
plan: 02
type: summary
requirements:
  - PERF-02
tasks_completed: 2
files_modified: 2
duration: ~45s
status: complete
---

# Summary: v0.14-03-02 — Timing Instrumentation + Test Coverage

## What Was Done

### Task 1: Wall-clock timing in run-formal-verify.cjs

Added `startMs = Date.now()` at the start of the async IIFE (before Phase 1) and a `Wall-clock: Xs (Yms)` summary line after the step results table.

Output example:
```
[run-formal-verify] ════════════════════════════════════════════════════════════════
[run-formal-verify] SUMMARY — 2/2 passed
[run-formal-verify] ════════════════════════════════════════════════════════════════
[run-formal-verify]  ✓  generate:tla-from-xstate       Generate TLA+ spec ...
[run-formal-verify]  ✓  generate:alloy-prism-specs      Generate Alloy + PRISM ...
[run-formal-verify] ════════════════════════════════════════════════════════════════
[run-formal-verify] Wall-clock: 0.1s (127ms)
[run-formal-verify] ════════════════════════════════════════════════════════════════
```

### Task 2: Two new tests in run-formal-verify.test.cjs

**Test 4 — timing (PERF-02):**
- Runs `--only=generate` with 120s timeout hard cap
- Asserts `result.signal === null` (no timeout kill)
- Asserts elapsed < 120000ms
- Asserts output matches `/Wall-clock/i`

**Test 5 — parallelization smoke (PERF-01):**
- Runs `--only=tla` (exercises Phase 2 parallel execution)
- Asserts all 8 TLA+ step IDs appear in output
- Guards against silent step drops from concurrency bugs

## Verification

- `node --check bin/run-formal-verify.cjs` exits 0
- `node bin/run-formal-verify.cjs --only=generate` output includes "Wall-clock: Xs (Yms)"
- `node --test bin/run-formal-verify.test.cjs` → 5 tests, 5 pass (3 existing + 2 new)
- `npm test` → 333 tests, 333 pass (no regressions)

## Requirements

- PERF-02: SATISFIED — per-step wall-clock timing visible in summary output; machine-verifiable via Test 4 (timing bound assertion)
