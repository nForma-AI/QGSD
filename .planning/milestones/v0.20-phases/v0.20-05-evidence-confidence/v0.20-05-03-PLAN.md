---
phase: v0.20-05
plan: "03"
type: execute
wave: 2
depends_on:
  - v0.20-05-02
files_modified:
  - bin/run-formal-verify.cjs
  - bin/run-formal-verify.test.cjs
autonomous: true
requirements:
  - EVID-01
  - EVID-02

must_haves:
  truths:
    - "run-formal-verify.cjs STEPS array includes a 'ci:conformance-traces' entry that invokes validate-traces.cjs"
    - "Running run-formal-verify.cjs --only=ci produces a check-results.ndjson entry with observation_window"
    - "A test in run-formal-verify.test.cjs asserts ci:conformance-traces is in STEPS"
    - "npm test passes with the new STEPS entry and test"
  artifacts:
    - path: "bin/run-formal-verify.cjs"
      provides: "STEPS entry for ci:conformance-traces invoking validate-traces.cjs"
      contains: "ci:conformance-traces"
    - path: "bin/run-formal-verify.test.cjs"
      provides: "test asserting ci:conformance-traces STEPS entry exists"
      contains: "ci:conformance-traces"
  key_links:
    - from: "bin/run-formal-verify.cjs"
      to: "bin/validate-traces.cjs"
      via: "STEPS entry type:node script:validate-traces.cjs"
      pattern: "validate-traces\\.cjs"
    - from: "bin/run-formal-verify.test.cjs"
      to: "bin/run-formal-verify.cjs"
      via: "require STEPS + assert includes ci:conformance-traces"
      pattern: "ci:conformance-traces"
---

<objective>
Wire validate-traces.cjs into the formal verification pipeline via a STEPS entry, and add a test asserting its presence.

Purpose: validate-traces.cjs now writes enriched v2.1 NDJSON with observation_window (EVID-02). Wiring it into run-formal-verify.cjs makes it part of every formal verification run, so evidence confidence appears in check-results.ndjson automatically. The test guards against the STEPS entry being accidentally removed.

Output: One new STEPS entry in run-formal-verify.cjs; one new test in run-formal-verify.test.cjs; end-to-end smoke confirming NDJSON output contains observation_window.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/run-formal-verify.cjs
@bin/run-formal-verify.test.cjs
@.planning/phases/v0.20-05-evidence-confidence/v0.20-05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ci:conformance-traces STEPS entry to run-formal-verify.cjs</name>
  <files>bin/run-formal-verify.cjs</files>
  <action>
    Add a new STEPS entry to the STEPS array in bin/run-formal-verify.cjs for validate-traces.cjs.

    Location: Append to the end of the CI enforcement section (after the existing `ci:liveness-fairness-lint` entry at approximately line 191-194, before the closing `];`).

    New entry:
    ```javascript
    {
      tool: 'ci', id: 'ci:conformance-traces',
      label: 'Conformance trace validation — XState machine replay with evidence confidence (EVID-01, EVID-02)',
      type: 'node', script: 'validate-traces.cjs', args: [],
    },
    ```

    This follows the exact same structure as the existing ci:trace-redaction, ci:trace-schema-drift, and ci:liveness-fairness-lint entries.

    After adding, verify the STEPS array header comment count is updated if there is one (check the header for total count references — look at how the run-formal-verify.cjs header documents step counts, e.g., "Total: N steps"; increment if present).

    Do NOT add this entry to the TLA+, Alloy, or PRISM sections. It belongs in the CI enforcement section alongside the other `tool: 'ci'` entries.
  </action>
  <verify>
    Run: `node -e "const s=require('./bin/run-formal-verify.cjs'); " 2>&1` — should produce no syntax errors (module loads cleanly).

    Also: `grep "ci:conformance-traces" bin/run-formal-verify.cjs` — should return the new entry.
  </verify>
  <done>
    `bin/run-formal-verify.cjs` contains a STEPS entry with `id: 'ci:conformance-traces'` and `script: 'validate-traces.cjs'`. The file loads without syntax errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add STEPS presence test and run integration smoke</name>
  <files>bin/run-formal-verify.test.cjs</files>
  <action>
    Add one test to bin/run-formal-verify.test.cjs asserting that the ci:conformance-traces STEPS entry exists.

    Pattern: Read the existing tests in run-formal-verify.test.cjs to find how other STEPS entries are tested (e.g., ci:liveness-fairness-lint was added in v0.20-02). Follow the same pattern exactly.

    Expected pattern from prior phases (v0.20-02-03):
    ```javascript
    test('STEPS contains ci:conformance-traces entry', () => {
      // Read STEPS from run-formal-verify.cjs source (or require and check exports)
      const src = fs.readFileSync(path.join(__dirname, 'run-formal-verify.cjs'), 'utf8');
      assert.ok(src.includes("id: 'ci:conformance-traces'"), "STEPS must include ci:conformance-traces");
    });
    ```

    If run-formal-verify.test.cjs uses a different pattern (e.g., spawning the process and checking --help output, or requiring a STEPS export), match that pattern instead. Read the file before adding the test.

    After adding the test, run the integration smoke to confirm observation_window appears in NDJSON output end-to-end:

    Smoke: `node bin/run-formal-verify.cjs --only=ci:conformance-traces 2>&1; grep observation_window .formal/check-results.ndjson | tail -1 | node -e "const l=require('fs').readFileSync('/dev/stdin','utf8'); const r=JSON.parse(l); process.stdout.write(JSON.stringify({has_ow:!!r.observation_window, check_id:r.check_id, surface:r.surface})+'\n')"`

    Expected smoke output: `{"has_ow":true,"check_id":"ci:conformance-traces","surface":"ci"}`

    If the conformance-events.jsonl file does not exist in the repo root (common on a fresh checkout), the validate-traces early-exit path runs and still writes observation_window with reason: 'no-log'. That is acceptable for the smoke test — observation_window must still be present.
  </action>
  <verify>
    1. `node --test bin/run-formal-verify.test.cjs 2>&1 | grep "conformance-traces"` — shows "ok" for the new test
    2. `npm test` passes with no regressions
    3. `grep "ci:conformance-traces" .formal/check-results.ndjson | tail -1` — NDJSON record present with check_id field (after running the smoke above)
  </verify>
  <done>
    - bin/run-formal-verify.test.cjs has a passing test for ci:conformance-traces STEPS entry
    - npm test passes (all suites green)
    - .formal/check-results.ndjson contains at least one record with check_id: 'ci:conformance-traces' and observation_window present
    - EVID-01 and EVID-02 fully closed: confidence in divergences (already existed), observation_window in NDJSON (implemented in Plan 02), STEPS wired (this task)
  </done>
</task>

</tasks>

<verification>
1. `grep "ci:conformance-traces" bin/run-formal-verify.cjs` — STEPS entry present
2. `npm test` — full suite passes
3. Run smoke: `node bin/run-formal-verify.cjs --only=ci:conformance-traces` exits 0, writes NDJSON record
4. `node -e "const fs=require('fs'); const lines=fs.readFileSync('.formal/check-results.ndjson','utf8').split('\n').filter(Boolean); const last=lines.map(l=>JSON.parse(l)).filter(r=>r.check_id==='ci:conformance-traces').pop(); console.log(!!last.observation_window, last.check_id, last.surface)"` outputs: `true ci:conformance-traces ci`
</verification>

<success_criteria>
- run-formal-verify.cjs STEPS contains ci:conformance-traces entry invoking validate-traces.cjs
- run-formal-verify.test.cjs has passing test for ci:conformance-traces
- npm test: full suite green
- NDJSON smoke: observation_window present with window_start, window_end, n_traces, n_events, window_days
- EVID-01 closed: confidence field in divergence objects (already implemented in v0.19-04, confirmed passing tests)
- EVID-02 closed: observation_window in check-result NDJSON records (implemented Plan 02, wired Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/v0.20-05-evidence-confidence/v0.20-05-03-SUMMARY.md`
</output>
