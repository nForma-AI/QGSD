---
phase: v0.20-05
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - bin/validate-traces.test.cjs
autonomous: true
requirements:
  - EVID-01
  - EVID-02

must_haves:
  truths:
    - "8 new tests in validate-traces.test.cjs all FAIL (RED) before implementation"
    - "Tests cover observation_window field presence in NDJSON output for all writeCheckResult call sites"
    - "Tests cover check_id = 'ci:conformance-traces' and surface = 'ci' in NDJSON records"
    - "Running validate-traces.cjs against a conformance log produces NDJSON without observation_window — confirming RED state"
  artifacts:
    - path: "bin/validate-traces.test.cjs"
      provides: "8 Wave 0 RED tests for EVID-01 and EVID-02"
      min_lines: 244
      contains: "observation_window"
  key_links:
    - from: "bin/validate-traces.test.cjs"
      to: "bin/validate-traces.cjs"
      via: "spawnSync + NDJSON stdout parsing"
      pattern: "observation_window"
---

<objective>
Scaffold Wave 0 RED tests for EVID-01 and EVID-02 in validate-traces.cjs.

Purpose: Lock the contract before implementation. RED tests specify that `validate-traces.cjs` must write `observation_window` (window_start, window_end, n_traces, n_events, window_days) and v2.1 fields (check_id, surface) to every check-result NDJSON record. Tests MUST fail now — they pass after Plan 02 implementation.

Output: 8 failing test cases appended to `bin/validate-traces.test.cjs`.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/qgsd/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/qgsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v0.20-05-evidence-confidence/v0.20-05-RESEARCH.md
@.planning/phases/v0.20-05-evidence-confidence/v0.20-05-VALIDATION.md
@bin/validate-traces.test.cjs
@bin/validate-traces.cjs
</context>

<feature>
  <name>EVID-01 + EVID-02 Wave 0 RED Test Scaffold</name>
  <files>bin/validate-traces.test.cjs</files>
  <behavior>
    All 8 tests must FAIL when run against the current validate-traces.cjs (which does not yet write observation_window or v2.1 fields).

    The existing test file (236 lines) uses `node:test` + `node:assert` with a `runValidator()` helper that:
    - Creates a temp dir with a .planning/conformance-events.jsonl
    - Spawns validate-traces.cjs via spawnSync with cwd set to temp dir
    - Returns { status, stdout, stderr }

    To test NDJSON output, the helper (or a new variant) must also read the check-results.ndjson file written to the temp dir. Pattern: after runValidator(), read `path.join(tmpDir, 'formal', 'check-results.ndjson')` and parse the last line.

    Test cases to add (append after line 236):

    1. "observation_window.window_start is present as ISO-8601 string"
       - Run validator with 1 valid conformance event line
       - Read last NDJSON record from .formal/check-results.ndjson
       - assert.ok(record.observation_window, 'observation_window present')
       - assert.match(record.observation_window.window_start, /^\d{4}-\d{2}-\d{2}T/)

    2. "observation_window.window_end is present as ISO-8601 string"
       - Same setup
       - assert.match(record.observation_window.window_end, /^\d{4}-\d{2}-\d{2}T/)

    3. "observation_window.n_traces equals 0 when no scoreboard"
       - Run without .planning/quorum-scoreboard.json in temp dir
       - assert.strictEqual(record.observation_window.n_traces, 0)

    4. "observation_window.n_events equals conformance log line count"
       - Run with 3 valid conformance event lines
       - assert.strictEqual(record.observation_window.n_events, 3)

    5. "observation_window.window_days is 0 when no scoreboard"
       - Run without scoreboard
       - assert.strictEqual(record.observation_window.window_days, 0)

    6. "observation_window.window_days matches multi-day scoreboard span"
       - Create quorum-scoreboard.json in temp .planning/ with 2 rounds, dates 10 days apart
       - Run validator with valid event
       - assert.strictEqual(record.observation_window.window_days, 10)

    7. "check_id is ci:conformance-traces"
       - Run validator
       - assert.strictEqual(record.check_id, 'ci:conformance-traces')

    8. "surface field is ci"
       - Run validator
       - assert.strictEqual(record.surface, 'ci')

    Helper for NDJSON reading: after runValidator(), read the check-results.ndjson from tmpDir/.formal/check-results.ndjson, split by newline, filter non-empty, parse last line. Create a helper function `getLastNDJSON(tmpDir)` for reuse across tests.

    Scoreboard fixture for test 6: { "rounds": [{ "date": "2026-01-01T00:00:00.000Z" }, { "date": "2026-01-11T00:00:00.000Z" }] } — span is exactly 10 days.

    Valid conformance event line (for runValidator calls): JSON.stringify({ action: 'plan_phase', timestamp: new Date().toISOString() })

    IMPORTANT: The runValidator helper currently does NOT copy .formal/ dir. The writeCheckResult call in validate-traces.cjs writes to process.cwd()/.formal/check-results.ndjson. The temp dir will have this path automatically since cwd is set to tmpDir in spawnSync. Verify this works by checking the existing integration tests at lines 74-109 of validate-traces.test.cjs.
  </behavior>
  <implementation>
    Do NOT implement validate-traces.cjs changes in this plan — that is Plan 02.
    Only add tests. Confirm all 8 tests fail with "AssertionError" or similar (not module-load errors).

    RED state verification command: `node --test bin/validate-traces.test.cjs 2>&1 | grep -E "fail|FAIL|not ok" | head -20`

    Expected: 8 test failures referencing observation_window, check_id, surface.
    If tests pass (green), the implementation is already present — stop and report.
    If tests error (not AssertionError), fix the test setup so they properly fail.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Wave 0 RED — 8 observation_window + v2.1 field tests</name>
  <files>bin/validate-traces.test.cjs</files>
  <action>
    Append to bin/validate-traces.test.cjs (after line 236, after the existing test suite) 8 new test cases for EVID-01 and EVID-02.

    Step 1 — Add getLastNDJSON helper before the new tests:
    ```javascript
    // Helper: read last NDJSON record written by validate-traces.cjs
    function getLastNDJSON(tmpDir) {
      const ndjsonPath = path.join(tmpDir, 'formal', 'check-results.ndjson');
      if (!fs.existsSync(ndjsonPath)) return null;
      const lines = fs.readFileSync(ndjsonPath, 'utf8').split('\n').filter(l => l.trim().length > 0);
      if (lines.length === 0) return null;
      return JSON.parse(lines[lines.length - 1]);
    }
    ```

    Step 2 — Add helper for valid conformance event line:
    ```javascript
    const VALID_EVENT = JSON.stringify({ action: 'plan_phase', timestamp: new Date().toISOString() });
    ```

    Step 3 — Add scoreboard fixture factory:
    ```javascript
    function writeScoreboard(tmpDir, rounds) {
      fs.writeFileSync(
        path.join(tmpDir, '.planning', 'quorum-scoreboard.json'),
        JSON.stringify({ rounds }),
        'utf8'
      );
    }
    ```

    Step 4 — Write the 8 tests using `test()` from `node:test`. Each test:
    - Creates a fresh tmpDir via runValidator or direct setup
    - For tests needing NDJSON output: run with a valid event line so validate-traces exits without error and writes result
    - Uses getLastNDJSON(tmpDir) to read the NDJSON
    - Asserts the expected field/value

    Note: The existing runValidator() helper does NOT return tmpDir. You may need to either:
    (a) Modify runValidator to also return tmpDir (check if it cleans up — if cleanup happens in the helper, add an option to skip cleanup for tests that need to read NDJSON), or
    (b) Write a new runValidatorWithNDJSON helper that returns { status, stdout, stderr, tmpDir } and does NOT cleanup automatically.

    Read the existing runValidator implementation carefully (lines 13-35 of validate-traces.test.cjs) before deciding. The safest approach is adding a new runValidatorKeepTmp helper that does NOT call fs.rmSync at the end.

    Test names must exactly match:
    - "observation_window.window_start is ISO-8601 string"
    - "observation_window.window_end is ISO-8601 string"
    - "observation_window.n_traces is 0 without scoreboard"
    - "observation_window.n_events equals conformance log line count"
    - "observation_window.window_days is 0 without scoreboard"
    - "observation_window.window_days matches multi-day scoreboard span"
    - "check_id is ci:conformance-traces"
    - "surface is ci"
  </action>
  <verify>
    Run: `node --test bin/validate-traces.test.cjs 2>&1 | tail -30`

    Expected RED state:
    - 8 tests fail (AssertionError: observation_window undefined, or assertion on check_id/surface)
    - Previously passing tests (confidence tier tests, MCP field tests) remain GREEN
    - No module load errors or import errors
    - Exit code non-zero from the test runner
  </verify>
  <done>
    `node --test bin/validate-traces.test.cjs` shows exactly 8 new test failures (RED) for observation_window and v2.1 field tests. All previously passing tests continue to pass. No test errors (as opposed to assertion failures) in the new tests.
  </done>
</task>

</tasks>

<verification>
Run: `node --test bin/validate-traces.test.cjs 2>&1 | grep -E "^(ok|not ok)" | tail -20`
Expected: 8 lines starting with "not ok" for the new tests; remaining lines "ok".
</verification>

<success_criteria>
- bin/validate-traces.test.cjs has 8 new failing tests (RED state confirmed)
- All 8 tests target observation_window fields and v2.1 NDJSON fields (check_id, surface)
- Previously passing tests still pass
- Wave 0 RED state committed before Plan 02 implementation begins
</success_criteria>

<output>
After completion, create `.planning/phases/v0.20-05-evidence-confidence/v0.20-05-01-SUMMARY.md`
</output>
