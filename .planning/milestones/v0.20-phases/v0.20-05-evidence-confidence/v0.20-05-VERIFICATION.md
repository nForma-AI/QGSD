---
phase: v0.20-05
status: passed
verified: 2026-02-28
---

# Phase v0.20-05 Evidence Confidence — VERIFICATION

## Goal
Evidence-driven checks in `validate-traces.cjs` annotate `never_observed` paths with a confidence level and write `observation_window` metadata to `check-results.ndjson`, making it clear whether "never seen" means "truly safe" or "insufficient data".

## Success Criteria Verification

### SC-1: Every divergence/never_observed entry includes confidence field (PASS)
- `validate-traces.cjs` line 180: `computeConfidenceTier(scoreboardMeta.n_rounds, scoreboardMeta.window_days)` — computed once per run
- Lines 194, 207, 218, 240: `confidence` field spread into all divergence objects
- Verified: `grep -n "confidence" bin/validate-traces.cjs` returns 5 hits
- Confidence tier test suite: 12 boundary tests all pass (0,0)=low, (500,14)=medium, (10000,90)=high

### SC-2: check-results.ndjson entries include observation_window with all 5 required fields (PASS)
Smoke output confirms:
```
check_id:     ci:conformance-traces
surface:      ci
has_ow:       true
window_start: 2001-02-21T00:00:00.000Z (earliest scoreboard round)
window_end:   2026-02-28T23:17:16.495Z
n_traces:     534
n_events:     12671
window_days:  9134
```
All 5 fields present: window_start, window_end, n_traces, n_events, window_days.

### SC-3: Confidence thresholds produce correct tiers (PASS)
- `computeConfidenceTier(0, 0)` → `low`
- `computeConfidenceTier(500, 14)` → `medium` (exact medium boundary)
- `computeConfidenceTier(10000, 90)` → `high` (exact high boundary)

## Test Results
- 70/70 tests passing across all suites
- validate-traces.test.cjs: 32/32 (24 pre-existing + 8 new EVID tests)
- run-formal-verify.test.cjs: 11/11 (including ci:conformance-traces STEPS guard)
- conformance-schema.test.cjs: all passing
- write-check-result.test.cjs: all passing

## Requirements
- EVID-01: confidence field in divergence objects — CLOSED
- EVID-02: observation_window in NDJSON records — CLOSED

## Plans
1. v0.20-05-01: Wave 0 RED scaffold (8 failing tests) — COMPLETE
2. v0.20-05-02: Implementation of observation_window + v2.1 fields — COMPLETE
3. v0.20-05-03: Wire ci:conformance-traces into run-formal-verify STEPS — COMPLETE

## Commits
- `bd8863a5` feat(v0.20-05-01): add Wave 0 RED tests for EVID-01 and EVID-02
- `fc1bfb60` feat(v0.20-05-02): implement observation_window and v2.1 fields in validate-traces.cjs
- `46633376` feat(v0.20-05-03): wire validate-traces into run-formal-verify STEPS
