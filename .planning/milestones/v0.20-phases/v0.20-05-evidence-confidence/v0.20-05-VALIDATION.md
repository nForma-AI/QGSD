---
phase: v0.20-05
slug: evidence-confidence
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-28
---

# Phase v0.20-05 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.20-05`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.20-05`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in test runner (`node:test`) |
| **Config file** | package.json `scripts.test` → `node --test bin/*.test.cjs` |
| **Quick run command** | `node --test bin/validate-traces.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~30 seconds |
| **CI pipeline** | .github/workflows/test.yml — exists |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/validate-traces.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v0.20-05-01-W0 | 01 | 0 | EVID-01, EVID-02 | unit | `node --test bin/validate-traces.test.cjs` | ✅ (add tests) | ⬜ pending |
| v0.20-05-02-T1 | 02 | 1 | EVID-02 | unit | `node --test bin/validate-traces.test.cjs` | ✅ | ⬜ pending |
| v0.20-05-02-T2 | 02 | 1 | EVID-02 | unit | `node --test bin/validate-traces.test.cjs` | ✅ | ⬜ pending |
| v0.20-05-03-T1 | 03 | 2 | EVID-01, EVID-02 | integration | `npm test` | ✅ | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] Add 8 RED tests to `bin/validate-traces.test.cjs` covering:
  - `observation_window.window_start` is ISO-8601 string
  - `observation_window.window_end` is ISO-8601 string
  - `observation_window.n_traces` equals scoreboard round count
  - `observation_window.n_events` equals conformance log line count
  - `observation_window.window_days` is 0 when no scoreboard
  - `observation_window.window_days` matches scoreboard span for multi-day
  - `check_id` matches correct STEPS entry ID (ci:conformance-traces)
  - `surface` field is `ci`

*Tests must be RED before implementation begins. Implementation task turns them GREEN.*

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.
> These are surfaced during `/qgsd:verify-work` UAT.

*All phase behaviors have automated verification coverage.*

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 30s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.20-05`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 1 | — | — | — | scaffold |
| 1 | 2 | — | — | — | ⬜ pending |
| 2 | 1 | — | — | — | ⬜ pending |

**Phase validation complete:** pending
