---
phase: v0.20-05
plan: "02"
status: complete
completed: 2026-02-28
---

# v0.20-05-02 SUMMARY: Implement observation_window + v2.1 fields in validate-traces.cjs

## Objective
Implement observation_window and v2.1 fields in validate-traces.cjs to turn Wave 0 RED tests GREEN.

## What Was Built

### bin/validate-traces.cjs
- Added `buildObservationWindow(scoreboardMeta, n_events)` helper function
  - Reads quorum-scoreboard.json to extract earliest round date for `window_start`
  - Fail-open: missing/malformed scoreboard yields `n_traces=0, window_days=0, window_start=now`
  - Returns: `{ window_start, window_end, n_traces, n_events, window_days }`
- Added `_startMs = Date.now()` timer at entry point for `runtime_ms` measurement
- Updated all 4 `writeCheckResult()` call sites with full v2.1 field set:
  - `check_id: 'ci:conformance-traces'`
  - `surface: 'ci'`
  - `property: 'Conformance event replay through XState machine'`
  - `runtime_ms: Date.now() - _startMs`
  - `summary`: descriptive outcome string
  - `observation_window`: from `buildObservationWindow()`

### bin/validate-traces.test.cjs
- Fixed `runValidatorKeepTmp` helper to pass `CHECK_RESULTS_PATH` env var pointing to `tmpDir/.formal/check-results.ndjson`
- Fixed `window_days` multi-day test to construct a fresh tmpDir with explicit scoreboard fixture

## Test Results
- 32/32 tests passing (all GREEN)
- 8 new EVID-01/EVID-02 tests: RED → GREEN
- 24 previously passing tests: no regressions

## NDJSON Smoke Output
```
has_ow: true  check_id: ci:conformance-traces  surface: ci  n_events: 1
```

## Commits
- `fc1bfb60` feat(v0.20-05-02): implement observation_window and v2.1 fields in validate-traces.cjs

## key-files
created: []
modified:
  - bin/validate-traces.cjs (buildObservationWindow + _startMs + 4 writeCheckResult sites updated)
  - bin/validate-traces.test.cjs (helper fixes for isolated NDJSON reads)
