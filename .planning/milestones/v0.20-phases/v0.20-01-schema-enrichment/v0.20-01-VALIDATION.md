---
phase: v0.20-01
slug: schema-enrichment
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-28
---

# Phase v0.20-01 — Validation Strategy

> Generated by `qgsd-phase-researcher` during `/qgsd:plan-phase v0.20-01`.
> Updated by `qgsd-plan-checker` after plan approval.
> Governs feedback sampling during `/qgsd:execute-phase v0.20-01`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node:test` (built-in) with `node:assert` |
| **Config file** | none — uses `npm test` script in package.json |
| **Quick run command** | `node --test bin/write-check-result.test.cjs` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~10 seconds |
| **CI pipeline** | `.github/workflows/` — check existing |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `node --test bin/write-check-result.test.cjs`
- **After every plan wave:** Run `npm test`
- **Before `/qgsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| schema-update | 01 | 1 | SCHEMA-01 | manual | `node -e "const s=require('./formal/check-result.schema.json'); console.log(Object.keys(s.properties))"` | ✅ | ⬜ pending |
| wave-0-tests | 01 | 0 | SCHEMA-02 | unit | `node --test bin/write-check-result.test.cjs` (8 tests FAIL) | ✅ | ⬜ pending |
| writer-upgrade | 01 | 1 | SCHEMA-02 | unit | `node --test bin/write-check-result.test.cjs` (all PASS) | ✅ | ⬜ pending |
| caller-update | 01 | 2 | SCHEMA-03 | integration | `npm test` | ✅ | ⬜ pending |
| integration-verify | 01 | 3 | SCHEMA-01,02,03 | smoke | `node bin/run-formal-verify.cjs --only=tla 2>&1 \| head -20` | ✅ | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `bin/write-check-result.test.cjs` — Add 8 failing tests for v2.1 field validation:
  1. `writeCheckResult throws on missing check_id`
  2. `writeCheckResult throws on missing surface`
  3. `writeCheckResult throws on missing property`
  4. `writeCheckResult throws on missing runtime_ms`
  5. `writeCheckResult throws on missing summary`
  6. `v2.1 record has all required fields at top level`
  7. `v2.1 record without observation_window omits the field`
  8. `triage_tags defaults to empty array`

*All 8 tests must be written BEFORE `write-check-result.cjs` is modified.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Schema backward compat — 5-field NDJSON passes validation | SCHEMA-01 | Requires existing NDJSON fixture + ajv CLI | Run `npx ajv validate -s formal/check-result.schema.json -d <existing-5-field-record.json>` |
| All 21 active callers emit `runtime_ms` accurately | SCHEMA-03 | Timing fidelity requires live tool execution | Run each formal tool, check `runtime_ms` in NDJSON output is > 0 and plausible |

---

## Validation Sign-Off

Updated by `qgsd-plan-checker` when plans are approved:

- [ ] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 15s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/qgsd:execute-phase v0.20-01`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 8 test stubs | `node --test bin/write-check-result.test.cjs` | — | 8 | scaffold |
| 1 | schema + writer | `node --test bin/write-check-result.test.cjs` | 8 | 0 | ✅ sampled |
| 2 | 21 callers | `npm test` | — | — | ✅ sampled |
| 3 | integration | smoke run | — | — | ✅ sampled |

**Phase validation complete:** pending
