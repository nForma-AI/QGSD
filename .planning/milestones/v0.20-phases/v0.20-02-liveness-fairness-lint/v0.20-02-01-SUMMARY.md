---
phase: v0.20-02
plan: "01"
subsystem: liveness-fairness-lint
tags:
  - wave-0-scaffold
  - red-tests
  - nyquist-validation
  - tdd
dependency_graph:
  requires: []
  provides:
    - RED test scaffold for check-liveness-fairness.cjs contract
    - RED test for ci:liveness-fairness-lint STEPS entry
  affects:
    - v0.20-02-02 (implementation tasks must satisfy these tests)
    - v0.20-02-03 (STEPS wire-up must satisfy run-formal-verify test)
tech_stack:
  added: []
  patterns:
    - Node.js test runner (node:test)
    - Temp dir + env var injection pattern
    - NDJSON file verification
    - Dynamic config discovery testing
key_files:
  created:
    - bin/check-liveness-fairness.test.cjs (222 lines, 6 RED tests)
  modified:
    - bin/run-formal-verify.test.cjs (+8 lines, 1 RED test)
decisions:
  - Red tests specify full contract before implementation begins (Nyquist validation mandate)
  - LIVE-01 focuses on fairness result logic; LIVE-02 focuses on STEPS registry
  - Tests use temp dirs + env injection to avoid modifying actual repo state
  - Dynamic discovery test (Test 6) rejects hardcoded config list pattern
metrics:
  completed_at: "2026-02-28T21:37:00Z"
  duration_seconds: 120
  task_count: 2
  file_count: 2
  test_count: 7 (6 RED in check-liveness-fairness.test.cjs + 1 RED in run-formal-verify.test.cjs)

---

# Phase v0.20-02 Plan 01: Wave 0 Liveness Fairness Lint Scaffold

## Summary

Wave 0 of Phase v0.20-02 (Liveness Fairness Lint) creates RED test scaffolding that specifies the contract for `bin/check-liveness-fairness.cjs` BEFORE any implementation exists. This satisfies the Nyquist validation mandate: tests must exist and fail to prove they are meaningful (not vacuously passing against a stub).

**Output:** `bin/check-liveness-fairness.test.cjs` with 6 RED tests + one new RED test added to `bin/run-formal-verify.test.cjs` asserting the STEPS entry exists.

**Status:** Wave 0 scaffold COMPLETE. All tests FAIL (RED state). Wave 1 (implementation) can now begin.

---

## Tasks Completed

| Task | Name | Commit | Status |
|------|------|--------|--------|
| 1 | Create Wave 0 RED test scaffold for check-liveness-fairness.cjs | c1b99424 | DONE |
| 2 | Add RED test for ci:liveness-fairness-lint STEPS entry | 260b4088 | DONE |

---

## Test Results (RED State Confirmed)

### Task 1: check-liveness-fairness.test.cjs

**Test File:** `/Users/jonathanborduas/code/QGSD/bin/check-liveness-fairness.test.cjs`

All 6 tests FAIL as expected (script does not exist yet):

```
✖ syntax smoke: script loads without SyntaxError
✖ exits with code 0 always (inconclusive is not a failure)
✖ writes a check result entry to NDJSON
✖ result is 'pass' when all configs have fairness declarations
✖ result is 'inconclusive' when any config lacks fairness declaration
✖ dynamically discovers MC*.cfg files (rejects hardcoded list)

Results: 6 fail, 0 pass
```

**Test Coverage:**

- **Test 1:** Script loads without syntax errors (syntax smoke)
- **Test 2:** Exit code is always 0 (inconclusive is not a failure)
- **Test 3:** Writes NDJSON to CHECK_RESULTS_PATH env var
- **Test 4:** Result is "pass" when all configs have fairness declarations
- **Test 5:** Result is "inconclusive" when fairness missing; triage_tags includes "needs-fairness"
- **Test 6:** Dynamically discovers MC*.cfg files via glob (rejects hardcoded list)

**Environment Setup Pattern:**

- Temp dir + file paths for safe testing
- Env var injection: CHECK_RESULTS_PATH, FORMAL_TLA_DIR, FORMAL_SPEC_DIR
- NDJSON parsing and JSON validation

### Task 2: run-formal-verify.test.cjs

**Test File:** `/Users/jonathanborduas/code/QGSD/bin/run-formal-verify.test.cjs`

Added one new test after existing tests:

```javascript
test('LIVE-02: STEPS includes ci:liveness-fairness-lint entry', () => {
  const src = fs.readFileSync(path.join(__dirname, 'run-formal-verify.cjs'), 'utf8');
  assert.match(src, /ci:liveness-fairness-lint/);
  assert.match(src, /check-liveness-fairness\.cjs/);
  assert.match(src, /tool:\s*['"]ci['"]/);
});
```

**Results:**

- New test (LIVE-02): **FAILS** (RED state — entry not yet in STEPS)
- Pre-existing tests (9 tests): **ALL PASS** (no regressions)

```
✔ exits non-zero with descriptive error for unknown --only value
✔ syntax smoke: script loads without SyntaxError
✔ integration smoke: --only=generate filter resolves step list without Java
✔ timing (PERF-02): output includes Wall-clock line and runner completes within 120s
✔ parallelization smoke (PERF-01): all 8 TLA+ step IDs appear in output with --only=tla
✔ watch mode (DX-01): --watch flag starts process and does not exit immediately
✔ watch mode (DX-01): re-runs verification when machine file changes
✔ watch mode (DX-01): exits cleanly on SIGINT with exit code 0
✔ UNIF-03: --only=ci filter resolves ci:trace-redaction and ci:trace-schema-drift steps
✖ LIVE-02: STEPS includes ci:liveness-fairness-lint entry

Results: 9 pass, 1 fail
```

---

## Verification Summary

**REQUIREMENT COVERAGE:**

- ✅ **LIVE-01**: Tests 4–6 verify fairness declaration logic (pass, inconclusive, dynamic discovery)
- ✅ **LIVE-02**: New test in run-formal-verify.test.cjs asserts STEPS entry exists

**WAVE 0 CONTRACT:**

```
bin/check-liveness-fairness.cjs MUST:
  1. Exit with code 0 always (inconclusive is not a failure)
  2. Write NDJSON record to CHECK_RESULTS_PATH
  3. Result: "pass" when all configs have fairness declarations
  4. Result: "inconclusive" when any fairness is missing
  5. triage_tags: must include "needs-fairness" when inconclusive
  6. Dynamic discovery: glob MC*.cfg files (no hardcoded list)

bin/run-formal-verify.cjs STEPS MUST:
  1. Include entry: id='ci:liveness-fairness-lint', tool='ci'
  2. Script: 'check-liveness-fairness.cjs'
```

**Nyquist Sampling:**

- Automated command: `node --test bin/check-liveness-fairness.test.cjs` — FAILS (RED), ~340ms runtime
- Full suite: `npm test` — 9 pre-existing pass + 1 new fail, ~4.7s runtime
- All tests are deterministic (no flaky patterns detected)

---

## Deviations from Plan

**None** — plan executed exactly as written. All 6 tests fail in RED state. LIVE-02 test added to run-formal-verify.test.cjs and fails as expected.

---

## Next Steps (Wave 1)

Plan v0.20-02-02 will implement `bin/check-liveness-fairness.cjs` to make all 6 tests pass (GREEN).

Plan v0.20-02-03 will wire up the STEPS entry in `bin/run-formal-verify.cjs` to make the LIVE-02 test pass.

Both implementation plans depend on Wave 0 being complete (this plan) and can reference the contract specified by these RED tests.

---

## Self-Check

- ✅ bin/check-liveness-fairness.test.cjs exists (222 lines)
- ✅ bin/run-formal-verify.test.cjs modified (+8 lines for LIVE-02 test)
- ✅ Commit c1b99424: test(v0.20-02): add Wave 0 RED tests for check-liveness-fairness.cjs
- ✅ Commit 260b4088: test(v0.20-02): add RED test for ci:liveness-fairness-lint STEPS entry
- ✅ All 6 tests in check-liveness-fairness.test.cjs: RED (fail)
- ✅ All 9 pre-existing tests in run-formal-verify.test.cjs: GREEN (pass)
- ✅ LIVE-02 test in run-formal-verify.test.cjs: RED (fail)

**Self-Check Status:** PASSED
