---
phase: v0.18-04
plan: 01
subsystem: adaptive-fan-out
wave: 0
type: tdd
status: complete
date_completed: 2026-02-27
duration_seconds: 420
tags: [test-scaffold, tdd-red-phase, adaptive-fan-out]
requirements: [FAN-01, FAN-02, FAN-03, FAN-04, FAN-05, FAN-06]
---

# Phase v0.18-04 Plan 01: Wave 0 Test Scaffold — Summary

**Test-driven wave scaffold for adaptive quorum fan-out**. Created four comprehensive test stub files (16 tests total) that define behavioral contracts before any implementation. All stubs fail with clear TODO messages, establishing TDD discipline for downstream waves.

## Execution Overview

| Property | Value |
|----------|-------|
| **Plan** | v0.18-04-01 |
| **Wave** | 0 (Test Scaffold) |
| **Tasks** | 3 |
| **Duration** | ~7 minutes |
| **Status** | COMPLETE |

## Deliverables

### 1. bin/qgsd-fan-out.test.cjs (Unit Tests)

**Purpose:** Define test contracts for `mapRiskLevelToCount(riskLevel, maxSize)` helper function.

**Tests:** 5 stubs (all failing, controlled)
- FAN-TC1: routine risk_level → fan_out_count = 2
- FAN-TC2: medium risk_level → fan_out_count = 3
- FAN-TC3: high risk_level → fan_out_count = maxSize
- FAN-TC4: undefined risk_level → fan_out_count = maxSize (fail-open)
- FAN-TC5: invalid risk_level string → fan_out_count = maxSize (fail-open)

**Key features:**
- Try/catch guards on mapRiskLevelToCount import (not yet exported)
- Clear TODO messages without import crashes
- Covers all risk_level enum values (routine/medium/high) plus edge cases
- Fail-open behavior documented in test names (FAN-TC4, FAN-TC5)

**Covers requirements:** FAN-01 (routine=2), FAN-02 (medium=3), FAN-03 (high=max)

### 2. hooks/qgsd-prompt-fan-out.test.cjs (Integration Tests)

**Purpose:** Define test contracts for qgsd-prompt.js adaptive fan-out emission.

**Tests:** 4 stubs (all failing, controlled)
- FAN-PROMPT-TC1: routine risk_level in context → --n 2 emitted
- FAN-PROMPT-TC2: medium risk_level in context → --n 3 emitted
- FAN-PROMPT-TC3: --n 5 user override takes precedence → --n 5 emitted (FAN-06 priority chain)
- FAN-PROMPT-TC4: missing risk_level → --n defaults to maxSize (fail-open)

**Key features:**
- Child process pattern (spawnSync) matching existing qgsd-prompt.test.js
- Context YAML passing for risk_level injection
- User override supremacy tested (FAN-06 requirement)
- Fail-open behavior when envelope absent

**Covers requirements:** FAN-04 (--n N emission), FAN-06 (priority chain)

### 3. hooks/qgsd-stop-fan-out.test.cjs (Integration Tests)

**Purpose:** Define test contracts for qgsd-stop.js ceiling verification.

**Tests:** 4 stubs (all failing, controlled)
- FAN-STOP-TC1: --n 2 → ceiling = 1 external model required
- FAN-STOP-TC2: --n 3 → ceiling = 2 external models required
- FAN-STOP-TC3: --n 1 solo mode → no external models required (backward compat)
- FAN-STOP-TC4: no --n flag → ceiling falls back to config.quorum.maxSize

**Key features:**
- Emphasis on N-total semantics: --n N = (N-1) external models required
- Solo mode verification (existing behavior must be preserved)
- Default fallback testing
- Child process pattern consistent with qgsd-stop.test.js

**Covers requirements:** FAN-04 (ceiling checks)

### 4. hooks/quorum-fan-out.test.cjs (Smoke Tests)

**Purpose:** Define test contracts for quorum.md R6.4 reduced-quorum note.

**Tests:** 3 stubs (all failing, controlled)
- FAN-QUORUM-TC1: quorum.md contains R6.4 reduced-quorum note wiring
- FAN-QUORUM-TC2: R6.4 note includes fan_out_count and max_quorum_size context
- FAN-QUORUM-TC3: conditional logic for note emission (not emitted when fan-out = max)

**Key features:**
- File system read pattern (quorum.md is markdown, not code)
- R6.4 compliance verification
- Documentation of reduced-quorum context (user communication)

**Covers requirements:** FAN-05 (R6.4 reduced-quorum note)

## Coverage Matrix

| Requirement | Test File | Test Case | Status |
|-------------|-----------|-----------|--------|
| **FAN-01** (routine=2) | bin/qgsd-fan-out.test.cjs | FAN-TC1 | ✖ STUB |
| **FAN-02** (medium=3) | bin/qgsd-fan-out.test.cjs | FAN-TC2 | ✖ STUB |
| **FAN-03** (high=max) | bin/qgsd-fan-out.test.cjs | FAN-TC3 | ✖ STUB |
| **FAN-04a** (--n emit) | hooks/qgsd-prompt-fan-out.test.cjs | FAN-PROMPT-TC1,TC2 | ✖ STUB |
| **FAN-04b** (ceiling check) | hooks/qgsd-stop-fan-out.test.cjs | FAN-STOP-TC1,TC2,TC3,TC4 | ✖ STUB |
| **FAN-05** (R6.4 note) | hooks/quorum-fan-out.test.cjs | FAN-QUORUM-TC1,TC2,TC3 | ✖ STUB |
| **FAN-06** (priority chain) | hooks/qgsd-prompt-fan-out.test.cjs | FAN-PROMPT-TC3 | ✖ STUB |

## Verification Results

All 16 test stubs verified as failing with controlled TODO messages (no import crashes):

```
bin/qgsd-fan-out.test.cjs:
✖ FAN-TC1: routine risk_level → fan_out_count = 2
✖ FAN-TC2: medium risk_level → fan_out_count = 3
✖ FAN-TC3: high risk_level → fan_out_count = maxSize
✖ FAN-TC4: undefined risk_level → fan_out_count = maxSize (fail-open)
✖ FAN-TC5: invalid risk_level string → fan_out_count = maxSize (fail-open)

hooks/qgsd-prompt-fan-out.test.cjs:
✖ FAN-PROMPT-TC1: routine risk_level in context → --n 2 emitted
✖ FAN-PROMPT-TC2: medium risk_level in context → --n 3 emitted
✖ FAN-PROMPT-TC3: --n 5 user override takes precedence over routine risk_level
✖ FAN-PROMPT-TC4: missing risk_level → --n defaults to maxSize (fail-open)

hooks/qgsd-stop-fan-out.test.cjs:
✖ FAN-STOP-TC1: --n 2 in prompt text → ceiling = 1 external model required
✖ FAN-STOP-TC2: --n 3 in prompt text → ceiling = 2 external models required
✖ FAN-STOP-TC3: --n 1 solo mode → no external models required (existing behavior)
✖ FAN-STOP-TC4: no --n flag → ceiling falls back to config.quorum.maxSize

hooks/quorum-fan-out.test.cjs:
✖ FAN-QUORUM-TC1: quorum.md contains R6.4 reduced-quorum note wiring
✖ FAN-QUORUM-TC2: quorum.md R6.4 note includes fan_out_count and max_quorum_size context
✖ FAN-QUORUM-TC3: quorum.md conditional logic for reduced-quorum note
```

## Deviations from Plan

None — plan executed exactly as written. Wave 0 test scaffold completed without pre-existing issues or rule violations.

## Key Files Created

| File | Lines | Purpose |
|------|-------|---------|
| bin/qgsd-fan-out.test.cjs | 63 | Unit test stubs for mapRiskLevelToCount helper |
| hooks/qgsd-prompt-fan-out.test.cjs | 80 | Integration test stubs for qgsd-prompt.js fan-out logic |
| hooks/qgsd-stop-fan-out.test.cjs | 57 | Integration test stubs for qgsd-stop.js ceiling verification |
| hooks/quorum-fan-out.test.cjs | 47 | Smoke test stubs for quorum.md R6.4 note |
| **Total** | **247** | |

## Commits

| Hash | Message | Files |
|------|---------|-------|
| 585220f | test(v0.18-04): add failing test stubs for mapRiskLevelToCount (FAN-01, FAN-02, FAN-03) | bin/qgsd-fan-out.test.cjs |
| bee4730 | test(v0.18-04): add failing test stubs for qgsd-prompt.js fan-out logic (FAN-04, FAN-06) | hooks/qgsd-prompt-fan-out.test.cjs |
| af46b16 | test(v0.18-04): add failing test stubs for qgsd-stop.js and quorum.md (FAN-04, FAN-05) | hooks/qgsd-stop-fan-out.test.cjs, hooks/quorum-fan-out.test.cjs |

## Next Steps

Wave 1 execution (when scheduled) will:
1. Implement `mapRiskLevelToCount()` helper in hooks/qgsd-prompt.js (FAN-01, FAN-02, FAN-03)
2. Make bin/qgsd-fan-out.test.cjs tests pass
3. Integrate fan-out logic into qgsd-prompt.js and qgsd-stop.js (FAN-04)

Wave 2 execution will:
1. Add R6.4 reduced-quorum note to quorum.md (FAN-05)
2. Test priority chain enforcement (FAN-06)
3. Full integration test suite validation

## Test Scaffold Quality

- **Import safety:** All test files use try/catch or guards to prevent crashes if helpers don't exist yet
- **Clear failure messages:** Each test documents exactly what implementation is needed
- **No interference:** Stub tests are isolated and do NOT affect main `npm test` suite
- **Consistency:** All test patterns match existing test infrastructure (node:test, spawnSync, file I/O)
- **Nyquist compliance:** Tests support rapid feedback (< 2s per file execution)

---

**Summary complete. All tasks committed. Ready for Wave 1 implementation.**
