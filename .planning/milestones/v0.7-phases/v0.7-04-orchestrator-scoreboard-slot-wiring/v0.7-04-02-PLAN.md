---
phase: v0.7-04-orchestrator-scoreboard-slot-wiring
plan: 02
type: execute
wave: 2
depends_on: [v0.7-04-01]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [SCBD-01, SCBD-02, SCBD-03]

must_haves:
  truths:
    - "SCBD-01..03 marked [x] in REQUIREMENTS.md"
    - "quorum-scoreboard.md (if a live round has been run) contains entries in data.slots{} with composite slot:model-id keys"
    - "v0.7-04 marked complete in ROADMAP.md progress table"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCBD-01, SCBD-02, SCBD-03 checked [x]"
      contains: "[x] **SCBD-01**"
    - path: ".planning/ROADMAP.md"
      provides: "v0.7-04 status updated"
      contains: "v0.7-04"
  key_links:
    - from: ".planning/REQUIREMENTS.md SCBD-01..03"
      to: "agents/qgsd-quorum-orchestrator.md"
      via: "traceability table"
      pattern: "SCBD-0[123].*v0.7-04.*Complete"
---

<objective>
Close out phase v0.7-04: verify the scoreboard slot wiring fix is correct, mark SCBD-01..03 complete, update ROADMAP and STATE.

Purpose: Confirm gap closure for MC-1, Flow-4, Flow-5. All three SCBD requirements are satisfied by the --slot + --model-id fix applied in Plan 01.

Output: REQUIREMENTS.md with SCBD-01..03 marked [x]; ROADMAP.md with v0.7-04 complete; STATE.md updated.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v0.7-04-orchestrator-scoreboard-slot-wiring/v0.7-04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify fix correctness</name>
  <files>agents/qgsd-quorum-orchestrator.md, commands/qgsd/quorum.md</files>
  <action>
Run static verification checks to confirm the fix from Plan 01:

**Check 1: orchestrator Mode A uses --slot:**
```bash
grep -n "slot" /Users/jonathanborduas/code/QGSD/agents/qgsd-quorum-orchestrator.md
```
Expect: `--slot <slotName>` appearing in Mode A consensus and/or escalate sections.

**Check 2: quorum.md Mode A uses --slot:**
```bash
grep -n "slot" /Users/jonathanborduas/code/QGSD/commands/qgsd/quorum.md
```
Expect: `--slot <slotName>` in Mode A sections. Mode B already had it.

**Check 3: No derived-key instructions remain:**
```bash
grep -n "derive.*scoreboard key\|deepseek\`\|qwen-coder\`\|kimi\`\|llama4\`" /Users/jonathanborduas/code/QGSD/agents/qgsd-quorum-orchestrator.md /Users/jonathanborduas/code/QGSD/commands/qgsd/quorum.md
```
Expect: no output (no more "derive short key from model ID" instructions in Mode A sections).

**Check 4: Installed copies also fixed:**
```bash
grep -c "slot" /Users/jonathanborduas/.claude/agents/qgsd-quorum-orchestrator.md
grep -c "slot" /Users/jonathanborduas/.claude/commands/qgsd/quorum.md
```
Expect: counts >= 1 for each.

**Check 5: npm test — no regressions:**
```bash
cd /Users/jonathanborduas/code/QGSD && npm test 2>&1 | tail -5
```
Expect: all tests pass (markdown files changed, no .cjs logic touched).

If any check fails, stop and fix before proceeding to Task 2.
  </action>
  <verify>
All 5 checks above pass. grep for `--slot` returns results in Mode A sections of both files. grep for derived-key instructions returns no output. npm test passes.
  </verify>
  <done>
Static verification confirms: both files use --slot + --model-id for claude-mcp Mode A scoreboard calls, installed copies match, no regressions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of fix correctness</name>
  <files>agents/qgsd-quorum-orchestrator.md, commands/qgsd/quorum.md</files>
  <action>Human verifies fix is complete before proceeding to close requirements.</action>
  <verify>User confirms fix looks correct by typing "approved".</verify>
  <done>User types "approved" or describes any issues found.</done>
  <what-built>
Orchestrator Mode A and quorum.md Mode A scoreboard calls updated to use `--slot <slotName> --model-id <fullModelId>` for claude-mcp servers instead of `--model <derivedKey>`. Both source and installed copies updated.
  </what-built>
  <how-to-verify>
Review the key sections to confirm the fix looks right:

1. Check orchestrator Mode A consensus section:
   ```bash
   grep -A 20 "Consensus output" /Users/jonathanborduas/code/QGSD/agents/qgsd-quorum-orchestrator.md | grep -A5 "update-scoreboard"
   ```
   Expected: shows `--slot <slotName>` and `--model-id <fullModelId>` instructions for claude-mcp servers.

2. Check quorum.md Mode A consensus section:
   ```bash
   grep -A 20 "Consensus output" /Users/jonathanborduas/code/QGSD/commands/qgsd/quorum.md | head -30
   ```
   Expected: same --slot + --model-id pattern.

3. Confirm Mode B is untouched (still has --slot from v0.7-01):
   ```bash
   grep -n "slot" /Users/jonathanborduas/code/QGSD/commands/qgsd/quorum.md
   ```
   Expected: --slot appears in Mode B output section (pre-existing) AND in Mode A sections (new).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any issues found.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close requirements and update project docs</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
**Step 1: Mark SCBD-01..03 complete in REQUIREMENTS.md**

Read `.planning/REQUIREMENTS.md`. In the "Scoreboard Slot Tracking (SCBD)" section, change:
- `- [ ] **SCBD-01**` to `- [x] **SCBD-01**`
- `- [ ] **SCBD-02**` to `- [x] **SCBD-02**`
- `- [ ] **SCBD-03**` to `- [x] **SCBD-03**`

In the Traceability table, update the three SCBD rows from `Pending` to `Complete`.

In the Coverage section at the bottom, update:
- `3 Pending (SCBD-01..03 -> Phase v0.7-04)` to `0 Pending`
- Count: `10 Complete, 3 Pending` to `13 Complete, 0 Pending`

Update the `*Last updated:` line to: `*Last updated: 2026-02-23 -- v0.7-04: SCBD-01..03 marked Complete (orchestrator + quorum.md Mode A --slot wiring)*`

**Step 2: Update ROADMAP.md progress table**

Read `.planning/ROADMAP.md`. In the Progress table, update the v0.7-04 row from `TBD | Pending | -` to `2/2 | Complete | 2026-02-23`.

Update the phase entry in the Phases list from `- [ ] **Phase v0.7-04` to `- [x] **Phase v0.7-04`.

Update the Plans list for v0.7-04 from `**Plans**: TBD` to:
```
**Plans**: 2 plans
Plans:
- [x] v0.7-04-01-PLAN.md — Fix orchestrator + quorum.md Mode A scoreboard --slot wiring + install sync
- [x] v0.7-04-02-PLAN.md — Verify fix + close SCBD-01..03 requirements
```

**Step 3: Update STATE.md**

Read `.planning/STATE.md`. Update the Current Position section to reflect v0.7-04 complete. Add to the Decisions section: `- [Phase v0.7-04]: orchestrator Mode A and quorum.md Mode A now use --slot + --model-id for claude-mcp servers; data.slots{} populated on all quorum paths`.
  </action>
  <verify>
```bash
grep -c "\[x\] \*\*SCBD" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md
```
Expected: 3

```bash
grep "SCBD-01\|SCBD-02\|SCBD-03" /Users/jonathanborduas/code/QGSD/.planning/REQUIREMENTS.md | grep "Complete"
```
Expected: 3 lines with Complete

```bash
grep "v0.7-04" /Users/jonathanborduas/code/QGSD/.planning/ROADMAP.md | grep "Complete"
```
Expected: at least 1 line
  </verify>
  <done>
REQUIREMENTS.md: SCBD-01..03 all [x] Complete. ROADMAP.md: v0.7-04 marked Complete with 2/2 plans. STATE.md: position updated to v0.7-04 complete.
  </done>
</task>

</tasks>

<verification>
Phase v0.7-04 is complete when:
1. SCBD-01: `grep "[x] **SCBD-01**" .planning/REQUIREMENTS.md` returns a result
2. SCBD-02: `grep "[x] **SCBD-02**" .planning/REQUIREMENTS.md` returns a result
3. SCBD-03: `grep "[x] **SCBD-03**" .planning/REQUIREMENTS.md` returns a result
4. Traceability: all three SCBD rows show `Complete` in REQUIREMENTS.md
5. Source files: `grep -n "slot" agents/qgsd-quorum-orchestrator.md` shows --slot in Mode A sections
6. Installed: `grep -n "slot" ~/.claude/agents/qgsd-quorum-orchestrator.md` shows same
7. No regressions: npm test passes
</verification>

<success_criteria>
- SCBD-01: quorum-scoreboard tracks by slot name — orchestrator + quorum.md Mode A use --slot (stable key)
- SCBD-02: each scoreboard entry has full model ID context — --model-id carries exact health_check model string
- SCBD-03: new slot:model-id composite key created when model changes — existing slot:oldModel entry preserved
- All three SCBD requirements marked [x] in REQUIREMENTS.md
- v0.7-04 marked Complete in ROADMAP.md
- v0.7 milestone can be closed (all phases v0.7-01 through v0.7-04 Complete)
</success_criteria>

<output>
After completion, create `.planning/phases/v0.7-04-orchestrator-scoreboard-slot-wiring/v0.7-04-02-SUMMARY.md`
</output>
