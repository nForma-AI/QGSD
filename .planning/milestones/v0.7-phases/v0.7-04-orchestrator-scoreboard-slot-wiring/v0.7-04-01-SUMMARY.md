---
plan: v0.7-04-01
phase: v0.7-04-orchestrator-scoreboard-slot-wiring
status: complete
date: 2026-02-23
---

## Summary

Fixed orchestrator Mode A and quorum.md Mode A scoreboard update calls so claude-mcp servers use `--slot <slotName> --model-id <fullModelId>` instead of `--model <derivedKey>`. Both source files and installed copies at `~/.claude/` updated. Closes MC-1, Flow-4, Flow-5 gaps.

## Tasks Completed

| Task | Status | Notes |
|------|--------|-------|
| Task 1: Fix orchestrator Mode A scoreboard calls | Complete | Both Consensus output + Escalate sections updated |
| Task 2: Fix quorum.md Mode A scoreboard calls | Complete | Both Consensus output + Escalate sections updated |
| Task 3: Install sync | Complete | `node bin/install.js --claude --global` ran; installed copies verified |

## What Was Built

- `agents/qgsd-quorum-orchestrator.md` — Mode A Consensus output and Escalate sections now use dual-variant bash blocks: native agents keep `--model`, claude-mcp servers use `--slot <slotName> --model-id <fullModelId>`. Also updated team identity capture description to remove derived-key language.
- `commands/qgsd/quorum.md` — Same dual-variant fix applied to Mode A Consensus output and Escalate sections. Mode B (pre-existing correct usage) left unchanged. Team identity capture description updated.
- Install sync propagated both fixes to `~/.claude/agents/qgsd-quorum-orchestrator.md` and `~/.claude/commands/qgsd/quorum.md`.

## Key Decisions

- **Escalate section expanded**: Instead of "same pattern as Consensus output above" back-reference, both Escalate sections now carry the full explicit dual-variant bash block. This prevents future misinterpretation when Escalate is read in isolation.
- **Team identity capture descriptions updated**: The sentence describing derived-key derivation (e.g., `deepseek-ai/DeepSeek-V3` → `deepseek`) was replaced with instructions to capture full model ID and use it as `--model-id`. Consistent with the scoreboard update blocks.
- **Mode B unchanged**: Mode B already uses `--slot + --model-id` correctly from v0.7-01 INT-04. Not modified.

## Verification

```
grep -c "slot" agents/qgsd-quorum-orchestrator.md  → 16 (source)
grep -c "slot" commands/qgsd/quorum.md             → 8 (source)
grep -c "slot" ~/.claude/agents/qgsd-quorum-orchestrator.md → 16 (installed)
grep -c "slot" ~/.claude/commands/qgsd/quorum.md   → 8 (installed)
grep "derive.*key|→ key \`deepseek\`" ...          → no output (correct)
```

## Self-Check: PASSED

All plan must_haves satisfied:
- [x] `--slot <slotName>` appears in Mode A consensus + escalate sections of both files
- [x] No "derive short key" instructions remain for claude-mcp servers in Mode A
- [x] Native agents (claude, gemini, opencode, copilot, codex) still use `--model` flag — unchanged
- [x] Installed copies at `~/.claude/` reflect the fix
- [x] Commit: `6ec43b0`

## key-files

### created
- .planning/phases/v0.7-04-orchestrator-scoreboard-slot-wiring/v0.7-04-01-SUMMARY.md

### modified
- agents/qgsd-quorum-orchestrator.md
- commands/qgsd/quorum.md
